# Фаза 8. Тестирование и совместимость

QUIC — шифрованный бинарный протокол поверх ненадёжного транспорта. Ошибка в нём
проявляется как «иногда у одного клиента страница не грузится». Поэтому тесты
здесь не заключительный этап, а инструмент разработки: каждая фаза из `00` §6
имеет собственный набор и без него не считается завершённой.

---

## 1. Уровень 1: юнит-тесты на векторах RFC

Встраиваются в существующий `tests/unit/` (`runner`), собираются при
`INCLUDE_HTTP3=yes`. Полный перечень — в конце каждого документа фазы. Ключевое:
для QUIC существуют **официальные байтовые векторы**, и они закрывают самую
опасную часть.

| Источник | Что проверяет |
|---|---|
| RFC 9000 §A.1–A.3 | varint, восстановление номера пакета, кодирование ACK |
| **RFC 9001 Приложение A** | Initial-секреты, AEAD, header protection, Retry-тег, ChaCha20 — целиком, байт в байт |
| **RFC 9204 Приложение B** | QPACK: 5 сценариев, включая блокировку потока и эвикцию |
| RFC 7541 Приложение B/C | Хаффман (уже покрыт `test_hpack.c`, переиспользуется) |
| RFC 8448 | HKDF-Expand-Label |

Если Приложение A из RFC 9001 сходится — криптографический слой почти
наверняка верен. Это самая высокая отдача на строку теста во всём проекте.

## 2. Уровень 2: детерминированный сквозной тест

`tests/unit/test_quic_conn.c` — два объекта соединения в одном процессе,
соединённые эмулятором сети. Требует минимального **клиента** (~600 строк):
клиентская роль в `quicconn`, клиентские Initial-секреты, отправка ClientHello
через тот же `quictls`. Затраты окупаются: без своего клиента фазу 4 невозможно
отлаживать пошагово.

Эмулятор сети управляет:

- потерями (0 / 5 / 20 %, детерминированный ГПСЧ с фиксированным seed);
- переупорядочиванием (сдвиг до 3 пакетов);
- дублированием;
- задержкой (постоянной и джиттером);
- ограничением полосы (для проверки congestion control);
- обрезкой датаграмм (проверка обработки MTU).

Время инжектируется через `quic_time_set_source()` — тест «проматывает» минуты
за миллисекунды, что делает проверяемыми idle timeout, PTO-экспоненту,
persistent congestion, retire CID через 3×PTO и key update.

Сценарии: рукопожатие; передача 100 МБ по одному потоку; 100 параллельных
потоков; отмена потока в середине; миграция (смена адреса в эмуляторе);
key update в обе стороны; CONNECTION_CLOSE в каждом состоянии; исчерпание
flow control и его разблокировка.

### 2a. Ход работ

**Сделано (2026-08-09): слой помех в `quicclient`.** Не то, что описано выше —
это не два объекта в одном процессе с инжектированным временем, а настоящий
сокет с потерями, переупорядочиванием и дублированием: `--loss`, `--loss-in`,
`--reorder`, `--dup`, `--seed`. Взято дешёвое и немедленно полезное; детерминированный
внутрипроцессный стенд с `quic_time_set_source()` (idle timeout, экспонента PTO,
persistent congestion, retire CID через 3×PTO за миллисекунды) остаётся.

**Потери разведены по направлениям, и это не мелочь.** Входящие заставляют
восстанавливаться **сервер** — это интересное направление. Исходящие заставляют
восстанавливаться клиента, а у него восстановления нет вовсе (`quicloss` в нём не
используется, он минимален по устройству), поэтому потерянное им пропадает
навсегда. Смешивать два этих числа значило бы считать провалом харнесса провал
сервера.

Seed обязателен: воспроизводимость — единственное, что отличает протокольный тест
от лотереи, и «в прошлый раз проходило» есть характерное свойство рандомизированного
теста, который тихо сломан.

**Найдены два настоящих бага сервера, оба невидимые на loopback.**

1. **PTO не слал пробник** (RFC 9002 §6.2.4). PTO ничего не объявляет
   потерянным — он говорит, что прошёл круг без подтверждений, — и выход только
   один: заставить пира что-нибудь сказать. Без пробника соединение вставало
   навсегда: детектор потерь объявляет пакет потерянным лишь по подтверждению
   **более позднего**, а если пропал последний отправленный пакет, более позднего
   не существует. Ровно это и происходило, когда клиент ронял один ответ.
2. **Короткие 1-RTT-пакеты не уходили вообще.** Защита заголовка берёт выборку
   16 байт, начиная с четвёртого байта после номера пакета (RFC 9001 §5.4.2), так
   что шифротекста должно быть не меньше; пакет из одного `PING` — а пробник PTO
   это ровно он — давал один байт полезной нагрузки, `quichp_apply` падал, и
   `__build_packet` возвращал 0. Пробник строился и молча не отправлялся.

Второй баг маскировал первый: после починки PTO ничего не изменилось, и это
списалось бы на «PTO не помогает», если бы не счётчики. Их пришлось развести:
`pto_fired` рядом с новым `pto_probes_sent` — 20 против 12 отправленных датаграмм
и назвали место. Один счётчик этого сказать не мог.

Отдельно стоит записать **две ошибки метода**, обе стоили времени:

- сервер не перезапускался после пересборки, и «исправление не помогло» было
  проверкой старого бинарника;
- отладочный `log_error` внутри `quicconn_tick` «чинил» баг: syslog-запись
  сдвигала тик, пробник склеивался с ACK, полезная нагрузка вырастала выше
  четырёх байт, и защита заголовка начинала работать. Диагностика, меняющая
  тайминг, на этом слое лжёт — счётчики (расслабленные атомики) не лгут.

Проверено после починки: 18 комбинаций `--loss-in` × seed проходят, плюс
переупорядочивание до 40 %, дублирование 25 % и их смеси. Под помехами проверены
и пути, которые до сих пор были только рассуждением: `--key-update --reorder 40`
(это и есть та переупорядоченность, ради которой держится `rx_prev`),
`--cid --loss-in` (ретрансмиссия `NEW_CONNECTION_ID`), `--migrate --loss-in`,
`--path-challenge --loss-in 30`.

Две проверки самого харнесса пришлось сделать детерминированными, а не
«подождать и посмотреть»: после валидации пути и после провокации протокольной
ошибки серверу нечего слать по своей инициативе, поэтому тест теперь сам шлёт
`PING` — §10.2.1 велит закрывающемуся эндпоинту повторять `CONNECTION_CLOSE`
**в ответ на входящий пакет**, а не по таймеру.

**Тот «открытый вопрос» закрыт, и не в пользу гипотезы о баге сервера.**
`decryption failed at level 3` оказался **stateless reset**: приборка показала
`type=254`, 29 байт случайного содержимого. Первый `CONNECTION_CLOSE` терялся, за
время ожидания истекал период закрытия, соединение забывалось — и на следующий
`PING` сервер честно отвечал по §10.3. Клиент их не узнавал, потому что reset
специально неотличим от 1-RTT-пакета с коротким id, и «не расшифровалось» —
единственный симптом, который у него есть.

Теперь узнаёт: сравнивает последние 16 байт с токенами из `NEW_CONNECTION_ID`
**до** разбора, а не после неудачи. Заодно это первая проверка того, что
объявленные токены совпадают с посылаемыми — раньше `stateless_reset_sent`
считался, но никто не подтверждал, что пир их распознаёт.

**Урок, повторившийся четыре раза: на теряющем пути пассивное ожидание — не
проверка.** Сервер по своей инициативе не шлёт ничего, а восстановление
запускается подтверждением более позднего пакета, так что молчащий клиент не даёт
ему ни повода ответить, ни возможности заметить потерю. Все циклы ожидания в
тесте теперь сами шлют `PING`, а челлендж переспрашивается (§8.2.1 прямо это
предусматривает — `quicclient_path_challenge` раньше отказывался повторять).
После этого 21 прогон под помехами проходит без исключений.

**Тот вопрос разобран, и за ним нашлись ещё два бага — оба про восстановление.**
Путь был такой: счётчики сказали `cids_issued 4` = `cids_announced 4` при трёх
дошедших, то есть объявление ушло и пропало; `packets_lost 0` и `pto_fired 0`
сказали, что потерю никто не заметил. Дальше — юнит-тест на `quicloss` с
инжектированным временем (`test_quic_loss_stranded_packet`), потому что модуль
чистый и вопрос «взводится ли таймер» решается без сервера. Тест показал, что
**`quicloss` прав в обоих путях**: подтверждение более позднего пакета возвращает
кадры застрявшего, а одинокий пакет в полёте взводит PTO. Значит, дело выше.

1. **Потерянные кадры обрабатывались в двух местах, и они расходились.** Узнать о
   потере можно двумя способами — из подтверждения более позднего пакета и по
   таймеру потерь, — и ветка таймера в `quicconn_tick` умела только `CRYPTO` и
   `STREAM`, молча выбрасывая `NEW_CONNECTION_ID` и `NEW_TOKEN`. На тихом
   соединении, где второго подтверждения не будет никогда, таймер — единственный
   путь, и объявление не переотправлялось. Дублирование убрано: обе ветки зовут
   одну `__requeue_lost()`.
2. **Таймеры соединения проверялись раз в секунду.** Отладочная строка показала
   ровно это: `timeout` на 27 мс впереди `now`, `inflight 1`, `hs 1` — таймер
   взведён правильно, — но `quicconn_tick` за две секунды вызвался один раз, и
   дедлайн просто попал между подметаниями воркера. PTO в десятки миллисекунд с
   секундной гранулярностью не работает. Теперь таймеры соединения крутятся и на
   приёме датаграммы: соединение уже залочено и обрабатывается, так что это
   бесплатно, и любое соединение, которое хоть чем-то обменивается, получает
   разрешение таймеров по своему же трафику. Полностью это закрыто следом
   (`01` §6a): у эндпоинта появился свой `timerfd` на ближайший дедлайн среди
   соединений, так что и молчащее соединение просыпается по своему PTO, а не по
   подметанию. Восстановление после потери — 20–50 мс вместо секунды.

После обеих починок строгая проверка замены проходит 10 seed'ов из 10, а полная
матрица помех — 27 прогонов из 27.

### 2b. Стенд: сделано (2026-08-12)

**`tests/unit/test_quic_conn.c` — то самое, что описано выше**: настоящее
серверное соединение (`quicconn_accept` → `quicconn_recv`/`_tick`/`_send`) и
клиент из `tests/quicclient` в одном процессе, между ними эмулятор пути, время
через `quic_time_set_source()`. Восемь сценариев, весь `runner` — две секунды,
трасса событий побайтово совпадает между прогонами.

Чего это стоило в продакшн-коде — трёх швов, и ни одного «если тест»:

- `quicendpoint_t::send_hook` — куда уходят датаграммы вместо сокета. Хук, а не
  фальшивый fd: подделывать надо не сокет, а *путь* — придержать, потерять,
  продублировать, доставить позже, — и sendmsg этого не выражает;
- у клиента — транспорт без сокета (`quicclient_connect_inproc`,
  `quicclient_deliver`, `quicclient_flush`): цикла нет вовсе, порядок событий
  принадлежит вызывающему;
- `STAND_TRACE=1` — весь обмен построчно с виртуальными временами, плюс лог
  клиента. Переключатель, а не пересборка: это разница между «спросить стенд» и
  «отредактировать стенд».

**Стенд обязан моделировать `want_write`.** Первая версия этого не делала — и
64 КБ ответа лежали в потоке, пока через тридцать секунд не срабатывал idle
timeout. Выглядело как вставший congestion window. В сервере ответ обработчика
попадает в очередь эндпоинта (`quicendpoint_wake`) и будит воркер через
eventfd; без этого «приложение что-то произвело» вообще не событие. Теперь стенд
разбирает `ep.tx_head` ровно как `__endpoint_tick`.

**И «подталкивать» часы на дедлайне.** `epoll_wait` возвращается *не раньше*
срока, поэтому проверка вида `now > deadline` в сервере всегда видит уже ушедшее
время. Виртуальные часы стоят ровно на дедлайне, тик ничего не делает, и стенд
топчется на месте. Если после тика дедлайн тот же — время двигается на
микросекунду вперёд.

**Найдено сразу, за один вечер вместо предыдущего.**

1. **ClientHello больше не влезает в пакет, а клиент слал только первый.**
   OpenSSL 3.5 по умолчанию предлагает гибридный постквантовый key share, и
   ClientHello вырос до 1518 байт. `__flush` строил одну датаграмму за вызов,
   вторая половина оставалась ждать «чего-нибудь ещё». Сервер получал половину
   ClientHello, честно отвечал голым ACK — и **весь его флайт уезжал только
   после следующей датаграммы клиента**: рукопожатие стоило лишнего круга. А на
   пути, который теряет этот ACK, стоило соединения целиком: ни у одной стороны
   не было в полёте ничего ack-eliciting, значит, ни PTO, ни ретрансмиссии — обе
   ждали до idle timeout. Это баг харнесса, но он же и объясняет, на что похож
   `handshakeloss`.
2. **Настоящая утечка в сервере**: `__discard_space` звал `quicack_init`, который
   делает `memset`, — массив диапазонов предыдущего пространства просто терялся.
   Два пространства выбрасываются на каждом рукопожатии, то есть пара сотен байт
   на соединение, навсегда. ASan против простаивающего сервера этого не видит:
   там нет соединений. Стенд открывает и закрывает их внутри одного процесса.
3. **Метод: ложная гипотеза стоит теперь минуту.** Пока причина №1 не была
   видна, версия была «OpenSSL нужен второй `SSL_do_handshake`». Правка в
   `quictls_advance` (цикл по ходам) — сборка — прогон — не изменилось ничего;
   откат. Вчера такая проверка стоила бы два-три прогона по две минуты с
   воспроизведением «раз из трёх» и закончилась бы верой в гипотезу.

Ещё одна ошибка чтения, стоящая записи: `timer 0` в трассе я принял за «PTO не
взведён», а это соединение уже было закрыто — печаталось `s->conn != NULL ? ... : 0`.
Трасса должна печатать различающее состояние, а не удобное: следующая версия
строки печатает «connection gone» словом.

**Числа, которые теперь проверяются как числа.** Чистое рукопожатие — ровно два
круга (40 мс при RTT 20 мс), не больше и не меньше. С потерянным первым флайтом
сервера — 726 мс: 666 = kInitialRtt(333 мс) × 2, плюс три однонаправленных
конца. PTO-экспонента проверяется по временам отправок при чёрной дыре в
обратную сторону: каждый следующий интервал не меньше 1,7× предыдущего. Idle
timeout — умирает после дедлайна и ни микросекундой раньше. 64 КБ через путь с
20 % потерь в обе стороны — байт в байт.

**Список §2 покрыт целиком** (последние два пункта — в §2j).

### 2c. Потерянная половина ClientHello: у клиента появился PTO

**Сделано сразу следом (2026-08-12).** Клиент стенда получил **единственный
кусок восстановления, без которого нельзя** (`quicclient_next_timeout` /
`quicclient_tick`): §6.2.2.1 прямо говорит, что разблокировать сервер —
обязанность клиента, и без этого «тупик» был бы свойством харнесса, а не
сервера. Это **не** RFC-восстановление: списка отправленных пакетов у клиента
нет и входящие ACK ни с чем не сверяются (это `quicloss`, а клиент минимален по
устройству). Это грубое правило, ради которого пробник и существует, — пир
замолчал, надо его толкнуть, — с переотправкой флайта, а не голого PING
(§6.2.4). Ограничено дважды: взводится только до прихода `HANDSHAKE_DONE` и не
более пяти раз, иначе инструмент превратился бы в машину, держащую соединения
открытыми. База 666 мс = kInitialRtt × 2; оценщика RTT нет намеренно — клиент
ничего не измеряет, и «оценка» была бы фиксированным числом в костюме. Цикли
инструмента (`quicclient_run`/`_pump`) крутят таймер сами, стенд — вручную,
потому что там время принадлежит вызывающему.

Плюс два точечных скрипта в эмуляторе — `drop_nth_to_server` и
`delay_nth_to_server`, по порядковому номеру датаграммы. Процентом это не
выразить: «потерять вторую» — сценарий, а не вероятность, а
переупорядочивание по проценту сдвигает все датаграммы одинаково и не
переупорядочивает ничего.

**Сценарий «потеряна вторая датаграмма ClientHello» и есть портрет
`handshakeloss`,** и трасса читается целиком:

```
[1000000] c->s 1314 bytes, due 1010000
[1000000] c->s 1200 bytes DROPPED (scripted)
[1010000] s->c 1200 bytes                     <- один ACK, ничего больше
[1010000] crypto in I len=1264 consumed=1264; in flight I/H/A 0/0/0; timer 31010000
[1686000] client PTO -> resending the flight
[1696000] server: crypto in I len=1518; in flight 1/2/1; timer 2362000
```

То есть: сервер держит 1264 из 1518 байт ClientHello, отвечать ему нечем, в
полёте у него **ничего** ack-eliciting — значит, PTO не взводится, и
единственный его таймер — idle timeout через тридцать секунд. MUST'а он не
нарушает: §6.2.2.1 требует пробника от того, у кого не подтверждён **свой**
Handshake-пакет, а сервер их ещё не отправлял. Соединение спасает клиентский
пробник, и тест утверждает именно это — `client.pto_fired >= 1` **и**
`conn->loss.pto_count == 0`, то есть *чьё* восстановление сработало. Цена —
ровно один клиентский PTO (600–900 мкс… мс: 666 + рукопожатие).

**Второй сценарий — те же две половины, но переставленные, ничего не
потеряно.** Он проверяет ту часть сборки CRYPTO, до которой доходит только
переупорядочивающий пир: вторая половина приезжает первой и **обязана лежать в
дырке**, потому что склеенное сообщение TLS отверг бы так, что это выглядело бы
поломкой своего кодека. Наблюдаемо — `crypto in I len=0 consumed=0` на первом
пакете (непрерывный префикс пуст, TLS не получает ничего) и `len=1518` на
втором. Пробников нет ни у кого: переупорядочивание — не потеря, и путать их
означает превращать заминку на 25 мс в заминку на 666.

Итого в стенде десять сценариев.

### 2d. Узкое место: полоса, очередь и tail drop

**Сделано (2026-08-12).** У эмулятора появилась модель, которой не хватало:
`bandwidth_bps` + `queue_pkts`. Датаграмма ждёт освобождения линии
(`link_free_*` на направление), занимает её на `len × 8 / bps`, и только потом
летит `delay_us`. Очередь — это те датаграммы, чья сериализация ещё не началась
(поле `start_us`), то есть её глубина считается, а не оценивается; полная
очередь роняет **хвост**. Tail drop считается отдельным счётчиком
(`queue_dropped_*`): причина другая, чем у случайной потери — это собственный
перебор отправителя, вернувшийся к нему, — и тест, который бы их не различал,
докладывал бы баг отправителя как невезение.

Это и есть interop-путь в процессе:
`simple-p2p --delay=15ms --bandwidth=10Mbps --queue=25`. Случайная потеря
спрашивает «работает ли восстановление»; очередь спрашивает **«держится ли
отправитель внутри пути»**, а это разный код — и именно на нём проект дважды
попадался (§3i).

**Регрессия §3i выражена отношением, потому что и отказ был отношением**
(«пир получил меньше десятой части отправленного»). Замер сейчас: 512 КБ
доехали байт в байт за **497 мс против 419 мс чистой сериализации — линия
загружена на 84 %**, отправлено 485 датаграмм, доставлено 426, tail drop 59.
Планка стоит на трёх четвертях: там, где она разделяет 88 % и 10 %, а не там,
где она фиксирует сегодняшнее число.

Выше её и не поставить, и это свойство пути, а не отправителя: очередь (25
пакетов) **меньше** произведения полосы на задержку (10 Мбит/с × 30 мс ≈ 31),
поэтому потерям-ориентированный контроллер обязан её перебрать, чтобы найти. Из
59 отбросов **57 пришлись на одну пачку длиной ~57 мс** — два круга, ровно
столько, сколько нужно, чтобы наполнить очередь и потом об этом услышать, — и
только 2 на оставшиеся 380 мс. Это учебный перебор на выходе из slow start;
записано здесь, чтобы следующий, кто увидит пачку потерь в начале передачи, знал,
что это говорит путь.

**Второй сценарий — 2 % потерь поверх того же узкого места** (форма
interop-кейса `transferloss`). Здесь встречаются два режима отказа сразу:
ретрансмиссии приходится пролезать по пути, который уже забит, — там жил тупик
«ретрансмиссия, запертая flow control» из §3i, — а FIN проверяется **отдельным
утверждением от байтов**, потому что §3t был ровно этим: все байты доехали, поток
не закончился, и пир ждал 420 секунд кадра, который ехал внутри дубликата.

Двенадцать сценариев, 101802 проверки, весь `runner` — 1,5 с.

### 2e. Параллельные потоки и кредит на потоки

**Сделано (2026-08-13).** Два сценария, которые до сих пор проверялись только
h2load против живого сервера, то есть недетерминированно и не в CI.

**Тридцать два потока одновременно, под 15 % потерь в обе стороны.** Отправка
делит одно соединение между всеми открытыми потоками по кругу, а во всех прежних
тестах поток был один. Что здесь может сломаться — не «доходят ли данные», а
«доходят ли данные *этого* потока по этому потоку»: смещение, применённое к
чужому потоку, или кадр, построенный для одного и учтённый другому, дают
соединение, где всё работает, а один ответ незаметно является другим. Поэтому у
каждого потока своё тело **и своя длина** (поток, получивший чужие данные, иначе
остался бы правильного размера), и проверяется каждый байт. «Параллельно» —
утверждение, а не намерение: `conn->stream_count >= 32` в момент, когда все
ответы поставлены в очередь. Потери сверху ради того же взаимодействия, что и в
§2d: потерянные кадры тридцати двух потоков возвращаются через одно окно
перегрузки.

**Сто пятьдесят потоков подряд при объявленном лимите сто.** Это стена, которую
нашёл чужой клиент и не мог найти ни один наш тест (§7a): кредит выдаётся на
поток и не возобновлялся, поэтому соединение кончалось после
`initial_max_streams_bidi` запросов — не медленный путь, а именно стена.
Возобновляет его `MAX_STREAMS`, но только для потоков, которые транспорт
действительно отпустил, а отпускает он чужой поток лишь когда закончены **оба**
направления **и приложение прочитало то, что пришло**. Поэтому помощник
`__serve` читает запрос, который собирается ответить, и зовёт
`quicconn_consumed` — стенд, который только пишет, эту половину молча не
проверял бы. Замер: `served 150 of 150 (allowance 100), closed 149` — то есть
кредит возобновлялся сто пятьдесят раз, а `stream_count` остался меньше лимита,
что и делает пообъектный обход `conn->streams` на каждый пакет коротким.

Клиенту для этого понадобилось две вещи: `CLIENT_MAX_STREAMS` 16 → 64 и
`quicclient_stream_release()` — иначе слоты кончились бы в харнессе задолго до
кредита на сервере, и тест мерил бы себя.

Четырнадцать сценариев, 101948 проверок, весь `runner` — 1,8 с.

### 2f. Отмена потока: найден неверный final size

**Сделано (2026-08-13).** Два сценария отмены — со стороны отправителя
(`RESET_STREAM` посреди тела) и со стороны получателя (`STOP_SENDING`, на который
§3.5 обязывает ответить `RESET_STREAM` с тем же кодом). Клиенту для второго
понадобилось уметь слать `STOP_SENDING` и **запоминать** входящий
`RESET_STREAM` (раньше он его печатал): «поток отменён» неотличимо от «поток
встал», пока это не записано.

**Найден настоящий дефект, и он ровно про то, ради чего отмена существует.**
`RESET_STREAM` сообщал `send.write_off` — всё, что **записало приложение**, — а
§4.5 определяет final size как «на единицу больше смещения байта с наибольшим
смещением, **отправленного** в поток». Разница — это брошенный хвост, то есть
весь смысл отмены. Замер на стенде: ответ в 131072 байта, отменённый после
19936 отправленных, объявлял пиру **131072**.

Чем это плохо, кроме неточности: §4.5 обязывает получателя учесть final size в
**канальном** контроллере потока. Значит, пир считал бы съеденными 128 КБ нашего
канального окна там, где мы потратили 20, — а предел применяет он. Достаточно
нескольких отменённых ответов, и мы получаем `FLOW_CONTROL_ERROR` за данные, на
которые имели право. Починка — одна строка (`sent_off` вместо `write_off`), тест
на неё — `final_size < записанного` и `final_size >= уже полученного`.

Шестнадцать сценариев, 101983 проверки.

### 2g. Вторая половина §4.5, и ещё один дефект рядом с ней

**Сделано следом (2026-08-13).** Приёмная сторона того же абзаца:
`quicstream_on_reset` учитывал final size в окне **потока**, но в
`conn->recv_flow` не попадало ничего — канальное окно кредитовалось только
фактически пришедшими байтами. Пир, отменивший поток, часть которого потерялась
в пути, считает съеденным весь final size; мы — только дошедшее.

Ошибки при этом не возникает (пир строже нас), и потому это самый неприятный
сорт дефекта: предел, который мы объявляем, выводится из **нашего** счёта
(`quicflow_should_update` даёт `used + auto_window`), так что эффективное окно
пира уменьшается на брошенный хвост каждой отменённой им передачи —
накапливаясь, пока он не встанет. Приходит это как «соединение через некоторое
время стало медленным», то есть как что угодно.

Починка — учесть в канальном окне разницу между новым и прежним «использованным»
окна потока, ровно один раз, на кадре `RESET_STREAM`.

**Рядом нашёлся второй дефект, и он тяжелее.** `RESET_STREAM` для потока,
которого мы никогда не видели, **молча игнорировался**: обработчик звал
`__stream_find` и на промахе возвращал «всё в порядке». Так теряются сразу две
вещи. §3.2: кадр обязан **открыть** поток, как открыл бы его `STREAM`-кадр, —
иначе лимит на число потоков не применяется ни к одному id, приехавшему в виде
сброса, и пир может сбрасывать сколько угодно ids даром. И §4.5: учитывать final
size не в чем, потока-то нет.

Случай не экзотический: это ровно то, что происходит, когда **всё**, что пир
успел отправить в поток, потерялось, а сброс дошёл. Ни один тест, который не
теряет данные потока целиком, этого не покажет.

Починка: четырёхслучайная логика `__stream_for_send_frame` (поток есть / нам на
него нельзя слать / наш, уже освобождённый / чужой невиданный → открыть) была
ровно тем, что нужно, с одной заменой — `quicstream_can_receive` вместо
`quicstream_can_send`. Обобщена в `__stream_for_frame(..., permitted)` с двумя
тонкими обёртками; второй копии этих четырёх случаев в проекте быть не должно.

Клиент научился слать `RESET_STREAM` (final size = `out.sent_off`, то есть по
§4.5), без чего оба сценария были бы непроверяемы.

**Оба теста проверены с той стороны, с какой такие тесты только и имеют смысл:**
на откаченной правке сервера они падают — «хвост не учтён в канальном окне» и
«id не учтён как открытый», — и проходят с ней. Тест регрессии, который не был
показан падающим, не тест.

Восемнадцать сценариев, 102012 проверок.

### 2h. Окно, до которого наконец можно дотянуться

**Сделано (2026-08-13).** Последний пункт списка §2 — исчерпание flow control и
разблокировка. Клиент этого не выражал вовсе: объявлял 64 МБ и **никогда** не
слал `MAX_DATA`/`MAX_STREAM_DATA`, то есть ни одно окно в обмене не было
достижимо. Предел, до которого нельзя дотянуться, — предел, который не
проверяется, а поднимающий его код — это то место, где лежали три бага этой фазы.

Теперь у клиента свои окна (`quicclient_connect_inproc_windowed`) и кредит,
следующий за чтением: `MAX_STREAM_DATA`/`MAX_DATA` на `прочитано + окно`, когда
объявленного осталось меньше половины. Учёт сознательно грубее серверного —
клиент считает прочитанное **тестом**, а не наибольшее полученное смещение; для
пира, читающего всё, это одно и то же, а «тест, который не читает» — ровно тот
случай, который хочется уметь создавать нарочно. Входящие `DATA_BLOCKED` и
`STREAM_DATA_BLOCKED` записываются: «передача встала» и «передача встала и
сказала почему» — разные результаты, и только второй доказывает, что её
остановило окно.

**Сценарий сразу нашёл дефект, и ровно тот, от которого защищались.** Сервер
дошёл до 16384 байт (объявленное окно потока) и **замолчал совсем**: ни
`STREAM_DATA_BLOCKED`, ни пакета, ни таймера — в трассе единственный оставшийся
дедлайн idle timeout через тридцать секунд.

Причина — исключение для «висящего FIN». Условие звучало как «мы заблокированы,
если окна нет **и** не осталось одного FIN», а «остался один FIN» проверялось
как «FIN поставлен в очередь». Ответ, записанный целиком — то есть **любой**
ответ, который производит слой h3, — имеет FIN в очереди с первого байта. Значит,
ветка уведомления о блокировке была недостижима именно для того случая, ради
которого написана. Комментарий рядом с ней описывает последствие дословно:
«отправитель заблокирован и молчит, пир не знает, что мешает, и не поднимает
предел; мы перестаём строить пакеты, `want_write` гаснет, в полёте не остаётся
ничего, чтобы взвести таймер» — и это соединение, которое живо, простаивает и
кончено.

Починка — проверять то, что имелось в виду: `sent_off == write_off`, то есть
перед FIN действительно ничего не осталось. Тест показан падающим на прежнем
условии («и сказала, что заблокирован») и проходящим на новом.

**Заодно исправлена ошибка порядка в самом стенде:** цикл вычитывания сначала
шагал, потом читал. На соединении, вставшем у закрытого окна, «шагнуть» означает
прыгнуть к ближайшему таймеру — то есть к idle timeout, — и тест мерил бы смерть
соединения вместо разблокировки. Читать надо сперва, а кредит после чтения
требует немедленного flush: заблокированный сервер не пришлёт датаграмму, к
которой можно было бы прицепить `MAX_DATA`, а ждать её — значит ждать того, что
этот `MAX_DATA` и должен вызвать.

Девятнадцать сценариев, 102029 проверок. **Список §2 закрыт**, кроме 100 МБ
одним потоком (только Release) и `CONNECTION_CLOSE` в остальных состояниях.

### 2i. Матрица помех целиком, за секунды

**Сделано (2026-08-13).** Один обмен под каждой комбинацией того, что делает
путь: чисто, потери 10/25 %, потери 40 % в одну сторону, переупорядочивание,
дублирование, их смеси, узкое место, узкое место с потерями, и то же самое с
окном, до которого можно дотянуться. Четырнадцать ячеек × пять seed'ов в
обычном прогоне; `STAND_MATRIX_SEEDS=250` превращает ту же таблицу в **3500
прогонов за 15 секунд**, `STAND_MATRIX_ONLY="<кусок имени>"` воспроизводит одну
ячейку с трассой. Это та самая матрица, что раньше стоила 27 прогонов по
минутам с пересборкой между ними (§2a) — разница в том, что отказ теперь
называет комбинацию и seed, а это и есть разница между матрицей и лотереей.

**Она нашла четыре вещи, три из них — в первые же минуты.**

1. **Ловушка чтения (харнесс).** «Все байты доехали, поток не закончился». После
   §3t FIN не едет на ретрансмиссии — пир выбросил бы дубликат вместе с ним, —
   поэтому при потере последнего пакета конец потока приезжает **отдельным
   кадром, после последнего байта**. Цикл вычитывания, выходивший по счётчику
   байт, докладывал это как незавершённый поток. Ждать надо и FIN.
2. **Кредит, потерянный в пути, не повторялся (клиент).** `MAX_STREAM_DATA`
   отправлялся один раз, при чтении, и на этом всё: мы считаем, что у пира есть
   окно, пир знает, что нет, и обе стороны ждут. Теперь решение принимается
   **при сборке пакета** — есть что выдать, или `credit_resend` говорит, что
   прошлая выдача могла не дожить, — а `credit_resend` ставят два события:
   входящий `DATA_BLOCKED`/`STREAM_DATA_BLOCKED` (§4.1 ровно для этого) и
   собственный PTO.
3. **`STREAM_DATA_BLOCKED` не переотправлялся при потере (сервер, §13.3).**
   Кадр отправляется один раз на значение предела (`blocked_sent`), и рассуждение,
   оправдывающее это для остальных кадров с пределами — «следующий пакет всё
   равно несёт текущее значение», — здесь **ровно обратное**: заблокированность
   и есть состояние, в котором следующего пакета нет. Кадр теперь
   регистрируется в детекторе потерь, и на потере защёлка снимается. Тест
   показан падающим без правки.
4. **Клиент по PTO переотправлял рукопожатие спустя сотню секунд.** Настоящий
   клиент выбрасывает Initial/Handshake-ключи (RFC 9001 §4.9) и не смог бы; наш
   их держит, поэтому пробник посылал ClientHello — и сервер, чьё соединение к
   тому времени умерло по idle timeout, честно открывал по нему **новое**.
   Вставшая передача выглядела как рукопожатие на 107-й секунде.

**Одна ячейка была неправильной, и это тоже результат.** Потери 40 % в обе
стороны означают круг, доходящий с вероятностью 0,36; соединение с
тридцатисекундным idle timeout при этом вправе умереть, и требовать обратного —
требовать против протокола. Ячейка приведена к семантике старой матрицы:
потери в одну сторону, ту, где восстанавливаться приходится **серверу**.

После всех правок: **3500 прогонов, ноль отказов**; обычный прогон (70 ячеек)
добавляет к `runner` около трёх десятых секунды.

Двадцать сценариев, 102030 проверок, весь `runner` — 2 с.

### 2j. Остаток списка: закрытие во всех состояниях и 100 МБ

**Сделано (2026-08-13), и на этом §2 закрыт полностью.**

**`CONNECTION_CLOSE` в остальных трёх состояниях.** Было одно закрытие — из
`ACTIVE`. Добавлены:

- **из `HANDSHAKE`**, и это регрессия на самую дорогую находку h3spec (§3d.7):
  кадр уходил на одном уровне, выбранном по **нашим** ключам, а сервер владеет
  1-RTT-ключами на целый флайт раньше, чем клиент может их прочитать. Любая
  ошибка времени рукопожатия уезжала в пакет, который пир не откроет: сервер
  закрывался правильно, пир видел тишину до таймаута. Момент в тесте выбран
  ровно тот: клиент отправил ClientHello и больше ничего, `tx[APP]` у него ещё
  невалиден. Утверждается не «закрылось», а «пир прочитал закрытие»;
- **`CLOSING`** (§10.2.1): молчит, пока не спросят, и повторяет пакет в ответ на
  входящий. Проверяются обе половины — вторую забыть легко, а первая и есть то,
  что не даёт превратить закрытие в усилитель;
- **`DRAINING`** (§10.2.2): пир закрылся первым — мы не шлём **ничего**, даже
  подтверждения, и уходим по истечении периода. Для этого клиент научился слать
  свой `CONNECTION_CLOSE`.

Две ловушки самого стенда, обе про время: период закрытия — три PTO, поэтому
«тихое» окно надо брать заведомо короче (иначе меряешь не тишину, а сбор
соединения), а состояние `DRAINING` ловится предикатом, а не фиксированным
окном — оно короче, чем кажется.

**100 МБ одним потоком.** Приложение здесь **дозаписывает** через
`quicconn_write_room`, ровно как слой h3, а не кладёт сто мегабайт в буфер
отправки: за счёт этого проверяется то, до чего размер только и добирается —
уплотнение буфера по мере подтверждений, номера пакетов за пределами коротких
кодировок, окно перегрузки в установившемся режиме и сам бюджет забегания
вперёд (утверждение в конце: `unsent_bytes` не превысил его ни разу).

Замер, Release: **104 857 600 байт байт-в-байт, 2992 мс виртуального времени,
0,8 с настоящего** — то есть около 125 МБ/с чистой обработки (крипто и
кадрирование, без сокетов) и 35 МБ/с «по проводу» при RTT 20 мс. По умолчанию
сценарий гоняет 1 МБ (санитайзерная сборка везёт около 2 МБ/с, §7);
`STAND_BIG_MB=100` — это то, чем измерено выше.

**Ловушка, стоившая первого прогона, и она про стенд, а не про сервер:** я
будил эндпоинт (`quicconn_want_write`) на каждом обороте цикла дозаписи.
Разбуженный эндпоинт — это событие «сейчас», поэтому виртуальные часы
переставали двигаться вовсе, датаграммы, назначенные на +15 мс, не наступали
никогда, и цикл открутил двадцать миллионов оборотов на t=0. Будить надо тогда,
когда действительно что-то произведено.

Двадцать четыре сценария, 102073 проверки.

---


## 3. Уровень 3: совместимость с чужими реализациями

Самое ценное. Наш код может быть самосогласованно неправильным.

**3.1 Ручная проверка (уже с фазы 3):**

```bash
# curl со сборкой ngtcp2/quiche
curl -v --http3-only https://localhost:443/

# quiche
cargo run --bin quiche-client -- https://localhost:443/ --no-verify

# ngtcp2
examples/client localhost 443 https://localhost/

# Chrome
chrome --origin-to-force-quic-on=localhost:443 --ignore-certificate-errors
# и chrome://net-export для лога
```

**3.2 quic-interop-runner** — стандартный отраслевой прогон. Требует обернуть
сервер в docker-образ по интерфейсу раннера (переменные `CERTS`, `ROOT`,
`QLOGDIR`, `TESTCASE`). Это ~50 строк Dockerfile и небольшой скрипт; сделать
стоит, потому что раннер даёт матрицу против ~15 реализаций.

Тест-кейсы, которые должны быть зелёными в v1:

| Кейс | Что проверяет |
|---|---|
| `handshake` | базовое рукопожатие |
| `transfer` | передача нескольких файлов |
| `longrtt` | рукопожатие при RTT 1.5 с |
| `chacha20` | ChaCha20-Poly1305 |
| `multiplexing` | много потоков одновременно |
| `retry` | Retry и валидация токена |
| `resumption` | возобновление сессии TLS |
| `http3` | сам HTTP/3 |
| `blackhole` | восстановление после полного пропадания сети |
| `handshakeloss` / `transferloss` | 30 % потерь |
| `handshakecorruption` / `transfercorruption` | порча пакетов |
| `ipv6` | IPv6 |
| `v2` | (фаза 9) |
| `zerortt` | (фаза 9) |

Не обязаны быть зелёными в v1: `zerortt`, `v2`, `ecn`, `amplificationlimit`
(проверяется отдельно), `keyupdate` (должен быть, но зависит от клиента),
`connectionmigration` (стоит добиться).

**3.3 h3spec** (github.com/kazu-yamamoto/h3spec — не summerwind, у того h2spec;
готовый бинарник в релизах, собирать Haskell не нужно) — аналог h2spec, который уже
использовался в этом проекте (147/147 по h2). Проверяет соответствие RFC 9114
поведением, а не байтами: коды ошибок, реакцию на недопустимые кадры, порядок
служебных потоков. Цель — прохождение без падений, с явным разбором каждого
несоответствия (как это делалось для h2spec).

**3.4 Wireshark** с `SSLKEYLOGFILE`. Наш `SSL_CTX` должен поддерживать
`SSL_CTX_set_keylog_callback` — включается только в Debug и по переменной
окружения, иначе это утечка ключей. Без этого разбор чужого дампа невозможен.

## 4. Уровень 4: qlog и qvis

`04` §10. Практика: при любом непонятном поведении включаем qlog на одно
соединение, открываем `.sqlog` в qvis (qvis.quictools.info), смотрим диаграмму
последовательности и график cwnd. Большинство ошибок восстановления видно
глазами за минуту.

## 5. Фаззинг

Все кодеки принимают данные от сети до всякой аутентификации, поэтому фаззинг
обязателен, а не желателен.

Цели (libFuzzer или AFL++, `-fsanitize=fuzzer,address`):

```
fuzz_quic_packet   — quicpkt_parse_header + quicpkt_next
fuzz_quic_frame    — quicframe_next по случайному payload
fuzz_quic_tp       — quictp_decode
fuzz_h3_frame      — h3frame_parser_feed
fuzz_qpack_decode  — qpack_decode_block (самая ценная цель)
fuzz_qpack_encoder_stream — qpack_decoder_read_encoder
fuzz_huffman       — huffman_decode
```

Критерий: 24 часа на каждой цели без падений и без OOM. Корпус засеивается
векторами из §1 — так фаззер сразу стартует с валидных входов.

Дополнительно — фаззинг **всего пути приёма** с известными ключами (сборка, где
`quiccrypto_open` подменяется на «скопировать»): это даёт покрытие логики
соединения, а не только парсеров.

### 5a. Сделано: семь целей и свой драйвер (2026-08-13)

**Все семь целей из списка выше написаны и запущены**, корпус засеян, падений
нет. `tests/fuzz/`, за флагом `-DBUILD_FUZZERS=yes`.

| цель | прогонов за 60 с | корпус (засев → рост) | рёбер |
|---|---|---|---|
| `fuzz_quic_packet` | 199 тыс. | 4 → 23 | 343 |
| `fuzz_quic_frame` | 194 тыс. | 7 → 75 | 445 |
| `fuzz_quic_tp` | 201 тыс. | 3 → 37 | 352 |
| `fuzz_h3_frame` | 201 тыс. | 5 → 23 | 366 |
| `fuzz_qpack_decode` | 201 тыс. | 3 → 40 | 396 |
| `fuzz_qpack_streams` | 203 тыс. | 3 → 13 | 337 |
| `fuzz_huffman` | 193 тыс. | 3 → 9 | 290 |

**На этой машине нет clang, а libFuzzer — рантайм clang.** Ставить его нельзя без
пароля, а ждать установки значило бы не сделать §5 вовсе. Зато у gcc есть тот же
сигнал, которым питается libFuzzer: `-fsanitize-coverage=trace-pc` зовёт
`__sanitizer_cov_trace_pc()` на каждом ребре. Недостающей оказалась только петля,
и она написана — `tests/fuzz/fuzz_main.c`: битовая карта рёбер по хешу адреса,
корпус в памяти, шесть классических мутаций, сохранение находок на диск.

**Цели при этом имеют сигнатуру libFuzzer** (`LLVMFuzzerTestOneInput`), и это не
украшение: с появлением clang драйвер заменяется на `-fsanitize=fuzzer`, а файлы
целей не меняются ни строкой. CMake выбирает сам по `CMAKE_C_COMPILER_ID`.

**Три ловушки, каждая стоила прогона.**

1. **Флаг покрытия нельзя класть в `CMAKE_C_FLAGS`.** Его наследует каждый
   `try_compile`, а пробная программа зовёт `__sanitizer_cov_trace_pc` и не
   определяет его — линковка падает, и CMake сообщает об этом как
   «`find_package(Threads)`: компилятор не понимает -pthread». Ровно та же
   ловушка описана рядом про `-fanalyzer`. Лечится `add_compile_options`.
2. **Инструментируется весь фреймворк, а не цели.** Рёбра, ради которых всё
   затевалось, лежат в парсерах внутри `libcwfr_framework.so`; инструментовать
   только `fuzz_targets.c` значит фаззить вслепую. Но тогда общая библиотека
   зовёт хук, которого нет в `cwfr` и в каждом модуле-хендлере — поэтому в
   `misc/fuzzcov.c` лежит **слабая** пустышка, а сильное определение драйвера
   вытесняет её в фаззинг-бинарниках (ELF ищет символ сначала в исполняемом
   файле).
3. **Хук нельзя инструментировать.** Флаг глобальный, значит и сам колбэк
   получает инструментовку, и первое же ребро внутри него ведёт обратно в него.
   Отчёт честный и озадачивающий: `stack-overflow ... in
   __sanitizer_cov_trace_pc`. Лечится `__attribute__((no_sanitize_coverage))` —
   на обоих определениях.

**Корпус сохраняется** (`id-<hash>` в каталоге засева), иначе каждый прогон
заново выводит одно и то же из семян, и «24 часа на цель» пришлось бы отсиживать
за один раз. Проверено: второй прогон `qpack_decode` стартовал с 35 семян вместо
3 и нашёл рёбра, которых не было в первом.

**Запуск:**

```bash
cmake .. -DCMAKE_BUILD_TYPE=Debug -DINCLUDE_HTTP3=yes -DBUILD_FUZZERS=yes
cmake --build . -j$(nproc)
ASAN_OPTIONS=detect_stack_use_after_return=0 \
  ./exec/fuzz_qpack_decode -seconds=600 -artifacts=/tmp/crashes \
  ../core/tests/fuzz/corpus/qpack_decode
```

Семена лежат в `tests/fuzz/corpus/<цель>/` — вектор RFC 9204 B.1, Initial из
RFC 9001 A.2, кадры QUIC и HTTP/3, инструкции обоих QPACK-потоков, строки
Хаффмана из RFC 7541 C.4.

**Чего это ещё не даёт, и это стоит держать в уме при чтении таблицы:**
критерий §5 — 24 часа на цель, а сделано по минуте; покрытие считается по хешу
адреса, то есть коллизии делают фаззер слепым к части рёбер; словаря
(протокольных констант) у мутатора нет; и второй абзац §5 — фаззинг всего
приёмного пути с подменёнными ключами — не начат. Первое лечится временем,
остальное — работой, и обе строчки остаются в плане.

## 6. Санитайзеры

Как и в h2:

- ASan+LSan — Debug/RelWithDebInfo по умолчанию;
- **TSan обязателен с фазы 4** (`-DSANITIZE=thread`) — QUIC добавляет
  межпоточные пути (публикация ответа, `want_write`, «чужой» воркер получает
  пакет). Не забыть: TSan-сборка требует TSan-инструментированных `.so`
  хендлеров, то есть путь в `config.json` должен указывать на TSan-сборку;
- напоминание из `core/INSTALL.md`: при профилировании памяти
  `ASAN_OPTIONS=detect_stack_use_after_return=0`, иначе простаивающий сервер
  «течёт» ~4.5 КБ/с.

## 7. Производительность

Измерять **только на Release** — в h2 уже выяснилось, что миллисекундный горб на
локах был в основном артефактом ASan (`docs/concurrency/01` §6 A.4).

Что меряем:

| Метрика | Инструмент | Ориентир |
|---|---|---|
| Запросов/с, короткие ответы | `h2load` с h3-поддержкой | не хуже 60 % от h2 на том же железе |
| Пропускная способность одного потока | `quiche-client` / свой клиент | ≥ 80 % от h2 по TLS |
| CPU на мегабайт | `perf stat` | худший случай — 2× от h2; если хуже, искать в AEAD (пересоздание `EVP_CIPHER_CTX`) и в syscall на датаграмму |
| Время рукопожатия | свой клиент | 1-RTT + время подписи |
| Память на соединение | `/metrics` + RSS | ≤ оценки из `07` §4 |

Профилировать в первую очередь: `sendmsg`/`recvmsg` (сисколл на датаграмму —
главная статья расхода; отсюда `recvmmsg`/`sendmmsg`, а в фазе 9 GSO/GRO),
AEAD, поиск по таблице CID, аллокации на пакет (их не должно быть — все буферы
из пулов эндпоинта).

### 3a. Первая чужая реализация

**h2load с ngtcp2/nghttp3 (2026-08-09)** — первый клиент, не делящий с сервером
ни строчки кода. Нашёл за час больше, чем весь собственный клиент за фазу, и
причина проста: `quicclient` делает один запрос на соединение, а h2load —
двести.

**1. Ответ ждал таймера подтверждений.** 25 мс на запрос, ровно
`http3_ack_delay_ms`; одно соединение упиралось в 40 запросов в секунду.
Трасса сисколлов (`strace -tt` на сервере, запущенном под ним, — цепляться к
чужому процессу не даёт `ptrace_scope=1`) показала прямо: запрос принят в
`36.563854`, ответ отправлен в `36.588888`. Причина — `quicendpoint_wake()`
клал соединение в очередь отправки, но разбудить воркера из `epoll_wait` было
нечем: очередь не дескриптор. Ответ ждал ближайшего таймера, а им была
ack-задержка. Добавлен `eventfd` эндпоинта, регистрируемый как ещё одно
соединение (как и таймер из `01` §6a). **10 соединений: 31 → 562 req/s.**

**2. Соединение исчерпывалось после сотни запросов.** Дальше — цепочка, в
которой каждое звено оказалось «код написан в фазе 4 и не вызывается»:

- `quicsendbuf_ack()` **не вызывалась ниоткуда** — подтверждённые данные никогда
  не освобождались, `fin_acked` не выставлялся, поток не мог достичь терминального
  состояния, буфер отправки хранил всё до конца соединения. Починено: acked-кадры
  теперь возвращаются из `quicloss_on_ack` через новый `out_acked`, симметрично
  потерянным;
- завершённые потоки **никогда не освобождались** (только при закрытии
  соединения) — отсюда и невозвращённый кредит, и обход растущего списка на
  каждый пакет. Добавлен `__streams_reap()` с хуком `app_done` (транспорт видит,
  что обе стороны закрыты, но не знает, дописал ли h3 ответ);
- `MAX_STREAMS` не отправлялся никогда;
- `quicflow_should_update()` / `quicflow_consumed()` — тоже без вызывающих, то
  есть `MAX_DATA` и `MAX_STREAM_DATA` не отправлялись никогда.

Побочно: освобождение потоков вскрыло, что кадр для уже закрытого потока
трактовался как `INTERNAL_ERROR`; §3.2 велит такие игнорировать (раньше это было
недостижимо — поток жил до конца соединения).

**Состояние на конец захода: сотня запросов на соединение всё ещё упирается в
ошибку 0x4.** Потоки освобождаются (счётчик `streams_released` растёт), кредит
считается, кадры выдаются. Регрессия при этом зелёная: 17 прогонов клиента,
юнит-тесты, сборка без h3.

### 3b. Второй заход: код 0x4 — это не flow control

**2026-08-09, тем же h2load.** Заход выше записал ошибку как
`FLOW_CONTROL_ERROR`, и следующим шагом назначил «различить три проверки flow
control счётчиками». Обе половины оказались неверны: **0x4 — это
`STREAM_LIMIT_ERROR`**, `FLOW_CONTROL_ERROR` имеет код 0x3. Flow control в этой
истории ни при чём, и день счётчиков был бы потрачен впустую. Спросить сам код
стоило одной команды: `h2load --qlog-file-base=` кладёт рядом sqlog, в котором
принятый `connection_close` лежит открытым текстом. **Кто закрыл соединение и
с каким кодом — первый вопрос к бинарному протоколу, а не последний.**

Найдено три бага, каждый следующий вскрывался за предыдущим.

**1. Порча стека в `__build_packet` (крэш всего сервера).** До ошибки 0x4 дело
даже не доходило: `*** stack smashing detected ***`, `abort()` в воркере,
процесс целиком. Это и есть «часть ресурсов в pending» в браузере — сервер
умирал посреди загрузки страницы, а не отвечал медленно.

Причина — знаковое вычитание в беззнаковой арифметике:

```c
if (cap < QUIC_AEAD_TAG_LEN + 32) return 0;          /* cap >= 48 */
const size_t payload_cap = min(cap - QUIC_AEAD_TAG_LEN, sizeof payload) - 64;
```

При `cap` от 48 до 79 включительно `payload_cap` уходит в отрицательное,
то есть в ~2^64, и писатели кадров получают такую «ёмкость» поверх стекового
буфера в 1200 байт. Такой `cap` приходит не из фантазии: это **хвост
anti-amplification бюджета** — `room = amplification_budget - total`, и когда
от 3× принятого остаётся полсотни байт, пакет строится ровно в эту дыру.
Починка — сравнивать `cap` с суммой всех трёх слагаемых, которые из него потом
вычитаются (`QUICCONN_HEADER_RESERVE`, тег, минимальный полезный payload);
резерв заголовка заодно перестал быть безымянной `64`.

**2. Лимит потоков проверялся по транспортному параметру, а не по выданному
кредиту.** Собственно 0x4. `MAX_STREAMS` отправлялся правильно
(`initial_max_streams_bidi + peer_bidi_closed`), а проверка на приёме
сравнивала индекс с `local_params.initial_max_streams_bidi` — то есть со
значением, которое перестало быть актуальным после первого же освобождённого
потока. Сервер выдавал кредит и убивал соединение за его использование: клиент
открывал 101-й поток **потому что мы ему разрешили**. Ровно сто запросов на
соединение — и разрыв; браузеру этого хватало на одну страницу с картинками.
Починено `__streams_limit()`: лимит — максимум из параметра и последнего
*отправленного* `MAX_STREAMS` (именно отправленного: потерянный по дороге кадр
клиент не использует, а строгость здесь отвергала бы законно открытый поток).

**3. `__stream_open_peer` открывал вчетверо больше потоков, чем просили.**
Всплыло уже после починки, из метрик: 20000 запросов дали
`streams_opened: 48556`. Счётчик `next_peer_bidi` — это **индекс**, а цикл
сравнивал его с **id**, который вчетверо больше:

```c
while (*next <= id)                    /* было: индекс против id */
while (*next <= quic_stream_index(id)) /* стало */
```

Лишние потоки — клиентские, и завершить их некому: они навсегда оставались в
списке соединения, который обходится на каждый отправляемый пакет. Починка
дала **+88 % req/s** (4959 → 9309) сама по себе, и `streams_opened` стал ровно
`запросы + 7×соединений`, как и должен.

**Проверено после всех трёх:**

| Прогон | Результат |
|---|---|
| `-n 500 -c 1` (то, что раньше умирало на 101-м) | 500/500 |
| `-n 20000 -c 50 -m 30`, 177 реальных файлов сайта | 20000/20000, 9438 req/s |
| Все 177 ассетов страницы в одном соединении | 176/176 (177-й — каталог, 404 честный) |
| `-n 300 -c 3 -m 10 -d` тело 2.6 МБ (приёмный flow control, ~800 МБ вверх) | 300/300 |
| `-n 400 -c 400` (шторм рукопожатий) | 400/400 |
| 13 сценариев `quicclient` (потери, дубликаты, реордер, миграция, key update, Retry) | все OK |
| Юнит-тесты | 101637/101637 |

`closed.local_error: 0`, `http3.connection_errors: 0`, `stream_errors: 0` на
всех прогонах.

**Сравнение с h2 — наконец сделано.** Тот же список из 177 файлов, те же
`-n 20000 -c 50 -m 30`, системный `h2load` по TCP против h3:

| | req/s | Доля от h2 |
|---|---|---|
| h2 по TLS/TCP | 12484 | — |
| h3 | 9438 | **76 %** |

Ориентир §7 — «не хуже 60 % от h2» — **выполнен**.

**Побочно подтверждено: `http3_so_rcvbuf` обязателен под нагрузкой.** Шторм из
400 рукопожатий на системном умолчании (~212 КБ) давал 387/400 за 33 с и
`drop.kernel_overflow: 13386`. С `http3_so_rcvbuf: 4194304` — 400/400 за 3.3 с
и `drop.kernel_overflow: 0`. Ключ описан в `07` §3, но в живом `config.json` его
не было; в документацию сайта стоит вынести его как рекомендацию по умолчанию,
а не как тонкую настройку.

### 3c. Третий заход: браузер как отдельная реализация

**2026-08-09, headless Chrome + netlog.** После 3b h2load показывал 20000/20000,
а в браузере часть ресурсов оставалась в `pending`. Это ровно та ситуация, ради
которой в §3 написано «чужая реализация»: **h2load — не браузер**, и всё, что
делает только браузер (миграция портов, PRIORITY_UPDATE, свой темп запросов),
не покрыто ничем.

Инструмент оказался лучше ожидаемого. `google-chrome --headless=new
--log-net-log=… --net-log-capture-mode=IncludeSensitive
--origin-to-force-quic-on=host:443 --host-resolver-rules="MAP host 127.0.0.1"
--virtual-time-budget=20000 --dump-dom` даёт покадровый лог QUIC со стороны
клиента, где каждое событие названо словами. Две поправки, без которых заход
теряется: `--no-proxy-server --disable-background-networking` (иначе 178 МБ лога
из резолвера и PAC, и до QUIC дело не доходит) и фильтрация по `QUIC_SESSION`
с нужным `host` — фоновые сессии к Google в логе есть всегда.

Диагноз лежал в одной строке параметров события:

```
QUIC_SESSION_DROPPED_UNDECRYPTABLE_PACKET  {"encryption_level": "ENCRYPTION_INITIAL"}   x396
QUIC_SESSION_PACKET_AUTHENTICATED                                                       x264
```

**Две трети того, что сервер слал в соединение, — Initial-пакеты, уже после
рукопожатия.** Причина — `quicloss_discard_space()` **не вызывалась ниоткуда**
(тот же список, что в 3a п.2), и ключи уровней не сбрасывались вовсе. Отсюда
цепочка: пространство Initial живёт вечно → его PTO срабатывает вечно (пир
никогда не подтвердит то, ключи от чего выбросил) → каждый пробник уходит
Initial-пакетом, добитым до 1200 байт по §14.1 → реальной датаграмме в этом
раунде отправки места уже нет. Соединение при этом живо и без единой ошибки:
страница просто не дозагружается.

Chrome реагировал так, как и должен на деградирующий путь: 8 попыток
**миграции порта**, 11 PATH_CHALLENGE. То есть браузер видел не «сервер
медленный», а «маршрут испортился» — и симптом уходил ещё дальше от причины.

Починка — `__discard_space()` в `quicconn.c` и два вызова по RFC 9001 §4.9:
Initial сбрасывается на **первом открывшемся Handshake-пакете** (§4.9.1 — это
момент, когда клиент сам перестаёт читать Initial), Handshake — по завершении
рукопожатия (§4.9.2; для сервера завершение и есть подтверждение, Finished
клиента не мог бы появиться иначе). Вместе с ключами уходит четыре вещи, и
каждая нужна: пространство потерь (иначе байты навсегда в congestion window, а
PTO тикает), отложенный ACK (послать его больше нечем), буфер CRYPTO (там лежит
цепочка сертификатов — самая большая аллокация рукопожатия) и счётчик
пробников на этом уровне (иначе он никогда не обнулится).

| | Было | Стало |
|---|---|---|
| Пакетов принято за загрузку страницы | 795 | 213 |
| Из них `DROPPED_UNDECRYPTABLE` (ENCRYPTION_INITIAL) | 537 | 1 (до рукопожатия) |
| `QUIC_PORT_MIGRATION_TRIGGERED` | 8 | 0 |
| `PATH_CHALLENGE` от клиента | 11 | 0 |
| Запросов страницы завершено | часть в pending | 17/17 |
| `-n 20000 -c 50 -m 30` | 9309 req/s | **10258 req/s** |

Проверено, что сброс ключей не сломал устойчивость: 14 сценариев `quicclient`
(включая `--loss-in 20 --reorder 20`, где Initial приходит повторно уже после
сброса — такой пакет молча отбрасывается по `!keys->valid`), 20000/20000 под
нагрузкой, 400/400 рукопожатий, 101637 юнит-проверок.

**Урок метода:** «свой клиент → чужой клиент → браузер» — три разные ступени, и
каждая ловит свой класс. h2load не мигрирует порты и не жалуется на деградацию
пути; браузер это делает и пишет об этом в лог, который читается словами.

### 3d. h3spec: 49/49

**2026-08-09.** Третья чужая реализация и первая, которая проверяет не «работает
ли», а «соответствует ли». Нашла десять настоящих дефектов за один заход.

Инструмент берётся готовым бинарником — собирать Haskell не нужно:
`kazu-yamamoto/h3spec`, релиз `h3spec-linux-x86_64` (репозиторий в §3.3 указан
неверно: у summerwind — h2spec). Запуск: `./h3spec -n <host> <port>`, где `-n`
отключает проверку сертификата; `-m "<кусок имени>"` гоняет один кейс, `-d`
печатает, что клиент видит на проводе.

**Первый прогон: 49 кейсов, 48 падений — потому что сервер не был запущен.**
Стоит записать: h3spec на мёртвый порт даёт ровно ту же картину, что на
несоответствующий сервер. С поднятым сервером стало 31.

**Одна строка кода объяснила 31 падение из 34 транспортных.** Лог сервера назвал
её сразу: `h3: could not open the service streams`. h3spec объявляет
`initial_max_streams_uni = 3` — ровно минимум RFC 9114 §6.2, — а
`h3conn_open_service_streams` открывает **четыре** потока: control, оба QPACK и
grease-поток RFC 9287. Отказ на четвёртом считался фатальным, соединение
умирало сразу после рукопожатия с `H3_INTERNAL_ERROR`, и ни один транспортный
кейс не успевал выполниться. Grease стал best-effort. **Мораль: необязательное
не имеет права быть фатальным**, а щедрость Chrome и h2load (оба дают больше
трёх) скрывала это от всех прежних проверок.

Дальше — по группам, каждая своя причина:

**Транспортные параметры (8 кейсов).** Валидация в `quictp.c` была написана
целиком и правильно — role-проверки, диапазоны, всё. Не работала **проводка**:
`__cb_got_transport_params` возвращал OpenSSL 0, тот ронял рукопожатие, и до
пира доезжал голый `close_notify`. Добавлено поле `quictls_t::transport_error`,
которым крипто-слой сообщает транспорту, каким кодом закрываться; `__on_alert`
отдаёт ему приоритет, иначе алерт успевал закрыть соединение первым.

**§7.3 не был реализован вовсе.** Комментарий в `quictp.h` честно говорил, что
проверка `initial_source_connection_id` «принадлежит слою соединения» — и там её
не было. Это единственная защита от склейки рукопожатия между двумя
соединениями. Теперь параметр обязателен и сверяется с `peer_initial_scid`,
запомненным на accept (не с `peer_cids[0]`: список пир вправе переписать).

**Зарезервированные биты заголовка (§17.2) не проверялись.** Две маски: `0x0c`
в long-заголовке, `0x18` в short.

**STOP_SENDING и MAX_STREAM_DATA для несуществующего потока игнорировались.**
Новый `__stream_for_send_frame()` различает четыре случая там, где раньше был
один `__stream_find`: поток, на который мы вообще не можем слать →
`STREAM_STATE_ERROR`; наш собственный, ещё не открытый → он же; чужой ещё не
виденный → открывается под тем же лимитом, что и STREAM-кадр; наш освобождённый
→ кадр опоздал, игнорируется.

**`NEW_TOKEN` серверу отвечал `FRAME_ENCODING_ERROR`** — кадр разобрался
прекрасно, ошибка была в роли, а не в кодировании (§19.7).

**Самое дорогое: `CONNECTION_CLOSE` уходил на одном уровне, выбранном по нашим
ключам.** Два бага в одном месте:

- Handshake-уровень не использовался никогда — только APP, иначе Initial;
- APP выбирался по `tx[APP].valid`, то есть по **нашим** ключам. Сервер владеет
  1-RTT-ключами с момента отправки своего Finished, а клиент прочитает их только
  обработав весь флайт. Любая ошибка, найденная во время рукопожатия, уезжала в
  пакет, который пир не мог открыть.

Наблюдаемо это выглядело так: сервер честно закрывался с `PROTOCOL_VIOLATION`
(в логе — `frame 0x1a not allowed at level 2`), а h3spec писал
`cannot decrypt: InitialLevel size = 46` и считал, что ответа не было. Для
живого клиента это худший режим отказа, какой есть у протокола: рукопожатие
висит до таймаута, причина не названа. Теперь §10.2.3 соблюдается — до
подтверждения рукопожатия кадр идёт на всех уровнях, которыми владеем,
склеенными в одну датаграмму.

**ClientHello без `quic_transport_parameters` проходил рукопожатие** с нулями во
всех лимитах (§8.2): OpenSSL расширение обязательным не считает и просто не
зовёт колбэк. Проверка стоит там, где появились Handshake-ключи, — то есть
сразу после чтения ClientHello: это и есть момент, когда факт установлен, и
отказ здесь экономит подпись сертификата на соединении, которое мы всё равно
отвергнем.

**TLS `KeyUpdate` в 1-RTT принимался молча** (RFC 9001 §6 — `CRYPTO_ERROR` с
`unexpected_message`). Читается по первому байту сообщения: OpenSSL отдаёт
post-handshake сообщения без комментариев.

**QPACK decoder-поток «дренировался, не разбирался».** Осознанное решение фазы 6,
и оно было неверным: `Insert Count Increment` — ошибка соединения при нуле
(§4.4.3) и при любом другом значении против кодировщика, который ничего не
вставляет. Появился `qpack_encoder_read_decoder()`, зеркальный
`qpack_decoder_read_encoder()`. Юнит-тест, закреплявший прежнее поведение,
переписан — он фиксировал решение, а не требование.

**Итог: 49/49.** Регрессия после всех правок: 20000/20000 под нагрузкой,
400/400 рукопожатий, 200/200 POST с телом, 14 сценариев `quicclient`, страница в
Chrome 17/17, юнит-тесты зелёные.

**Метод, который сработал каждый раз:** лог сервера рядом с выводом h3spec.
Восемь из десяти дефектов назвал сам сервер (`could not open the service
streams`, `frame 0x1a not allowed at level 2`, `closing, transport error 0xa`) —
h3spec говорил лишь, что ответа нет. Там, где лога не хватало, помогали
одноразовые строки с уровнями ключей: `keys initial=1 handshake=0 app=0` рядом с
размером close-пакета мгновенно показало, почему пир его не читает.

**Перепроверено после хирургии на пути отправки (2026-08-13).** §7e–§7g
переделали всё, что доходит до провода: пакетирование `sendmmsg`, сегментацию
GSO, коалесцирование пробуждения, порог бюджета записи. Это ровно тот класс
изменений, где соответствие RFC ломается тихо, — поэтому внешняя проверка
делалась **до** любых новых работ. **49/49 трижды в Release и один раз под
ASan** (ноль сообщений, ноль утечек), 3–4 секунды на прогон.

Тридцать семь строк `error` в логе за прогон пугать не должны: все до одной —
`quic: closing, transport error 0x8/0xa/0x5/0x7` и `application error
0x105/0x109/0x200…`, то есть те самые нарушения, которые h3spec провоцирует
нарочно, и отказ на них — правильный ответ. Ни одной строки про `sendmmsg` или
offload; **отсутствие таких строк и есть проверяемое утверждение** этого
прогона.

### 3e. quic-interop-runner: четыре чужие реализации, кейс `http3`

**2026-08-09.** Обвязка живёт в `backend/interop/` (`Dockerfile` +
`run_endpoint.sh`), а не в `core/`, и это не вкусовое: **core не самостоятельный
CMake-проект** — `find_package(OpenSSL)` делает `backend/CMakeLists.txt`, который
затем добавляет core подкаталогом. Контекст сборки обязан содержать оба.

Результат: **cwfr как сервер против quic-go, ngtcp2, picoquic и quiche — 3/3
файла побайтово у каждого**, клиент выходит с кодом 0, сервер дренирует по
SIGTERM. Стенд настоящий: ns3 между эндпоинтами, `simple-p2p --delay=15ms
--bandwidth=10Mbps --queue=25`.

Четыре препятствия по дороге, каждое стоит записать.

**1. `daemon()` вызывался безусловно** — и контейнер умирал на старте. Release-
сборка форкается и выходит, а для любого супервизора (Docker, Kubernetes,
`systemd Type=simple`) это процесс, который умер немедленно; раннер потом ждёт
свой таймаут против сервера, который на самом деле работает. Добавлен флаг
**`-f`** (`appconfig_foreground()`), и это не про interop: без него cwfr нельзя
запустить ни в одном оркестраторе.

**2. Базовый образ раннера не годится в рантайм.** `martenseemann/
quic-network-simulator-endpoint` — Ubuntu 24.04 с OpenSSL 3.0, а h3 нужен 3.5.
Обе стадии на 24.04→26.04 не переехать иначе как своей сборкой, поэтому образ
целиком на 26.04, а из базового забирается **только `/setup.sh`** — маршруты в
симулятор и отключение checksum offload, без которых ns3 отбрасывает всё.

**3. PCRE1 больше нет в дистрибутиве.** `framework_shared` линкует
`-l:libpcre.so.3`; PCRE1 EOL с 2020 года и в Ubuntu 26.04 не пакетируется вовсе
(на машине разработчика он оказался из стороннего PPA времён 20.04). В образе
`.deb` тянется из пула 24.04, `-dev` — распаковкой, а не установкой, чтобы не
тащить 16- и 32-битные варианты. **Это заплатка, а не решение: настоящее —
переход на PCRE2**, и он нужен не ради interop, а ради того, чтобы проект
собирался на текущих дистрибутивах.

**4. Разбор pcap раннером недоступен.** `run.py` доходит до проверки трассы и
падает: `tshark` не установлен (нужен root), а `pyshark` несовместим с Python
3.14 (`asyncio.get_event_loop` больше не создаёт цикл неявно). Поэтому прогон
делается напрямую через `docker compose` с теми же переменными, что подставляет
`interop.py`, а проверка — сравнение `/downloads` с `/www`. Это ровно то, что
проверяет сам кейс `http3` до анализа трассы; чего не хватает — подсчёта
рукопожатий и версии по pcap.

### 3f. HTTP/0.9 под `hq-interop` и первая транспортная матрица

**2026-08-10.** Почти все кейсы раннера возят файлы по **HTTP/0.9** с ALPN
`hq-interop`, а не по h3 — с одним лишь `h3` доступен единственный кейс.
Написан шим: `protocols/hq/` за флагом сборки **`INCLUDE_HQ_INTEROP`**
(по умолчанию `no`). Флаг сборки, а не ключ конфигурации, сознательно: у
HTTP/0.9 нет ни заголовков, ни хоста, ни длины, ни статуса, и он не должен быть
в одной неосторожной настройке от продакшн-бинарника. Обычная сборка не
содержит даже символа `hq_turn`.

Устройство: соединения-объекта нет вовсе, всё состояние запроса живёт на своём
потоке в `quicstream_t::app` — так же, как у h3, поэтому существующий reaping
транспорта работает без изменений. ALPN выбирает `h3`, когда пир предлагает оба:
пир, умеющий и то и другое, — не раннер.

**Результат против quic-go и ngtcp2** (сервером всегда cwfr):

| Кейс | quic-go | ngtcp2 |
|---|---|---|
| `handshake` | ✔ 3/3 | ✔ 3/3 |
| `transfer` | ✔ 3/3 | ✔ 3/3 |
| `retry` | ✔ 3/3 | ✔ 3/3 |
| `resumption` | ✔ 3/3 | ✔ 3/3 |
| `chacha20` | ✔ 3/3 | — |
| `http3` | ✔ 3/3 | ✔ 3/3 |
| `multiconnect` (30 % потерь) | **✘ 1–2 из 3** | — |
| `multiplexing`, `longrtt`, `*loss`, `*corruption`, `blackhole`, `rebind-*`, `amplificationlimit` | клиент отвечает `127` | то же |

Кейсы, помеченные `127`, **отклоняет клиент**, а не сервер: образы `:latest`
обоих клиентов этих имён не знают. Чтобы их получить, нужен другой клиент —
picoquic и quiche стоит попробовать следующими.

Что серверу пришлось знать о кейсе: `retry` требует, чтобы Retry действительно
отправлялся, а `handshake` — чтобы не отправлялся (политика `auto` не шлёт его
до тысячи рукопожатий в полёте, и это правильный продакшн-ответ и бесполезный
тестовый); `chacha20` требует, чтобы ChaCha20 был **единственным** шифром. Всё
три вынесены в `run_endpoint.sh`.

### 3g. Стоп `multiconnect`: окно отправки тратилось трафиком, а не смещением

`multiconnect` (три соединения по файлу, 30 % потерь) давал 2/3, 1/3, 2/3 —
систематику, не флуктуацию. **Все три рукопожатия завершались**, то есть
устойчивость рукопожатия к потерям была в порядке; вставала передача сразу
после него, клиент выходил по `timeout: no recent network activity`, а сервер
не закрывал ничего.

**Шаг 1: убедиться, что дело не в шиме.** Лог `hq` показал, что все 512000 байт
переданы транспорту на всех трёх соединениях, а первые два закрылись штатно.
Значит вопрос к транспорту, а не к HTTP/0.9.

**Шаг 2: воспроизвести локально.** Своим клиентом сразу не вышло — и это само по
себе улика: `quicclient` шлёт PING в каждом цикле ожидания (§2), чем кормит наше
восстановление подтверждениями; quic-go не шлёт и просто ждёт. Плюс две ловушки
стенда: ответ **gzip-уется** (155 КБ JS превращались в 13 КБ, и до лимитов дело
не доходило), а `quicclient` объявляет окна в 64 МБ. С несжимаемым файлом на
512000 байт и лимитами quic-go (`initial_max_data=786432`,
`initial_max_stream_data=524288`) отказ воспроизвёлся с первого раза:
при 0 % проходит, при 30 % — нет.

**Шаг 3: посекундный снимок соединения** (временный `log_error` в
`quicconn_tick`) назвал стоп одной строкой:

```
unsent=140279 cwnd=3773 inflight=0 pto=0 next_pn=429 largest_acked=428 want_write=0
```

140 КБ ждут отправки, **в полёте ноль, подтверждено всё, окно перегрузки
открыто, таймеров нет — и `want_write=0`**. Соединение живо, простаивает и
никогда не проснётся: слать некому, будить некому.

**Причина.** Второй снимок, по потоку:

```
send_flow used=524288 limit=524288 avail=0 | sendbuf sent_off=372293 write_off=512554
```

Доставлено 372 КБ уникальных данных, а окно списано на все 524288: **flow control
на стороне отправителя считал сумму отправленных байт, а не наибольшее
отправленное смещение**. Каждая ретрансмиссия списывалась повторно, и на теряющем
пути окно съедалось тем быстрее, чем хуже путь. §4.1 ставит предел на смещение
именно поэтому. Приёмная сторона всегда считала правильно
(`quicflow_record_received` с комментарием ровно об этом) — асимметрия и была
багом. Появилась `quicflow_consume_to(flow, end)`, симметричная приёмной;
соединение списывает ту же дельту, поскольку его окно есть сумма наибольших
смещений потоков.

**Вторая половина, без которой стоп остался бы вечным.** Заблокированный
отправитель обязан сказать об этом (§4.1: `DATA_BLOCKED` /
`STREAM_DATA_BLOCKED`), иначе пир не видит причины поднимать лимит, о котором не
знает. `quicflow_should_send_blocked()` был написан и **не вызывался ниоткуда** —
тот же список, что в 3a и 3b. Теперь кадры отправляются, по одному на значение
лимита. Это и есть штатный выход из тупика: даже если окно кончится законно,
соединение попросит добавки вместо того, чтобы замолчать.

| | до | после |
|---|---|---|
| `multiconnect`, 3 прогона | 2/3, 1/3, 2/3 | **3/3, 3/3, 3/3** |
| локально, 512 КБ несжимаемых при `--loss-in` | 30 % — отказ | 0/10/30/35/40 % — все OK |

Регрессия: h3spec 49/49, 20000/20000 под нагрузкой, 11 сценариев `quicclient`,
матрица `handshake`/`transfer`/`retry`/`resumption`/`chacha20`/`http3` против
quic-go и ngtcp2 — всё зелёное. Свойство закреплено юнит-тестом в
`test_quic_stream.c`: повторная отправка того же диапазона стоит нуля.

**Урок метода, третий раз за фазу:** когда «не работает», спрашивать надо не
«что сломалось», а «что сейчас в состоянии соединения». Пять чисел в одной
строке — `unsent`, `inflight`, `largest_acked`, `cwnd`, `want_write` — назвали
и симптом, и место. Гипотеза, выдвинутая до этого снимка, была верной по сути и
проверялась на **успешном** прогоне, где ретрансмиссий не было вовсе, — и
поэтому выглядела опровергнутой.

### 3h. Матрица: имя кейса ≠ имя, которое видит клиент

**2026-08-10.** В §3f записано, что `multiplexing`, `longrtt`, `*loss`,
`*corruption`, `blackhole`, `rebind-*` и `amplificationlimit` «отклоняет
клиент»: образы отвечали `127`. Это было неверно, и виноват был наш прогонщик.

Раннер передаёт эндпоинтам **не имя кейса**, а `test.testname(Perspective)` —
`interop.py` вызывает его отдельно для клиента и для сервера. Для всех кейсов из
списка выше клиентское имя — `transfer` или `multiconnect`, то есть имена,
которые знает каждый образ; кейс отличается от базового **сценарием ns3**
(`blackhole`, `drop-rate`, `corrupt-rate`, `rebind`, `droplist`) и набором
файлов, а не поведением клиента. Наш `matrix.sh` подставлял в обе переменные имя
кейса, клиент видел незнакомое `blackhole` и честно отвечал «не поддерживаю».

Урок общего свойства: **`127` от чужого образа — это утверждение о нашей
команде, а не о его возможностях.** Прежде чем записывать чужую реализацию в
ограничение, стоит посмотреть, что именно ей передали.

Прогонщик переписан на `driver.py`, который импортирует классы кейсов раннера и
берёт из них `testname`, `scenario`, `timeout` и генератор файлов — вместо того
чтобы повторять их своими словами. Проверка осталась прежней (побайтовое
сравнение `/downloads` с `/www` плюс код выхода клиента): в этом чекауте
`docker-compose.yml` не монтирует `/logs` никуда, поэтому ни pcap, ни keylog
наружу не выходят, и проверки раннера по трассе — число рукопожатий, лимит
потоков, факт смены пути, сама 3×-граница `amplificationlimit` — здесь не
работают ни у нас, ни у самого раннера.

### 3i. Два стопа на больших передачах: окно перегрузки и запертая ретрансмиссия

Как только матрица пошла по-настоящему, отвалился **базовый `transfer`** —
у всех четырёх клиентов сразу. Разница с §3f не в клиентах: там стенд возил
файлы по 5 КБ, 10 КБ и 500 КБ, а `TestCaseTransfer` генерирует три файла на
2, 3 и 5 МБ.

Сервер назвал симптом сам, своим же логом (`level: debug`, шим `hq`):

```
hq: stream 0: no write room at 262144 bytes     <- и так до конца прогона
hq: stream 8: no write room at 4984 bytes
hq: stream 8: no write room at 267128 bytes
...
hq: stream 8: no write room at 786432 bytes     <- дальше не растёт никогда
```

262144 — это `QUICCONN_WRITE_AHEAD_MAX`, общий на соединение; 786432 — это
`initial_max_data` quic-go. То есть транспорт принял в буферы отправки почти
мегабайт, объявил окно израсходованным — а клиент получил из этого **65 КБ** и
вышел по `timeout: no recent network activity`. Ровно тот же силуэт, что в §3g,
но причина другая, и их оказалось две.

**Первая: окно перегрузки ничего не ограничивало.** В `quicconn_send` проверка
стояла *после* `quicendpoint_send` и только для того, чтобы прервать цикл
раундов, выставив `more_pending = 1`, — то есть `want_write` оставался поднят,
эндпоинт немедленно возвращался и отправлял следующий пакет. Окно было не
ограничителем, а пожеланием: отправка шла на скорости линии, пока не кончится
flow control. `quicpacer_allowance`/`quicpacer_consume` не вызывались вообще —
пейсер существовал мёртвым кодом. На пути раннера (10 Мбит/с, 15 мс, очередь в
25 пакетов ≈ BDP) это выливает 786 КБ в очередь, вмещающую двадцатую часть
этого, и большая часть теряется. Оценка сходится с наблюдением: отправлено
786 КБ, доставлено 65 КБ.

Проверка перенесена **до** сборки пакета, а `more_pending` при закрытом окне
снят: окно открывается подтверждением или объявлением потери, и оба эти события
сами поднимают `want_write` (`__route` вызывает `quicconn_send` на каждой
пришедшей датаграмме). Получился нормальный отправитель, тактируемый
подтверждениями. Под окно попадают только два производителя объёма — `CRYPTO` и
`STREAM`. ACK не подпадают под контроль перегрузки вовсе, кадры, возвращающие
кредит пиру, — это противоположность перегрузки, а PTO-проба по §7.5 прямо
разрешена сверх окна: проба существует, чтобы заставить молчащего пира
заговорить, и не отправить её из-за нехватки окна — это способ остаться в
стопоре навсегда.

**Вторая, и это дедлок: ретрансмиссия была заперта flow control'ом.** Цикл
потоков считал `allowed = quicflow_available(...)` и при нуле пропускал поток
целиком — вместе с его потерянными диапазонами. Но §4.5 не списывает
переотправку повторно: смещения оплачены при первой отправке. Пропускать её
нельзя, и это не вопрос эффективности:

> пиру нужны недостающие байты, чтобы отдать приложению всё, что стоит за
> дырой; MAX_DATA он поднимает только на отданное — значит окно, которое
> запрещает ретрансмиссию, может открыть только сама эта ретрансмиссия.

Соединение живо, буферы полны, обе стороны ждут друг друга. Это и есть «сервер
не закрывает ничего», которое видел клиент.

Починка: `quicsendbuf_has_lost()` отвечает, будет ли следующий кусок
переотправкой (`quicsendbuf_next` отдаёт нижний потерянный диапазон целиком
прежде, чем посмотрит на новые данные, поэтому кусок всегда либо одно, либо
другое), и при переотправке окно не режет `room`; `quicstream_wants_send()`
возвращает 1 при непустом списке потерь независимо от окна. Списание после
отправки не меняется — `quicflow_consume_to` на уже оплаченном диапазоне
возвращает ноль, что закреплено тестом ещё в §3g.

**Почему это не поймали раньше.** Все три предыдущих измерения обходили обе
неисправности с разных сторон: 16-мегабайтные прогоны §7a шли по петле, где
терять нечего и окно перегрузки не закрывается; свой `quicclient` объявляет окна
в 64 МБ, так что flow control не запирается; а 512 КБ из §3g просто не успевали
упереться в `initial_max_data` чужого клиента. Нужен был **узкий путь с потерями
и честными окнами пира одновременно** — то есть ровно стенд раннера.

Свойство закреплено юнит-тестом в `test_quic_stream.c`: закрытое окно не мешает
переотправке.

### 3j. `HANDSHAKE_DONE` терялся один раз и навсегда

После §3i осталось одно красное поле: `handshakeloss` (50 рукопожатий подряд при
30 % потерь пачками по три). Клиенты обрывали прогон на первом же неудавшемся
соединении — quic-go на пятом, quiche на первом; picoquic продолжает после
неудач и терял одно из пятидесяти.

**Мерить надо было не то, что падало.** Кейс идёт минутами, и итерировать на нём
нельзя. Синтетический `handshakeloss1` — тот же сценарий ns3, то же клиентское
имя `handshake`, но **одно** соединение, — считается секундами. На нём сразу
стало видно, что это не «медленно», а «иногда никогда»: отказ примерно **1 раз
из 10**, и ровно столько же на образе, собранном до §3i. То есть неисправность
давняя, а не регрессия — важный вывод, потому что базовой линии по этому кейсу
никогда не было (§3h).

**Трасса назвала виновного.** `docker-compose.yml` этого чекаута не монтирует
`/logs`, поэтому pcap оставались внутри контейнера; они достаются override-файлом
compose, а `dumpcap` в симуляторе сбрасывает привилегии — каталогу нужен режим
0777, иначе трассы молча не пишутся. `tshark` на машине нет и не будет (нужен
root), но его и не потребовалось: снаружи датаграммы видны без расшифровки —
время, направление, длина и тип пакета из первого байта заголовка. Хватило
семидесяти строк на Python.

```
 0.384s  S->C    602B  1-RTT      <- рукопожатие завершено на нашей стороне
 1.169s  S->C     23B  1-RTT      <- последний пакет сервера
 2.022s  C->S     74B  Handshake
 3.896s  C->S     74B  Handshake  <- клиент пробует
 7.643s  C->S     74B  Handshake
15.136s  C->S     74B  Handshake
30.114s  C->S     74B  Handshake  <- и так до своего таймаута
```

Клиент **тридцать секунд** шлёт Handshake-пробы с экспоненциальным откатом, а
сервер не отвечает ни разу — и не может: рукопожатие он считает завершённым и
ключи этого пространства уже выбросил (§4.9). Пары пакетов по 74 байта — это
PTO-пробы клиента, который так и не узнал, что рукопожатие подтверждено.

**Причина в одну строку.** `HANDSHAKE_DONE` писался один раз, выставлял
`handshake_done_sent = 1` и **не регистрировался в loss recovery** — в отличие от
`NEW_TOKEN` и `NEW_CONNECTION_ID`, которые стоят в том же сборщике пакетов
пятью строками выше и ref заводят. §13.3 требует переотправлять этот кадр до
подтверждения, и здесь требование не формальное: клиент подтверждает рукопожатие
именно по нему и только после этого выбрасывает свои Handshake-ключи. Потерянный
`HANDSHAKE_DONE` не стоит round trip — он оставляет пира навсегда в состоянии,
из которого сервер уже не может его вывести.

| `handshakeloss1`, 20 прогонов | отказов |
|---|---|
| до §3i (образ от 2026-08-10 00:20) | 2 из 20 |
| после §3i | 2 из 20 |
| после §3j | **0 из 20** |

**Урок метода:** кейс, который падает раз в десять прогонов и идёт пять минут,
надо сначала **уменьшить до одного соединения**, а потом уже смотреть. И второй:
когда «сервер молчит», трасса снаружи отвечает на вопрос «кто замолчал» быстрее
любого лога — даже нерасшифрованная, даже без tshark.

### 3k. Пустая PTO-проба, и чем за неё заплатило окно перегрузки

После §3j `handshakeloss` прошёл у picoquic и ngtcp2 (50/50 оба, вместе с
`handshakecorruption`), а quic-go и quiche стали доходить до 16–46 соединений
вместо 4 и 0. Но не до пятидесяти — и вот тут выяснилось, что §3i за
правильность заплатил.

Трасса неудавшегося соединения:

```
5.388s  C->S   1280B  Initial     <- клиент повторяет ClientHello
5.388s  S->C   1200B  Initial     <- и получает ровно одну датаграмму в ответ
6.447s  C->S   1280B  Initial
6.447s  S->C   1200B  Initial
7.596s  S->C   1218B  Initial
```

Сервер отвечает **одной** датаграммой там, где анти-усиление разрешает три, и
его флайт с сертификатом не уходит. Причина — сложение двух вещей:

- **PTO-проба не несла данных.** «Реальные данные, если они есть» берутся из
  `quicsendbuf_pending()`, а после PTO ничего ещё не объявлено потерянным и всё
  отправлено по разу — значит в пробе оказывался голый `PING`. §6.2.4 требует
  обратного (*SHOULD* новые данные, иначе — уже отправленные), и требование не
  косметическое: PING лишь просит пира сказать хоть что-нибудь, чтобы **потом**
  сработало обнаружение потерь. Это стоит round trip, которого в рукопожатии
  под потерями просто нет.
- **Окно перегрузки стало обязательным (§3i).** Пока ни один ACK не доходит,
  `bytes_in_flight` не убывает — объявить потерю нечем, — окно закрывается и
  больше не открывается. Наружу выходят только PTO-пробы, то есть только
  `PING`. До §3i окно было пожеланием, сервер вслепую перепосылал флайт и
  случайно этим выигрывал.

Починка одна: на срабатывании PTO ставить в очередь **самый ранний
неподтверждённый диапазон** (`quicsendbuf_requeue_unacked`) — сперва в
`crypto_out` того пространства, где сработал таймер, иначе в первом потоке с
неподтверждёнными данными. Проба сразу несёт то, чего пиру не хватает, и она же
единственный пакет, которому §7.5 разрешает превысить окно.

| `handshakeloss`, quic-go, файлов до отказа | прогоны |
|---|---|
| образ до §3i | 17, 18 |
| после §3i и §3j | 2, 3, 5, 16, 17 |
| после §3k | **18, 21, 31** |

**Урок:** правка, делающая поведение более правильным, может открыть давнюю
неисправность, которую прежняя неправильность маскировала. §3i сам по себе был
верен; красным `handshakeloss` стал не от него, а от пустой пробы, которую он
перестал прощать.

### 3l. Повторный ClientHello — это заявление, а не шум

После §3k `handshakeloss` всё ещё не доходил до пятидесяти у quic-go и quiche.
Три измерения по очереди сузили вопрос до одной строки кода.

**Первое: чей это отказ вообще?** Образы раннера умеют обе роли, поэтому кейс
можно прогнать **с чужим сервером**: picoquic сервером и quic-go клиентом дают
50/50 за 52 секунды. Значит кейс на этом стенде проходим и остаток — наш. Это
первое, что стоит делать, прежде чем чинить: чужая реализация в роли сервера
стоит одного прогона и снимает вопрос «а вообще возможно ли».

**Второе: копится ли что-то между соединениями?** `handshakeloss1` (§3j) дал
**1 отказ из 20**, то есть каждое рукопожатие падает независимо с вероятностью
около 5 %, а 0.95⁵⁰ ≈ 8 % — ровно та доля прогонов, что доходила до конца.
Накопления нет, чинить надо одно рукопожатие.

**Третье: что видно в момент отказа.** Постоянная строка `log_debug` на
срабатывании PTO (пять чисел, как в §3g) показала подпись падающих соединений:

```
quic: pto level=0 count=1 in_flight=6815  cwnd=13500 unsent=0 srtt=0
quic: pto level=2 count=2 in_flight=10433 cwnd=13500 unsent=0 srtt=0
quic: pto level=0 count=3 in_flight=11206 cwnd=13500 unsent=0 srtt=0
quic: pto level=2 count=4 in_flight=14824 cwnd=13500 unsent=0 srtt=0
```

**`srtt=0`** — ни одной RTT-выборки за всё время жизни соединения, то есть ни
одно наше подтверждение не вернулось. Без выборки PTO равен удвоенному
`kInitialRtt`, то есть 666 мс, и удваивается: 0.67, 1.3, 2.7, 5.3, 10.7 с. К
четвёртому PTO израсходовано двадцать секунд, и клиент уходит по своему таймауту
рукопожатия. `pto_count` при этом сбрасывается корректно (§6.2.1,
`quicloss.c`) — сбрасывать его нечем, подтверждений нет.

**Причина.** Клиент в это время повторяет ClientHello — в трассе это видно как
`C->S 1280B Initial` раз за разом. Мы отвечали на него **подтверждением**: свой
флайт мы уже отправили, `quicsendbuf_pending()` пуст, и до §3k переотправить его
могло только собственное PTO. Но повторный ClientHello — не шум: это прямое
заявление пира «твоего флайта у меня нет». Ждать после него PTO значит платить
сотни миллисекунд за то, что тебе уже сказали.

Починка: `quictls_crypto_is_duplicate()` отвечает, лежит ли пришедший диапазон
целиком в уже собранном префиксе уровня; если да и рукопожатие ещё идёт —
неподтверждённый CRYPTO ставится в очередь на всех уровнях, для которых есть
ключи (какая половина флайта потерялась, по уровню дубликата не узнать).
Анти-усиление продолжает действовать, поэтому пир, повторяющий Initial, не может
этим выманить больше трёхкратного.

| | одно рукопожатие, 20 прогонов | `handshakeloss`, quic-go |
|---|---|---|
| после §3k | 1 отказ | 18, 21, 31 из 50 |
| после §3l | **0 отказов** | **50/50, 50/50, 50/50** (35–56 с) |

**Урок метода:** три измерения — «а у чужого сервера?», «одно соединение или
пятьдесят?», «что в состоянии в момент отказа?» — стоили меньше часа и заменили
собой перебор гипотез в коде. Две гипотезы, выдвинутые до них (сброс `pto_count`
и невключённый пейсер), оказались неверны: первая уже была реализована, вторая
по своему же коду не трогает первый флайт, потому что RTT-выборки ещё нет.

### 3m. Матрица целиком

**2026-08-11, после §3h–3p.** Шестнадцать кейсов на четырёх клиентах, сервером
всегда cwfr: **62 из 64**.

| Кейс | picoquic | quiche | quic-go | ngtcp2 |
|---|---|---|---|---|
| `handshake`, `transfer`, `http3`, `retry`, `resumption` | ✔ | ✔ | ✔ | ✔ |
| `chacha20` | ✔ | 127¹ | ✔ | ✔ |
| `multiplexing` (1999 файлов) | ✔ | ✔² | ✔ | ✔ |
| `longrtt`, `amplificationlimit`, `blackhole` | ✔ | ✔ | ✔ | ✔ |
| `transferloss`, `transfercorruption` | ✔ | ✔ | ✔ | ✔ |
| `rebind-port`, `rebind-addr` | ✔ | ✔ | ✔ | ✔ |
| `handshakeloss` | ✔ 50/50 | ✔ 50/50 | ✔ 50/50 | ✔ 50/50 |
| `handshakecorruption` | ✔ 50/50 | ✔ 50/50 | ✔ 50/50 | ✔ 50/50 |

Полный прогон идёт около часа. Для итерации есть быстрый набор (`fast.sh`) —
те же кейсы без восьми пятидесятисоединенческих слотов, вместо них
`handshakeloss5` (пять рукопожатий под теми же 30 % потерь), и два клиента
вместо четырёх: quic-go возит по HTTP/0.9 под `hq-interop`, quiche — по h3,
так что оба прикладных пути остаются закрыты. **26 проверок за 4 минуты.**
Перед коммитом всё равно полный.

¹ Клиент quiche не умеет выбирать шифр и говорит об этом сам — `127` из его
собственного `run_endpoint.sh`, а не отказ сервера.
² Было `✘ 988`, клиент выходил с кодом **101** — соглашение Rust о панике.
Закрыто в §3r: паника происходила в самом клиенте (1024 дескриптора на 1999
одновременно открытых файлов) и воспроизводится против quiche-сервера так же,
как против нашего. С поднятым `nofile` — 1999/1999. `GOAWAY`, который видно было
в логе сервера, — следствие остановки контейнера (он шлётся только при
`shutdown_now`), а не причина.
³ Разброс по прогонам: 3, 6, 32, 50 из 50 — то есть иногда проходит целиком.
Одиночное рукопожатие с тем же клиентом на том же сценарии не падает ни разу за
12 прогонов, так что дело не в отдельном рукопожатии. **Закрыто в §3y:** тот же
клиент против собственного сервера quiche падает так же (два отказа из пяти
прогонов, дословно то же сообщение о таймауте), то есть это свойство кейса, а не
наш дефект.

### 3n. Адрес считался подтверждённым слишком поздно

Остаток §3m — quiche — разбирался тем же способом, и первое, чего не хватило,
было **опознание соединения в логе**. Сервер под `multiconnect` перемежает
строки полусотни соединений, и связать «этот PTO» с «этим отказом» нечем.
Добавлены три метки с первыми четырьмя байтами `odcid`: `accepted`,
`handshake complete` и строка PTO. С ними отчёт сводится одним скриптом:

```
afa3f102  accepted=1 handshake=1 request=1 response=1 pto=2
d0c0399b  accepted=1 handshake=1 request=1 response=1 pto=10
456822a0  accepted=1 handshake=1 request=1 response=1 pto=4
856bd2f0  accepted=1 handshake=0 request=0 response=0 pto=10   <-- вот оно
```

Дальше подпись отказавшего соединения:

```
pto cid=c7bd7189 level=2 count=1 in_flight=326 cwnd=6940 amp=110 validated=0
pto cid=c7bd7189 level=2 count=2 in_flight=326 cwnd=6940 amp=110 validated=0
pto cid=c7bd7189 level=2 count=3 in_flight=326 cwnd=6940 amp=110 validated=0
```

`in_flight` **не меняется** от пробы к пробе при свободном окне перегрузки —
значит пробы не уходят вообще. Не уходят они из-за `amp=110`: бюджет
анти-усиления исчерпан, а `validated=0` говорит, почему он ещё действует.

**Причина.** `address_validated` выставлялся **только при завершении
рукопожатия**. §8.1 разрешает считать адрес подтверждённым гораздо раньше —
как только успешно расшифрован Handshake-пакет от пира: его ключи выводятся
только из нашего ServerHello, то есть пир доказуемо его получил. Пока мы этого
не делали, весь ход рукопожатия проходил под трёхкратным лимитом, и если
Finished клиента терялся, сервер оставался должен флайт, уже потратив бюджет:
PTO срабатывает, а отправлять нечем. §6.2.2.1 отвечает на это «тогда слать
должен клиент», но клиент, считающий рукопожатие завершённым, слать не станет.

Проверка выставляется теперь там же, где отбрасывается пространство Initial
(§4.9.1) — в этом месте уже стоит рассуждение ровно про этот момент.
Регрессия проверена отдельно, потому что правка трогает именно то, что мерит
кейс: `amplificationlimit` и `retry` проходят у picoquic, ngtcp2 и quic-go,
h3spec 49/49.

**Измерено, а не предположено.** Правка снимает лимит раньше, то есть выпускает
флайт большей пачкой — а пачка на этом пути и есть то, чем теряют (§3k). Проверка
после неё: quic-go `handshakeloss` 50/50 в трёх прогонах из четырёх (один — 13/50)
и заметно быстрее прежнего, 29–42 с против 35–56; picoquic и ngtcp2 50/50 оба
кейса; `amplificationlimit` и `retry` зелёные у всех троих; h3spec 49/49.
Единственный отказ из четырёх — в пределах разброса стенда, но это разброс, а не
доказанный ноль, и записано так.

**Что это не починило.** quiche по-прежнему даёт разброс: 50, 22, 48 из 50. Но
картина изменилась качественно: в прогоне с трассировкой **все 29 соединений,
которые сервер видел, прошли рукопожатие, получили запрос и отдали ответ**, а
обрывается прогон на соединении, которого сервер не видел вовсе — ни одной
строки `accepted`. Три остальных клиента на том же стенде делают 50/50.
Чтобы идти дальше, нужны счётчики отброшенных датаграмм
(`quic.datagrams_dropped{reason}`, §9): они ответят, дошли ли те датаграммы до
нас и почему отброшены. `/metrics` в interop-конфигурации не поднят, потому что
там нет ни одного HTTP-маршрута, — это отдельная работа по обвязке и
следующий шаг, а не очередная правка вслепую.

### 3o. Пакет, приходивший раньше своих ключей, терялся всегда

Разбор остатка §3n продолжился теми же метками. Отказавшее соединение выглядит
так:

```
--- db4e9eb2: handshake=True request=False response=False
```

Рукопожатие завершено, а **запрос до h3 не дошёл ни разу** — при том что клиент
повторял его тридцать семь секунд. Дальше нашлось место, где он исчезал:

```c
quickeys_t* keys = &conn->rx[level];
if (!keys->valid) {
    /* ... Dropped rather than buffered; the peer will retransmit. */
    return 1;
}
```

Комментарий описывает редкий случай — «1-RTT обогнал рукопожатие на
переупорядочивающем пути». На деле случай не редкий, а **обязательный**:
`quictls_advance`, который ставит ключи приложения, вызывается **после** цикла
по пакетам датаграммы. Значит 1-RTT пакет, склеенный за Finished клиента в одной
датаграмме, гарантированно приходит раньше своих ключей — и это ровно то, что
шлёт клиент, у которого запрос готов к моменту завершения рукопожатия.

Починка — отложить такой пакет и повторить обработку сразу после того, как
рукопожатие поставило ключи; повтор ровно один, и ничего не хранится дольше
одного вызова, так что расти этому буферу пир не может.

**Насколько это реально** (счётчик повторов, кейс `transfer`, одно соединение):

| клиент | пакетов отложено и обработано |
|---|---|
| quiche | 1 |
| picoquic | 1 |
| ngtcp2 | 1 |
| quic-go | 0 |

То есть против трёх реализаций из четырёх мы теряли по пакету **на каждом
соединении**, и стоило это полного повтора запроса.

**Но красноту quiche это не сняло** — и это стоит записать прямо. Симптом
«рукопожатие есть, запроса нет» после правки воспроизводится снова, значит у
него есть и вторая причина. Правка оставлена, потому что она обоснована
независимо и измерена таблицей выше, а не потому, что что-то починила в
матрице; §3m по quiche остаётся открытым.

### 3p. Кредит потоков возвращался и тут же терялся

§3o дала регрессию, и она стоит того, чтобы её записать целиком. После правки
`multiplexing` у picoquic встал ровно на **100 файлах из 1999** — а 100 это
`initial_max_streams_bidi`, то есть сервер перестал выдавать кредит на новые
потоки. Воспроизводилось три раза из трёх.

Бисекция заняла два прогона: закоммиченное состояние — 1999/1999 за 10.7 с,
с отключённым откладыванием — то же самое, значит виновата §3o. Но при этом за
весь прогон откладывался **ровно один пакет**. Один пакет не может стоить
девятнадцати сотен потоков — значит он не причина, а спусковой крючок.

Причина нашлась в `quicconn_send`, в двух соседних строках:

```c
__streams_reap(conn);                 /* внутри: want_write = 1 ради MAX_STREAMS */

if (!more_pending)
    atomic_store_explicit(&conn->want_write, 0, ...);   /* и тут же гасит */
```

Освобождение потока пира возвращает кредит и поднимает `want_write` — именно
затем, чтобы построился кадр `MAX_STREAMS`, о чём в `__streams_reap` есть
отдельный комментарий. Следующая же строка флаг сбрасывает. Работало это только
потому, что у занятого соединения всегда находился другой повод построить
пакет; у соединения, чей последний запрос только что закончился, повода нет, и
оно встаёт на `initial_max_streams_bidi` с кредитом, лежащим в памяти сервера.

Починка — сбрасывать флаг **до** уборки, а не после. Порядок «уборка после
цикла» сохранён: он про use-after-free на `conn->streams` и к флагу отношения
не имеет.

**Урок, третий раз за фазу и самый дорогой.** Правка §3o изменила тайминг
единственного пакета — и этого хватило, чтобы давняя гонка перестала
маскироваться. Когда правка ломает то, к чему не имеет отношения, вопрос не
«что я сломал», а **«что я перестал прикрывать»**: `multiplexing` проходил не
потому, что кредит работал, а потому, что всегда находился лишний пакет.

| `multiplexing` после §3p | |
|---|---|
| picoquic, ngtcp2, quic-go | ✔ 1999/1999 |
| quiche | ✘ 950/1999, выход 101 (паника Rust, §3m) |

**Старое наблюдение, оставленное как есть.** У падающих соединений quiche в
логе видна подпись
`srtt=1213180` — первая RTT-выборка в 1.2 секунды на пути с реальными 33 мс,
причём на свежем соединении. Дальше PTO считается от неё, и всё становится
медленным. Оценщик при этом совпадает с §5.3 дословно (первая выборка задаёт
`smoothed_rtt` напрямую, `ack_delay` не применяется до подтверждения
рукопожатия), то есть сама выборка законна: её даёт подтверждение, пришедшее
через 1.2 с после отправки. Что стоит посмотреть дальше — **откуда берётся такое
подтверждение**: по §5.1 выборка снимается с наибольшего *впервые*
подтверждённого пакета, и если им оказывается старый пакет при потерянных
новых, оценка раздувается законно, но вредно. Это уже вопрос к обнаружению
потерь, а не к матрице.

### 3q. TSan: чем нагружать и чем не получится

**2026-08-11.** Правки §3h–3p трогают `quicconn_recv` (порядок обработки пакетов
в датаграмме), `quicconn_send` (порядок гашения `want_write` относительно уборки
потоков) и добавляют чтения `conn->cc`/`conn->loss` в новых местах — то есть код,
который на нескольких воркерах бегает под connection-локом. Проверялось всё это
ASan-сборкой и раннером, а для гонок в проекте есть отдельный режим
(`-DSANITIZE=thread`, взаимоисключающий с ASan), и он не использовался. Пробел
закрыт: **юнит-тесты под TSan 101660/101660, ноль предупреждений**.

**Ловушка, из-за которой первая нагрузка ничего не измерила.** `quicclient -n N`
— это N параллельных запросов в одном соединении, и потолок у него **восемь**:
клиент держит `CLIENT_MAX_STREAMS` (16) слотов, а `h3client_get_many` отказывает
при `count > CLIENT_MAX_STREAMS / 2`. Прогон с `-n 15` при этом не падает
внятно: рукопожатие проходит, а фаза запросов сообщает `FAIL: not every response
completed` — то есть выглядит как отказ **сервера**. Порог нащупывается сразу
(`-n 8` — OK, `-n 10` — FAIL) и одинаков на ASan и TSan, что и есть подсказка:
санитайзер тут ни при чём.

Что из этого следует для нагрузки: конкурентность набирается **процессами**, а
не `-n`. Так же, как в §7a, где 120 соединений — это 120 процессов по одному
соединению, а не `-n 120`.

### 3r. `/metrics` в стенде, и что он сказал про quiche

§3n остановился на том, что дальше нужны счётчики, а `/metrics` в
interop-конфигурации не поднят. Теперь поднят: `"metrics": true` и один маршрут
на `lib_metrics.so` в `interop/run_endpoint.sh`, образ кладёт этот единственный
обработчик рядом с бинарником. Отдаётся **по TCP-листенеру**, а снимается изнутри
контейнера:

```
docker exec server curl -sk --resolve server:443:127.0.0.1 https://server/metrics
```

Три решения тут не косметические. Через QUIC спрашивать нельзя: вопрос ровно про
то, что теряется на пути к нам, и ответ терялся бы вместе с ним. Изнутри
контейнера — потому что с хоста в сеть симулятора хода нет. И `server`, а не
`localhost`: имя, которого нет в `domains`, отдаёт 404, а не маршруты первого
vhost (§3s — это оказалось отдельной находкой).

Снимать приходится **опросом раз в две секунды, а не один раз в конце**:
`--abort-on-container-exit` останавливает сервер в тот же момент, когда выходит
клиент, и к возврату `up` спрашивать уже некого. Опрос по умолчанию выключен
(`METRICS=1`): каждая выборка — это TLS-рукопожатие с сервером под тестом, а на
кейсах с потерями, где опоздавший таймер стоит рукопожатия, базовая линия
обязана уметь сказать, что её не трогали.

**Первый же ответ закрыл §3n — и не в пользу гипотезы.** На провалившемся
прогоне `handshakeloss` (quiche) при 76 полученных датаграммах **все `drop.*`
нули**. То есть датаграммы соединения, которого сервер «не видел», до нас не
доходили вовсе, а не отбрасывались нами. Гипотеза «дропаем до `accepted`»
снята; разброс quiche остаётся, и его причина не здесь.

Сам разброс, пять прогонов `handshakeloss` подряд: **50, 50, 50, 45, 28 из 50**.
Метрики его не создают — три из этих пятидесяти сняты с включёнными счётчиками, а
28 выпало без опроса. Разбор одного отказа — в §3t.

**А красная клетка `multiplexing` оказалась чужой.** Клиент quiche создаёт файл
на каждый из 1999 запросов и держит его открытым до конца прогона, упираясь в
дефолтные 1024 дескриптора контейнера:

```
thread 'main' panicked at apps/src/common.rs:128:23:
Error creating file for https://server4/enough-red-marker,
attempted path was /downloads/enough-red-marker: Too many open files (os error 24)
```

Это и есть «код 101» из сноски ² к §3m — паника Rust, о которой там было сказано
только, что три остальных клиента те же файлы возят. Решающая проверка заняла
один прогон: **quiche против собственного quiche-сервера падает так же** —
959/1999, тот же код 101. После `nofile: 65536` для клиентского контейнера тот же
клиент против cwfr даёт **1999/1999 за 4.9 с**.

Урок ровно тот же, что в §3h: **прежде чем чинить сервер, нужно убедиться, что
меряют сервер.** Здесь харнесс мерил лимит дескрипторов в контейнере клиента, и
единственный дешёвый способ это увидеть — подставить на место cwfr чужую
реализацию. Пока такой контрольной точки нет, любая красная клетка читается как
наша.

**Что для этого понадобилось в стенде.** Клиентский лог (`/logs/log.txt` у
quiche — там и была паника) не доставался: compose объявляет `SSLKEYLOGFILE`,
`QLOGDIR` и пишет pcap в `/logs`, но никуда `/logs` не монтирует. Раньше
монтирование включалось только вместе с подменой серверного эндпоинта
(`DBG_ENDPOINT`); теперь override берётся всегда, а `DBG_ENDPOINT` по умолчанию
указывает на штатный `interop/run_endpoint.sh` — то есть логи всех трёх сторон
остаются от каждого прогона даром.

### 3s. Незнакомый `:authority` был протокольной ошибкой вместо 404

Найдено при обвязке §3r и починено, потому что это расхождение, а не мелочь.
Запрос на имя, которого нет ни в одном `domains` этого листенера, получал:

| | ответ |
|---|---|
| HTTP/1.1 | `404` |
| HTTP/2 | `RST_STREAM(PROTOCOL_ERROR)` (curl: exit 92) |
| HTTP/3 | `H3_MESSAGE_ERROR` на потоке |

Оба кода приходили из одного места: `httpparser_select_server` возвращает
`HOST_NOT_FOUND`, а h2 и h3 сваливали **любой** её неуспех в «malformed».

Так нельзя по двум причинам. Во-первых, §4.1.2 (и §8.3.1 в h2) определяют
malformed через *форму* поля: отсутствующий, повторённый или синтаксически
негодный псевдо-заголовок. У этого запроса с формой всё в порядке — он адресован
не сюда, а это результат маршрутизации, о котором эти параграфы не говорят.
Во-вторых, один и тот же запрос не должен получать разный ответ в зависимости от
того, каким протоколом он приехал, — а HTTP/1.1 отвечал 404 задолго до того, как
появились h2 и h3.

Теперь `HOST_NOT_FOUND` → 404 во всех трёх (`H2_REQUEST_MISDIRECTED`,
`H3STREAM_ERR_MISDIRECTED` — из тех статусов, что **отвечаются**, а не сбрасывают
поток, рядом с 413/431/500). `BAD_REQUEST` из той же функции — пустой authority,
незакрытый IPv6-литерал — остаётся stream error: вот это как раз malformed по
букве §4.1.2.

Счётчик `http3.misdirected` заведён отдельно от `responses.4xx`: растущий он
означает либо клиента, стучащегося не туда, либо vhost, в чьём `domains` не
хватает имени, которым его реально зовут, — а в общей куче 404-х от обработчиков
этого не видно.

Проверено: `quicclient -a nosuchhost` → 404 (был сброс потока), curl по h2 → 404
(был exit 92), h1.1 без изменений. Регрессия: h3spec 49/49, h2spec 146 passed /
0 failed, unit 101660/101660.

### 3t. FIN, доехавший только внутри дубликата

Быстрый воспроизводитель нашёлся сам: в `fast.sh` кейс `handshakeloss5` дал
`FAIL 5/5 files, client exit 254, 48.5 с` — **все файлы скачаны верно**, а клиент
всё равно вышел с ошибкой, потратив на пять соединений 48 с вместо 5.5. Это тот
же отказ, что портит `handshakeloss`, только за минуту и с логами.

Последнее соединение в клиентском логе (quiche, TRACE):

```
59.653  rx pkt Short pn=15 len=1101
59.653  rx frm STREAM id=0 off=0 len=1079 fin=false
59.653  rx frm HEADERS stream=0 → 200; rx frm DATA len=1024 → got 1024 bytes
59.674  rx pkt Short pn=16 len=1101
59.674  rx frm STREAM id=0 off=0 len=1079 fin=true      ← те же байты, теперь с FIN
...
14:53:29  connection timed out after 35.8s and only completed 0/1 requests
```

Ответ доехал целиком — файл на диске верный, — но **FIN приехал только внутри
кадра, все данные которого дубликат**, и quiche запрос завершённым не счёл. Отсюда
и `5/5 files` при ненулевом коде выхода: проверка по файлам эту поломку не видит,
её видит только клиент.

**Что установлено.** На чистом пути FIN едет вместе с данными — локально
`quicclient` видит `STREAM 0, 77 bytes at 0 FIN` одним кадром. Значит в отказе
первая передача ушла **без** FIN, то есть на момент сборки пакета
`quicsendbuf.fin` ещё не был выставлен, а FIN уехал отдельным пустым кадром на
`write_off`; потом оба были объявлены потерянными, и ретрансмиссия склеила их
обратно — `quicsendbuf_next` отдаёт потерянный диапазон целиком, а строка
`*out_fin = fin && !fin_sent && offset + len == write_off` навешивает на него FIN.
Потери здесь ложные: пакет 15 дошёл, ACK на него ещё летел.

**Механизм воспроизведён детерминированно, за секунды и без стенда.** Полсотни
строк на голом `quicsendbuf` (положить тело → отправить без FIN → `finish()` →
отправить FIN пустым кадром → объявить потерянными оба):

```
packet 1: off=0 len=10 fin=0        тело ушло раньше, чем ответ был завершён
packet 2: off=10 len=0 fin=1        FIN отдельным пустым кадром
после потери одних данных: next() -> off=0 len=10 fin=0
после потери и FIN-кадра:  next() -> off=0 len=10 fin=1   ← вот оно
```

`quicsendbuf_next` отдаёт потерянный диапазон целиком, а `*out_fin = fin &&
!fin_sent && offset + len == write_off` навешивает на него FIN — и пир получает
конец потока в кадре, все данные которого дубликат. **И второго шанса нет:**
пакет с этой ретрансмиссией доходит и подтверждается, `fin_acked` выставляется,
`quicsendbuf_complete` истинно — сервер считает поток законченным ровно в тот
момент, когда пир считает, что он не начал заканчиваться. Никакой таймер этого не
разбирает: с нашей стороны всё отправлено и подтверждено.

**Правка — одна строка и комментарий.** FIN больше не едет на ретрансмиссии: он
навешивается только на **свежие** данные, а на повторе придерживается, и ветка
пустого кадра отдаёт его отдельно на следующем кадре. У пустого кадра нет
данных, которые можно счесть дубликатом, — отбрасывать в нём нечего. Цена — один
маленький кадр, и только когда хвост потока был объявлен потерянным.

Почему именно так, а не «починить порядок в h3»: потери — эвристика, диапазон,
объявленный потерянным, сплошь и рядом у пира уже есть, и любая ретрансмиссия
хвоста — это кадр, который приёмник вправе выкинуть целиком. Пока FIN ездит на
данных, надёжность конца потока равна готовности пира заглядывать внутрь заведомо
дублирующего кадра. RFC 9000 §2.2 требует уметь принимать повторные данные и
нигде не разрешает выбрасывать вместе с ними флаг конца, а §4.5 делает FIN
единственным источником final size, — то есть quiche здесь, похоже, неправ. Но
правота ничего не чинит, а не ставить пира в это положение стоит одну строку.

**Второй конец — порядок в h3, разобран и оставлен как есть.** Первая передача
ушла без FIN потому, что `h3_data_write` при исчерпанном write-ahead-бюджете
возвращает `H3_DATA_BLOCKED` **до** блока `if (is_last && fin_allowed)`, а
`h3_write_filter.__body` вызывается снова уже отдельным проходом — между
проходами воркер успевает собрать пакет из того, что в буфере. После правки
транспорта это стоит одного пустого кадра и ничего больше, поэтому трогать
общую цепочку фильтров ради этого не стал.

**И тут же вскрылся тупик, который эта правка вытащила на свет.** Первые пять
прогонов `handshakeloss` после неё: 4, 50, 35, 17, 50 — хуже базовой линии того
же дня (50, 50, 50, 45, 28), и **два прогона впервые упёрлись в 420-секундный
таймаут**, чего в базовой линии не было ни разу (максимум 158 с). Правка на одну
строку не могла ухудшить восстановление — значит она что-то открыла.

Открыла вот что. Цикл сборки пакета специально пропускает висящий FIN мимо
проверки закрытого окна:

```c
if (room == 0 && !(s->send.fin && !s->send.fin_sent)) { ...; continue; }
```

а `quicsendbuf_next` первой же строкой отвечал `if (buf == NULL || max_len == 0)
return 0;`. То есть исключение было **недостижимо**: при закрытом окне FIN не
собирался никогда. Раньше это почти не проявлялось — FIN ездил на данных, а
ретрансмиссия обходит flow control по §3i, — и ровно эту маскировку правка сняла.
Поток, у которого окно закрылось до отправки FIN, закончиться не мог вовсе; 420 с
это оно и есть.

Починка: `max_len == 0` больше не отказ на входе. Пустому кадру место для данных
не нужно, а ветки с данными и так останавливаются на `len == 0`. Проверка в обе
стороны: с нулевым бюджетом FIN выходит, данные — нет.

**Урок тот же, что в §3p, третий раз за фазу.** Правка ухудшила то, к чему не
имела отношения, и вопрос был не «что я сломал», а **«что я перестал
прикрывать»**: исключение для закрытого окна не работало с самого начала, и это
держалось только на том, что FIN обычно уезжал другим путём.

**Проверено.** Юнит-тесты на обе последовательности (`test_quic_buffers`:
«a retransmission does not carry the FIN», «a pending FIN survives a zero
budget»), unit 101672/101672, h3spec 49/49, `quicclient --loss-in 25..30`
четырнадцать сидов подряд, на чистом пути FIN по-прежнему едет вместе с данными
(`STREAM 0, 77 bytes at 0 FIN`).

**Чего этими правками доказать нельзя — и это главное, что тут стоит записать.**
`handshakeloss` (quiche) по пять прогонов на конфигурацию:

| | прогоны | прошло | худшее время |
|---|---|---|---|
| базовая линия того же дня | 50, 50, 50, 45, 28 | 3/5 | 158 с |
| только правка FIN | 4, 50, 35, 17, 50 | 2/5 | **420 с (таймаут ×2)** |
| обе правки | 33, 30, 50, 50, 20 | 2/5 | 192 с |

Таймауты ушли — вторая правка сняла то, что натворила первая. А **доля
прошедших не улучшилась**: 2 из 5 против 3 из 5 при историческом разбросе от 3
до 50 — это шум, и называть его ни победой, ни регрессией нельзя. Отсюда два
честных вывода:

1. §3t был настоящим тупиком (он воспроизводится детерминированно и кончается
   потоком, который не может закончиться), но **причиной красноты quiche он не
   был** — по крайней мере не единственной. Под потерями остаётся что-то ещё, и
   `handshakeloss` меряет сумму.
2. У правки есть цена, которую стоит держать в уме: FIN теперь доставляется
   отдельным кадром, и под 30 % потерь у него своя вероятность пропасть, то есть
   ещё один PTO на хвост. Разделить эту цену и шум на пяти прогонах невозможно;
   если когда-нибудь понадобится, различающий эксперимент — не матрица, а
   счётчик «FIN уехал отдельным кадром» против времени завершения потока.

Правки оставлены, потому что каждая обоснована **независимо от матрицы**: первая
закрывает доказанный тупик, вторая — недостижимую ветку, которую сама же и
обнажила. Обосновывать их «стало зеленее» было бы неправдой.

**Полная матрица после всех правок дня: 62 из 64** — ровно как в §3m, но красное
переехало. `chacha20`/quiche по-прежнему 127 (сноска ¹, отказ клиента), а вторая
клетка теперь **`handshakecorruption`/ngtcp2, 48 из 50 за 409 с**, при том что
`handshakeloss` у всех четырёх клиентов зелёный, включая quiche. Это и есть
подпись мигания: пятьдесят соединений под помехами дают отказ примерно раз в
несколько прогонов, и какая именно клетка покраснеет — вопрос жребия, а не
клиента. Считать этот прогон подтверждением правок нельзя ровно по той же
причине, по которой нельзя было считать предыдущий их опровержением.

### 3u. Серверная картина отказа: три строки, которых не хватало

Два раза подряд (§3n и §3t) разбор упирался в одно и то же: клиентская трасса
есть всегда, а со стороны сервера — только `accepted`, `handshake complete` и
строка PTO. «Сервер чего-то ждёт» дальше этого не разбирается. Добавлено три
`log_debug`, все с той же меткой `odcid`, что и PTO:

```
quic: rtt  cid=... level=2 pn=7 sample=1213180 ack_delay=0 srtt_prev=0 min_prev=0 first=1
quic: lost cid=... level=2 pn=5 age=41200 by=time
quic: send cid=... stream=0 off=0 len=1079 fin=0 rtx=0
```

Каждая отвечает на вопрос, который уже задавался и оставался без ответа:

- **`rtt`** — на §3p: откуда берётся первая выборка в 1.2 с на пути с реальными
  33 мс. §5.3 кладёт первую выборку в `smoothed_rtt` целиком, без вычета
  `ack_delay`, и от неё дальше считается всё — PTO и временной порог потерь.
  Отличить медленный путь от подтверждения, опоздавшего по своим причинам, можно
  только зная, **какой пакет** дал выборку и когда он ушёл. Поэтому в строке
  номер пакета и величина выборки, а не одно `srtt`.
- **`lost`** — какое из двух правил сработало. `by=count` значит, что более
  поздний пакет дошёл, а этот нет; `by=time` значит, что не слышно ничего в
  течение loss delay и объявление — догадка. Именно догадка и рождает
  ретрансмиссию данных, которые у пира уже есть (§3t).
- **`send`** — ответ глазами пира, с флагом `rtx`. Первая передача и повтор на
  том же смещении на проводе выглядят одинаково, а значат противоположное; §3t
  разбирался без этой строки и стоил лишнего круга.

Метка в `quicloss_t` лежит копией четырёх байт (`quicloss_set_cid_tag`), а не
указателем на соединение: диагностика не повод модулю восстановления знать про
слой выше.

Четвёртая строка, `recv cid=... level=N pn=X elic=0/1`, добавлена следом, когда
трёх не хватило (§3v): без неё «соединение замолчало» не отличается от «пир
замолчал», а лечится это противоположными способами.

**Чем за это платят.** `log_*` в этом проекте — это `vsyslog`, а в контейнере без
демона syslog он через `LOG_CONS` уходит в консоль, то есть запись на каждый
кадр. Урок §2 («отладочный `log_error` внутри `quicconn_tick` чинил баг: запись
сдвигала тик») никуда не делся, и `send` — самая частая из трёх. Поэтому все три
на уровне `debug`, которого в штатной interop-конфигурации нет, а включается он
подменой `run_endpoint.sh` через `DBG_ENDPOINT`. И читать под ними надо
структурные вопросы («поставил ли сервер ответ в очередь и когда»), а не
тайминговые: под включённым debug тайминг уже не тот.

### 7a. Ход работ

**Сделано (2026-08-09): первые измерения на Release.** Не то, что просит таблица
выше — сравнения с h2 по req/s нет и пока быть не может: системный `h2load`
(nghttp2 1.68) собран без ngtcp2, HTTP/3 он не умеет, а `curl` этой машины — без
h3. Мерилось то, для чего инструмент есть: масштабирование по числу соединений
своим клиентом, N процессов на N соединений.

| Соединений | Wall | Успешных | CPU сервера на соединение |
|---|---|---|---|
| 1 | 0.01 с | 1/1 | — |
| 10 | 0.03 с | 10/10 | 1.0 мс |
| 30 | 0.05 с | 30/30 | 1.3 мс |
| 60 | 0.08 с | 60/60 | 1.2 мс |
| 120 | 0.13 с | 120/120 | 0.8 мс |

Стоимость соединения **плоская**. Это, среди прочего, ответ на вопрос, отложенный
в `01` §6a: обход списка соединений вместо кучи по дедлайнам на этих масштабах
ничего не стоит. Мерить снова — когда соединений станут тысячи.

**Найдено и починено: невидимая потеря датаграмм.** До настройки буфера всплеск
из 120 рукопожатий давал 82–86 успешных из 120, и **все счётчики сервера
показывали ноль**: ни отказов, ни своих дропов, только PTO. Датаграммы выбрасывало
ядро по переполнению приёмной очереди сокета — `recvmmsg` не может сообщить о том,
чего не получил, а наши счётчики считают только то, что мы отвергли сами.
Подтверждено извне: `UdpRcvbufErrors` 47 → 88 за прогон.

Закрыто двумя вещами:

- **`SO_RXQ_OVFL`.** Ядро умеет отдавать счётчик отброшенного в контрольном
  сообщении рядом с датаграммой; эндпоинт считает разницу и кладёт её в
  `quic.drop.kernel_overflow`. Это единственный счётчик дропов, который не про
  решение сервера, а про машину под ним. После включения тот же всплеск честно
  показал `drop.kernel_overflow: 51`.
- **`http3_so_rcvbuf`.** Ключ был, умолчание — системное (~212 КБ). С 4 МБ дропы
  ядра исчезают, и 120 соединений из 120 проходят за 0.13 с.

**Пропускная способность и CPU на мегабайт — измерены, и нашли две
квадратичности.** До этого весь стек h3 в тестах возил 59-байтные ответы; на
16-мегабайтном файле картина другая.

Сравнение с h2 сделано `curl`-ом по тому же файлу с того же сервера — это
единственная часть таблицы §7, для которой инструмент нашёлся.

| | Пропускная | CPU сервера на МБ |
|---|---|---|
| h3, как было | 69 МБ/с | 16.2 мс |
| h3, после починок | 85 МБ/с | 11.2 мс |
| h2 по TCP | 93–840 МБ/с | 4.6 мс |

Ориентир §7 — «не хуже 80 % от h2 по пропускной» и «худший случай 2× по CPU».
Пропускная в норме, **CPU — примерно вчетверо, то есть вдвое хуже бюджета**.

Диагноз пришёл не от профилировщика: `perf` на этой машине запрещён политикой
ядра. Пришлось считать. 27 мкс CPU на датаграмму — это на порядок больше суммы
`sendmsg` и AEAD, значит дело не в них (гипотеза §7 про сисколл на датаграмму
здесь **не подтвердилась**). Зато CPU на мегабайт **рос с размером файла**
(10 мс/МБ на 8 МБ против 16.2 на 16 МБ) — а это подпись квадратичности.

1. **`quicloss_on_ack` обходил весь список неподтверждённых пакетов на каждое
   подтверждение**, проверяя каждый на вхождение в диапазоны. Длина списка —
   это congestion window; при тысяче пакетов в полёте и шести тысячах
   подтверждений выходит порядка 12 млн проверок, что и есть наблюдаемые сотни
   миллисекунд. Список отсортирован по номеру, и ответ тоже — цикл просто этим
   не пользовался. Остановка на `pn > largest`: **16.2 → 11.2 мс/МБ, 69 → 85 МБ/с.**
2. **`quicsendbuf` уплотнялся `memmove`-ом на каждое подтверждение** — двигал
   весь остаток (до write-ahead-бюджета, четверть мегабайта) ради двух пакетов,
   которые подтверждение обычно закрывает. Заменено на подвижное начало с
   уплотнением при мёртвом префиксе в половину буфера. **На этой нагрузке число
   не сдвинулось** — буфер мал и memmove не доминирует; правка оставлена, потому
   что квадратичность настоящая и вылезет при большем write-ahead или медленных
   подтверждениях.

Рост с размером остался (8.8 мс/МБ на 8 МБ против 12.5 на 16), то есть третий
источник есть. Без профилировщика дальше — гадание, поэтому здесь остановились.

**Границы честности этих чисел.** Клиенты — отдельные процессы, по одному на
соединение, и выше сотни это меряет уже машину под стендом, а не сервер. h2 в
таблице разогнан кэшем страниц (93 МБ/с на холодном файле против 840 на тёплом),
так что «в 4 раза по CPU» надёжнее, чем отношение пропускных. Большие передачи
годятся **только для Release**: под ASan те же 16 МБ идут 33 секунды вместо 0.2,
и в обычную матрицу помех их класть нельзя — они измеряют санитайзер. Время
рукопожатия из таблицы §7 по-прежнему не измерено.

---


### 3v. Чем на самом деле кончается отказ quiche: рукопожатие, которого не было

Первое же применение §3u дало ответ, и не тот, который искали. Разбор трёх
отказавших прогонов `handshakeloss` (под debug они падают чаще — сама диагностика
сдвигает тайминг, и это тоже данные).

**Отказавшее соединение целиком, со стороны сервера:**

```
accepted cid=d97c7c3a
recv cid=d97c7c3a level=0 pn=0  elic=1
pto  cid=d97c7c3a level=0 count=1 ... srtt=0 amp=1536 validated=0
rtt  cid=d97c7c3a level=0 pn=2 sample=33985 ... first=1
recv cid=d97c7c3a level=0 pn=1  elic=0
pto  cid=d97c7c3a level=2 count=1..3 ...
recv cid=d97c7c3a level=2 pn=9  elic=0
recv cid=d97c7c3a level=2 pn=14 elic=1
```

и на этом всё: **`handshake complete` нет, `send` нет ни одного, уровень 3 не
получен ни разу**. Четыре принятых пакета за пятьдесят девять секунд.

Со стороны клиента за то же время — 181 запись, 26 пакетов, требующих
подтверждения, **ноль полученных ACK**, запрос отправлен один раз и повторён ни
разу. Для сравнения, соседние удачные соединения того же прогона: 8 пакетов → 3
ACK и 16 → 4.

**Что из этого следует.** Клиент считает рукопожатие завершённым и перешёл на
1-RTT; сервер его не завершил, а значит не поставил ключи приложения — и каждый
последующий пакет клиента для нас неразбираем и до `recv` не доходит вовсе.
Отсюда и ноль ACK: подтверждать нечего, потому что ничего не принято. Это
устойчивый тупик, а не медленное соединение: обе стороны живы, обе шлют, и ни
одна не может сообщить другой, чего ей не хватает.

**Гипотеза §3p — про раздутую первую выборку RTT — этим разбором снята.**
Выборки на отказавшем соединении здоровые: `first=1 sample=33985` при реальных
33 мс, дальше 32083. Медленным его делает не оценка пути, а отсутствие
рукопожатия. Строку `rtt` стоило добавить хотя бы затем, чтобы закрыть эту
версию: она стоила бы дня чтения кода восстановления.

**Следующий шаг — почему рукопожатие не завершилось.** `recv level=2 pn=14
elic=1` говорит, что Handshake-пакет от клиента принят; Finished либо был в
потерянном пакете и клиент его не повторил, либо пришёл и был отброшен нами.
Второе — не праздное подозрение: в `a2ef441`..`14f0d72` появилась проверка
повторных криптоданных, и «дубликат», отброшенный при том, что оригинал до TLS не
дошёл, даёт ровно эту картину. Проверяется это одной строкой на приёме CRYPTO
(offset, длина, принято/дубликат) — то есть тем же способом, что и всё в §3u, и
это первое, что стоит сделать дальше.

### 3w. Сервер замолкает с неподтверждённым флайтом

§3v назвал подозреваемым разбор повторных криптоданных. Строка на приёме CRYPTO
(`off`, `len`, `dup`) и парная к ней `tls advance` (`complete`, ключи,
`params_seen`) эту версию **сняли**: на удачном пути видно ровно
`crypto level=2 off=0 len=36 dup=0` → `tls advance complete=1`, а на отказавших
`crypto level=2` не появляется вовсе. Ничего не отбрасывается — Finished клиента
до нас просто не доезжает.

Заодно добавлена `rstream` — зеркало `send` на приёме, потому что «запрос не
дошёл» и «запрос дошёл и застрял выше транспорта» до сих пор выглядели одинаково.

**Что видно на отказавшем соединении целиком** (`0a5e3bdf`, шесть прогонов под
debug, из них два отказа):

```
pto  level=2 count=1 in_flight=1687 cwnd=7017 amp=4930 validated=0
pto  level=2 count=2 in_flight=2510 cwnd=7017 amp=4107 validated=0
pto  level=2 count=3 in_flight=3333 cwnd=7017 amp=3284 validated=0
pto  level=2 count=4 in_flight=4156 cwnd=7017 amp=2461 validated=0
rtt  level=2 pn=11 sample=32256 ...
lost level=2 pn=0,1,4,7,8,9  (по count и по time)
recv level=2 pn=14 elic=0
tls advance complete=0
tls advance complete=0
<конец: больше об этом соединении сервер не сказал ничего>
```

**Ключевое — это молчание.** После последнего принятого Handshake-пакета
(подтверждение, `elic=0`) сервер не делает **ничего**: ни PTO, ни объявления
потерь, ни отправки, ни закрытия — при том что его собственный флайт с
сертификатом не подтверждён, а клиент ещё пятьдесят девять секунд ждёт и шлёт.
Клиент при этом не получает ни одного ACK, потому что получать нечего.

Гипотеза, которую это подсказывает и которую надо проверять следующей: все наши
Handshake-пакеты к этому моменту **объявлены потерянными и удалены из
`space->sent`**, то есть `ack_eliciting_in_flight` обнулился — и таймер PTO по
§6.2.1 снимается, потому что «в полёте ничего нет». Данные при этом никуда не
делись: они переставлены в очередь на повтор. Флайт, ждущий отправки, при снятом
таймере и есть вечное молчание. §6.2.2.1 предусматривает ровно этот тупик и
говорит, что таймер должен оставаться взведённым, пока рукопожатие не завершено.

Проверяется это тем же способом, что и всё в §3u: одной строкой на взведение и
снятие таймера (`level`, `ack_eliciting_in_flight`, `unsent`, когда сработает) —
и это следующий шаг. Правку до этой проверки делать нельзя: ровно так в §3t
правка «по механизму» открыла недостижимую ветку в другом месте.

**Сколько это стоит наблюдать.** Отказ ловится примерно раз в три прогона
`handshakeloss` под debug (шесть прогонов: 50, 50, 50, 50, 4, 50 из 50) — и сама
диагностика двигает тайминг в обе стороны, так что «стало лучше» по этим числам
читать нельзя. Ловится только картина отказа, не его частота.

### 3x. Подтверждения: версия, снятая собственным измерением

Строка `amp-block` (отказ построить пакет из-за §8.1, с бюджетом и остатком)
добавлялась, чтобы решить вопрос «чей это тупик»: сервер, упёршийся в собственный
анти-усилительный лимит, делает ровно то, что велит RFC, и может только ждать
пира. Ответ получился неожиданный — **ни одной такой строки на отказе не
появилось**, и версия про бюджет отпала вместе с версией §3w про снятый таймер.

Отказавшее соединение (`3dccf94a`) выглядит так:

```
recv cid=3dccf94a level=3 pn=11 elic=1
rstream cid=3dccf94a stream=2 off=0 len=24 fin=0
recv cid=3dccf94a level=3 pn=13 elic=1
rstream cid=3dccf94a stream=6 off=0 len=1  fin=0
recv cid=3dccf94a level=3 pn=14 elic=1
rstream cid=3dccf94a stream=2 off=0 len=24 fin=0     <- то же смещение снова
recv cid=3dccf94a level=3 pn=15 elic=1
rstream cid=3dccf94a stream=6 off=0 len=1  fin=0
... и так до конца соединения, validated=1, amp-block ни разу
```

Рукопожатие завершено, адрес подтверждён, пакеты **приходят непрерывно и почти
все ack-eliciting**. Клиент при этом раз за разом шлёт заново свой control-поток
(id=2, 24 байта) и qpack-поток (id=6) с **нулевого смещения** — то есть считает,
что они не дошли. За всё соединение он получил от нас **один** ACK на 19
отправленных 1-RTT пакетов.

Отсюда вывод, который уже не про пира: **мы получаем пакеты, требующие
подтверждения, и не подтверждаем их.** Клиент повторяет служебные потоки вечно,
запрос (stream 0, отправленный один раз) так до h3 и не доходит, и соединение
умирает по таймауту клиента. Никакой сети для этого не нужно — нужно только не
ответить.

**Где искать.** Приём ставит дедлайн (`quicack_on_received`) и поднимает
`want_write`; `quicconn_next_timeout` учитывает `quicack_deadline`;
`__build_packet` пишет ACK первым. Значит ломается одно из трёх: соединение не
будят к дедлайну подтверждения, `quicack_should_send` отвечает «нет», или пакет
на этом уровне не строится вовсе. Различает их одна строка в обработчике таймера
— что сработало и что было должно, — и это следующий шаг.

**И этот вывод оказался неверен — снят следующей же строкой.** `dgram`
(каждая уходящая датаграмма: размер и в каких пространствах она несла
подтверждение) на **удачном** прогоне даёт: отправлено **849 датаграмм, и ACK
нёс каждая из 849**. Клиент за тот же прогон записал 154 полученных ACK на 624
своих ack-eliciting пакета.

То есть подтверждения мы шлём непрерывно, а «отношение 1 к 19» из абзаца выше
меряло не нашу политику подтверждений, а **сколько их доехало**. На удачном
прогоне то же отношение 0.247 и есть соединения с 0 из 9 — при том что сервер
подтверждал всё. Между «мы не отправили» и «сеть съела» разницы в клиентском логе
нет, а я эту разницу там прочитал.

**Ошибка метода, и она стоит отдельной записи.** Счётчик по квитанциям пира —
это измерение канала, а не отправителя. При 30 % потерь в обе стороны (и
всплесками, а не равномерно) он занижает наш выход вчетверо даже там, где всё
работает. Правило: **утверждение «сервер чего-то не сделал» доказывается только
строкой на стороне сервера**; клиентский лог годится, чтобы заметить странность,
и не годится, чтобы её объяснить. Ровно та же ошибка, что в §2 («диагностика,
меняющая тайминг, лжёт»), но с другой стороны: диагностика, меряющая не тот
конец, лжёт тоже.

Правка из этого абзаца всё же сделана и оставлена, но **не как исправление
симптома**: `quicconn_tick` не имел ветки для наступившего дедлайна
подтверждения, хотя `quicconn_next_timeout` специально просит разбудить себя к
нему, а обход эндпоинта шлёт только при поднятом `want_write`. Пробуждение
приходило и пропадало впустую. На отношение подтверждений это не повлияло
(0.202 до, 0.091 / 0.215 / 0.188 после — то есть в пределах того же шума), и
так и записано.

**Что теперь известно про отказ.** Рукопожатие завершается, адрес подтверждён,
бюджет не мешает, подтверждения уходят с каждой датаграммой — а клиент всё равно
повторяет служебные потоки с нулевого смещения и не доводит запрос. Значит
следующий вопрос уже не «что сервер не делает», а **что из отправленного не
доезжает и почему именно на этих соединениях**; и отвечать на него надо парой
`dgram` (наш выход) против pcap симулятора (что вошло в сеть), а не клиентским
логом.

**Пять версий за вечер, четыре сняты измерением.** §3p (раздутая первая выборка
RTT) — снята строкой `rtt`; §3v (отброшенный дубликат CRYPTO) — строкой
`crypto`; §3w (снятый PTO-таймер) и версия про анти-усилительный бюджет —
строкой `amp-block`; §3x (мы не подтверждаем) — строкой `dgram`. Каждая была
правдоподобна, и каждая стоила бы дня правки не того места.

### 3y. Контрольный опыт: тот же кейс у quiche против самого себя

Пять версий за вечер (§3p, §3v, §3w, бюджет, §3x) — и ни одна проверка не
спросила того, что §3r уже однажды сделал решающим вопросом: **а у чужого сервера
этот кейс проходит?** Стенд это умеет: образы раннера играют обе роли, и
`SERVER_IMG` подставляет чужой сервер к тому же клиенту.

`handshakeloss`, клиент quiche, сервером **сам quiche**:

| прогон | результат |
|---|---|
| 1 | ✔ 50/50, 153 с |
| 2 | ✔ 50/50, 260 с |
| 3 | (артефакт харнесса: сервер не поднялся, 12 с) |
| 4 | ✘ **31/50, exit 254**, 148 с |
| 5 | ✔ 50/50, 198 с |
| 6 | ✘ **38/50, exit 255**, 168 с |

Два отказа из пяти состоявшихся прогонов, и в клиентском логе — **дословно то
самое сообщение**, за которым шёл весь вечер:

```
connection timed out after 66.124832301s and only completed 0/1 requests
```

Отношение подтверждений там же — 0.289 против наших 0.202–0.247, то есть в том же
диапазоне, что ещё раз подтверждает вывод §3x: это отношение меряет канал.

**Вывод.** Остаточная краснота `handshakeloss`/quiche — свойство связки
«этот клиент + этот сценарий», а не наш дефект: эталонная реализация против самой
себя падает так же и примерно с той же частотой. Пятьдесят последовательных
рукопожатий при 30 % потерь всплесками в обе стороны с жёстким таймаутом клиента
— кейс на грани, и раз в два-три прогона он не проходит ни у кого.

**Чего это стоило и что из этого следует для порядка работы.** Контрольный опыт
занимает десять минут и стоил бы вечера, будучи сделанным первым. Правило §3r
(«прежде чем чинить сервер, убедись, что меряют сервер») было записано — и не
применено, потому что на этот раз симптом выглядел слишком «нашим»: сервер
молчит, подтверждений нет, запрос не доходит. Формулировка правила усилена:
**контрольный опыт с чужим сервером делается до первой гипотезы, а не после
пятой** — он дешевле любой из них и единственный отвечает на вопрос, есть ли
вообще что чинить.

**Что вечер всё-таки дал.** Диагностический набор §3u (`recv`, `send`, `rstream`,
`rtt`, `lost`, `crypto`, `tls advance`, `amp-block`, `dgram`) — он остаётся, и
именно он снял четыре версии из пяти. Плюс две правки, обоснованные независимо от
матрицы: FIN, не едущий на ретрансмиссии (§3t), и ветка наступившего дедлайна
подтверждения в `quicconn_tick` — пробуждение, которое `quicconn_next_timeout`
специально запрашивает, а обработчик игнорировал.

### 7b. Третий источник роста: где его точно нет (2026-08-13)

В §7a осталось незакрытым: CPU на мегабайт **растёт** с размером файла (8,8
мс/МБ на 8 МБ против 12,5 на 16), две квадратичности найдены и убраны, «третий
источник есть, без профилировщика не ищется». Теперь у стенда есть 100 МБ за
0,8 секунды настоящего времени, и первый вопрос — не «какой профиль», а
**воспроизводится ли рост там, где нет ни сокетов, ни h3**.

**Не воспроизводится.** Стенд, Release, тот же обмен, только транспорт:

| размер | CPU | мс/МБ |
|---|---|---|
| 8 МБ | 76 мс | 9,48 |
| 16 МБ | 134 мс | 8,39 |
| 32 МБ | 255 мс | 7,96 |
| 64 МБ | 496 мс | 7,75 |
| 100 МБ | 774 мс | 7,74 |

Двенадцатикратный диапазон, полка на 7,7; спад в начале — амортизация
рукопожатия. **Транспорт из подозреваемых исключён**, а вместе с ним половина
кода: остаются UDP-эндпоинт, слой h3, соединение/воркер и сам сокет.

**На живом сервере рост есть** (Release, те же файлы, CPU снят из
`/proc/<pid>/stat`, и учитываются только прогоны, где клиент дочитал файл
целиком): 8 МБ — 7,5–8,8 мс/МБ, 16 МБ — 8,8–11,3, 32 МБ — 22,8–28,1, 64 МБ —
25,8–35,2. Разброс большой, но разница между краями диапазона — втрое.

**Профиль первым делом поймал не сервер, а конфигурацию замера.** 25 % CPU в
`__printf_buffer` под `log_debug`: конфиг был взят из отладочной сессии, с
`level: debug`. Отсюда правило, которое стоит записать рядом с §2a («диагностика,
меняющая тайминг, лжёт»): **числа производительности, снятые не на продакшн-уровне
логирования, не числа** — и это касается в том числе цифр §7a, их стоит
перемерить, прежде чем на них опираться.

**И побочная находка, которая важнее самой производительности.** Двенадцать
передач по 64 МБ подряд — **четыре отказа, все на рукопожатии следующего
соединения**; тридцать одиночных рукопожатий подряд — ноль отказов. То есть
рукопожатие ломается только вслед за большой передачей. Счётчики, снятые вокруг
одного такого отказа, назвали механизм: `drop.kernel_overflow +26980` при
`connections_accepted +1` из двух — приёмная очередь эндпоинта переполнена, и
Initial нового соединения выброшен ядром вместе с потоком подтверждений
предыдущего.

Но: в серии, где `/metrics` снимался **между** передачами, переполнения нет
вовсе (нули на всех размерах). Пауза в десятки миллисекунд, которую вносит сам
замер, даёт воркеру разгрести очередь — измерение меняет измеряемое, и это
третий раз за фазу.

**Инструмент сделан:** строка на соединение при закрытии
(`quicendpoint_detach`, уровень `info`) — длительность, `rx_overflow` **за время
жизни именно этого соединения** (`quicconn_t::rx_overflow_at_accept` против
счётчика эндпоинта), число потоков, PTO и srtt. Процессный счётчик на вопрос
«росло ли переполнение с размером» ответить не мог: он суммирует всё с запуска.

Заодно **клиент теперь прощается** (`CONNECTION_CLOSE` перед выходом, §10.2), и
это оказалось условием измеримости: клиент, уходящий молча, оставляет соединение
жить весь idle timeout, и его окно захватывает **следующие** передачи. Первая
таблица вышла нечитаемой именно поэтому — три соединения подряд отчитались
одинаковым переполнением.

**И первые же чистые данные опровергли складную версию.** По размерам
(каждая строка — своё соединение):

```
   4 МБ  closed after 30339 ms, rx_overflow=34169, pto=3, srtt=163 ms
   8 МБ  closed after   307 ms, rx_overflow=0,     pto=0, srtt= 40 ms
  16 МБ  closed after   846 ms, rx_overflow=327,   pto=0, srtt=144 ms
  32 МБ  closed after 32054 ms, rx_overflow=33971, pto=1, srtt=593 ms
  64 МБ  closed after  1158 ms, rx_overflow=0,     pto=0, srtt= 16 ms
```

Самая большая передача прошла **с нулевым переполнением и лучшим srtt из всех**.
Переполнение с размером не связано — оно вспыхивает и гаснет; а строки с
огромным `rx_overflow` — это ровно те, что закрылись по idle timeout, то есть
до них не дошёл `CONNECTION_CLOSE` и их окно снова захватило чужие передачи.
Иначе говоря, гипотеза «большая передача душит приёмный путь, и оттого растёт
CPU на мегабайт» **не подтверждена**: она объясняла бы монотонность, которой
нет.

**Обе величины в одной серии** (Release, клиент прощается, каждая строка — своё
соединение) дают уже связную картину:

| размер | CPU/МБ | rx_overflow | srtt |
|---|---|---|---|
| 4 МБ | 7,50 | 0 | 9,9 мс |
| 8 МБ | 8,75 | 0 | 21,7 мс |
| 16 МБ | 10,62 | 800 | 126,6 мс |
| 32 МБ | 16,25 | 7020 | 289 мс |
| 64 МБ | ~19–23 | десятки тысяч | 300–480 мс |

Рост CPU монотонен и подтверждён. **Но переполнение объясняет только верх
таблицы**: между 8 и 16 МБ стоимость мегабайта выросла на треть при нулевом (и
затем почти нулевом) переполнении, а `srtt` к этому моменту уже вырос вшестеро.
То есть отброшенные датаграммы — поздняя стадия, а не причина.

**Следующая гипотеза — и она тоже опровергнута замером.** Если подтверждения
опаздывают на сотни миллисекунд там, где физика даёт микросекунды, естественно
предположить, что воркер надолго не возвращается к сокету. Добавлен счётчик
`max_rx_gap` — наибольший интервал между двумя чтениями сокета, снимаемый в
`__endpoint_read` и обнуляемый на accept (иначе он показывает простой между
прогонами: сорок секунд сервера, которому нечего делать). Замер:

```
   4 МБ  max_rx_gap  9,3 мс   srtt   9,9 мс
   8 МБ  max_rx_gap 16,4 мс   srtt  21,7 мс
  16 МБ  max_rx_gap 17,1 мс   srtt 126,6 мс
  32 МБ  max_rx_gap 28,2 мс   srtt  289 мс
```

**Сокет читается каждые 9–28 мс даже на самой большой передаче.** Воркер не
уходит на сотни миллисекунд; версия «приёмный путь голодает» мертва.

Остаётся то, что этот замер не покрывает: **сам измеряющий клиент**. Он читает
пачку датаграмм и шлёт одно подтверждение на всю пачку, а с ростом окна
перегрузки пачки растут — то есть и `srtt`, и всплески подтверждений могут быть
свойством нашего инструмента, а не сервера. Проверяется это одним прогоном по
правилу, которое здесь уже дважды окупилось (§3y, §3r): **поставить на место
нашего клиента чужой** — образы interop-раннера умеют обе роли — и посмотреть,
растут ли `srtt` и CPU/МБ так же. До этого замера искать в коде нечего.

### 7c. Инструмент оправдан, и тем же замером назван виновник (2026-08-13)

Вопрос §7b — «не наш ли клиент рисует этот `srtt`» — можно задать двумя
способами. Дорогой: поднять раннер и поставить на своё место чужого клиента.
Дешёвый: **померить ту величину, ради которой чужой клиент и звался**. Гипотеза
говорит, что подтверждения опаздывают, потому что наш клиент не успевает
разгребать датаграммы, — а «сколько датаграмма пролежала в очереди сокета»
ядро сообщает само, `SO_TIMESTAMPNS`. Разность «сейчас минус метка ядра» и есть
та часть чужого круга, которая принадлежит не сети, а процессу.

Метка снимается теперь **с обеих сторон**: в `quicclient` (`__net_recv_stamped`,
плюс собственные CPU и wall на запрос) и в эндпоинте сервера
(`udp_datagram_t::stamp_us` → `rx_dwell` в строке закрытия соединения). Одна
величина, измеренная симметрично, отвечает на вопрос «чья очередь» без единой
чужой реализации.

**Ответ — не наша.** Release, уровень логирования `info`, каждая строка — своё
соединение:

| размер | `srtt` сервера | очередь **сервера** avg/max | очередь **клиента** max | CPU сервера | CPU клиента |
|---|---|---|---|---|---|
| 4 МБ | 8,9 мс | 7,3 / 11,2 мс | 0,07 мс | 7,5 мс/МБ | 3,6 мс/МБ |
| 8 МБ | 29,1 мс | 22,1 / 33,7 мс | 0,11 мс | 7,5–11 мс/МБ | 4,2–4,8 мс/МБ |
| 16 МБ | 113–120 мс | 78–82 / 126–134 мс | 0,15 мс | 16,5 мс/МБ | 3,8–3,9 мс/МБ |
| 32 МБ | 258 мс | 212 / 430 мс | 0,15 мс | 24–26 мс/МБ | 4,0–4,2 мс/МБ |
| 64 МБ | 303 мс | 80 / 214 мс | 0,11 мс | 25–33 мс/МБ | 3,7–3,8 мс/МБ |

Два вывода читаются прямо из таблицы. **`srtt` сервера — это его собственная
приёмная очередь**: 7,3 против 8,9; 22 против 29; 78 против 113; 212 против 258.
Круг, который сервер считает свойством пути, на 70–85 % состоит из времени, что
подтверждение пролежало у него самого. А **клиент не держит ничего**: его очередь
ни разу не превысила треть миллисекунды, и его цена мегабайта не растёт вовсе —
3,6–4,8 мс/МБ на всём шестнадцатикратном диапазоне, пока серверная растёт вчетверо.
Гипотеза об инструменте закрыта: инструмент плоский, растёт сервер.

**И заодно снята версия §7b о `max_rx_gap`.** Воркер возвращается к сокету каждые
4–14 мс, а датаграмма ждёт 80–200. Это не «воркер ушёл», это очередь: за визит
разбирается меньше, чем приходит между визитами. Значит, дорог сам приёмный путь —
и профиль (`perf`, Release, уровень `info`) назвал где:

```
44,0 %  quicloss_on_ack
41,6 %  __detect_lost
 1,6 %  __memmove_avx_unaligned_erms
```

**86 % сервера — в двух функциях, и обе тратят его на одно и то же.** Каждая после
своего прохода восстанавливала `sent_tail` полным обходом списка отправленных:

```c
    if (space->sent == NULL) space->sent_tail = NULL;
    else {
        quicsent_t* tail = space->sent;
        while (tail->next != NULL) tail = tail->next;   /* на каждый ACK */
        space->sent_tail = tail;
    }
```

Список отправленных — это окно перегрузки, тысячи пакетов на большой передаче;
обход на каждое подтверждение и есть та самая квадратичность, которую §7a уже
однажды убрал **из соседнего цикла в этой же функции** — и оставил тремя строками
ниже, дважды.

**Правка.** Хвост поддерживается в самом проходе: цикл помнит последний
оставленный пакет (`kept`), и при удалении хвостового ставит его. Это законно
ровно потому же, почему цикл имеет право останавливаться на `pn > largest`:
удаляется только префикс по возрастанию номеров, поэтому хвост может быть удалён
лишь тогда, когда за ним уже ничего нет, а значит `kept` — новый хвост.

**Что стало** (та же серия, те же файлы):

| размер | CPU сервера | `srtt` | очередь сервера avg | скорость |
|---|---|---|---|---|
| 4 МБ | 5,0 мс/МБ | 0,4–2,2 мс | 0,5–3,3 мс | 272–303 МБ/с |
| 8 МБ | 3,8–6,3 мс/МБ | 4,1–4,5 мс | 6,7–7,9 мс | 293–332 МБ/с |
| 16 МБ | 3,8–5,0 мс/МБ | 6,6–8,1 мс | 11–15 мс | 282–309 МБ/с |
| 32 МБ | 4,1–5,6 мс/МБ | 0,3–14,6 мс | 3–30 мс | 202–313 МБ/с |
| 64 МБ | 4,2–4,7 мс/МБ | 0,45–0,85 мс | 0,6–1,6 мс | 212–241 МБ/с |

**Роста нет** — полка 4–5 мс/МБ на всём диапазоне вместо 7,5 → 33. Это уровень
h2 (§3b: 4,6 мс/МБ), то есть ориентир §7 выполнен не «на 76 %», а вровень.
Передача 64 МБ ускорилась втрое-впятеро (было 40–70 МБ/с), `srtt` упал с 300 мс
до долей миллисекунды, приёмная очередь и `rx_overflow` схлопнулись в ноль:
дорогой приёмный путь и был причиной обоих симптомов, а не их следствием.
В профиле после правки обеих функций нет вовсе.

**Проверено:** 102073 юнит-теста в Release и под ASan; десять живых сценариев
`quicclient` (потери 20 % в обе стороны, реордер, дубликаты, key update,
PATH_CHALLENGE, параллельные потоки); 16 и 64 МБ и потери под ASan — чисто;
40 соединений × 30 потоков = 1200 запросов без единого отказа.

**Два отказа, которые нашлись рядом и НЕ являются регрессией** — проверено
контрольным опытом (сервер, пересобранный с откатанной правкой, ведёт себя так
же): `--cid --migrate` вместе даёт «the connection did not migrate», хотя каждый
флаг по отдельности проходит; `-n ≥ 40` параллельных запросов не завершается
(64 слота потоков у клиента, `-n ≤ 32` проходит). Оба — на стороне клиента
стенда, оба ждут своего разбора.

**Метод, три записи.**

1. **«Поставить чужого» — не единственная форма контрольного опыта.** У вопроса
   «чья это задержка» есть величина, измеримая на обеих сторонах; чужой клиент
   нужен там, где спорят о *поведении*, а не о числе. Здесь хватило метки ядра,
   и это заняло полчаса вместо вечера с докером.
2. **У убранной квадратичности бывают близнецы.** §7a убрал обход из цикла и не
   посмотрел на три строки ниже — а там был тот же обход, и ещё один в соседней
   функции. Убрав такое, стоит перечитать всю функцию и её соседей по файлу.
3. **`srtt` — это не путь.** На петле, где физика даёт микросекунды, 99 % круга
   оказалось собственной приёмной очередью сервера. Прежде чем читать `srtt` как
   свойство сети, надо знать, сколько из него пролежало у тебя.

### 7d. Сравнение с h2 перемерено: «76 %» не воспроизводится (2026-08-13)

Цифра §3b — 9438 req/s против 12484 у h2, «76 %, ориентир §7 выполнен» — снята
**с той самой квадратичностью внутри**, которую убрал §7c, и потому подлежала
перепроверке первой. Стенд тот же: 177 реальных ассетов сайта
(`frontend/docs/.vitepress/dist`), `-n 20000 -c 50 -m 30`, `h2load` с ngtcp2
(`/opt/nghttp2-http3/bin`, 1.71.0-DEV), один и тот же сервер Release с TLS/TCP и
h3 на 18443, уровень лога `error`.

| | req/s (3 прогона) | CPU сервера | на запрос |
|---|---|---|---|
| h2 по TLS/TCP | 20700 / 21500 / 21700 | 0,92 с | **46 мкс** |
| h3 | 4330 / 4020 / 4230 | 4,6–5,0 с | **222–235 мкс** |

**19–21 % от h2, а не 76 %.** Ориентир §7 («не хуже 60 %») на этом стенде
**не выполнен**, и запись §3b о его выполнении недействительна.

**Контрольный опыт — первым делом, а не после гипотез (§3y).** Собран сервер на
`9236e00` — том самом состоянии, где записаны 9438/10258 req/s:

| | h2 | h3 |
|---|---|---|
| код 2026-08-09 (`9236e00`) | 21200–22100 | **3450–3660** |
| код сегодня | 20700–21700 | **4020–4490** |

Сегодняшний код на **20 % быстрее** того, которым получены 9438. Значит,
**регрессии нет**, а невоспроизводима сама цифра: её стенд не записан рядом с
ней и восстановлению не подлежит (h2 при этом даёт 21000 против записанных
12484 — то есть отличается и вторая половина сравнения).

Два подозрения сняты замером, а не рассуждением:

- **гибридный CPU** (i7-12700F: 0–15 — P-ядра, 16–19 — E-ядра; первый профиль
  засветил `cpu_atom`, то есть воркер и правда бывал на E-ядре). Сервер прибит
  `taskset -c 2,3`, клиент — `8-15`: **46 и 222 мкс, ровно те же**;
- **сам клиент**: `h2load` тратит 2,2 с CPU на 4,58 с прогона (полъядра), пока
  серверный поток стоит на 100 %. Узкое место — сервер.

**Куда уходят 222 мкс.** Профиль плоский — ни одного символа выше 3,6 %, то
есть второй квадратичности там нет. Зато он собирается по вызывающим:

| | доля CPU |
|---|---|
| `udp_send` → `sendmsg` | **26 %** |
| `quicendpoint_wake` (в т.ч. `write` в eventfd) | **15 %** |

И счётчики называют почему: **24,8 отправленных датаграмм на запрос** (ассет в
среднем 29 КБ) — то есть ~25 сисколлов `sendmsg` на запрос, по одному на
датаграмму. Приём давно пакетный (`recvmmsg`, 10,8 датаграмм на запрос), отправка
— нет. У h2 те же байты уходят записями по 16 КБ, отсюда 46 мкс против 222.

**Следующий шаг определён замером, а не вкусом:** `sendmmsg` на пути отправки
(очередь `ep->tx_head` уже есть, все датаграммы раунда адресованы одному
`conn->path`, так что пакет собирается тривиально), затем UDP GSO
(`UDP_SEGMENT`) там, где ядро его поддерживает, и коалесцирование пробуждения
(не писать в eventfd, если пробуждение уже висит). Ожидаемо это снимает большую
часть 41 %; остальное — цена QUIC в пространстве пользователя против TCP в ядре,
и её надо мерить отдельно.

**Правило, которое стоило этой перепроверки:** число производительности без
записанного рядом стенда — не число. Ни одна сборка, старая или новая, не
воспроизвела 9438 req/s; вместе с §7b («не на продакшн-уровне лога — не число»)
это значит, что в таблицы §7 идут только числа с указанием конфига, сборки
инструмента и состояния машины.

### 7e. Путь отправки: три правки, вдвое дешевле запрос (2026-08-13)

§7d назвал три статьи расхода, и все три сделаны. Стенд §7d, без изменений.

| | req/s | CPU на запрос | доля от h2 |
|---|---|---|---|
| до | 4020–4490 | 222–235 мкс | 19–21 % |
| `sendmmsg` | 5270–5300 | 188 мкс | 24 % |
| + коалесцирование пробуждения | 6020–6070 | 164–166 мкс | 28 % |
| + UDP GSO | **8630–8800** | **113–116 мкс** | **39 %** |

**+97 % запросов в секунду и вдвое меньше CPU на запрос.** Большие передачи
выросли следом, без отдельной работы: 64 МБ — **609 МБ/с** против 212–241 в §7c
(и 40–70 в начале дня), 32 МБ — 574, 8 МБ — 507.

**1. `sendmmsg` (`udp_tx_batch_*`).** Приём пакетный с фазы 1, отправка была по
датаграмме за сисколл. Теперь у эндпоинта есть передающий пакет, `quicendpoint_send`
кладёт в него копию, а `quicendpoint_send_flush()` отдаёт всё ядру одним вызовом.
Копия обязательна: вызывающий строит датаграмму в буфере, который тут же
переиспользует.

Цена батча — **правило**: всякий путь, порождающий датаграммы, обязан слить перед
возвратом в цикл событий. Слив стоит в четырёх местах, и все они — концы витков:
`__endpoint_read`, `__endpoint_tick` (он же покрывает таймер, очередь пробуждений
и подметание воркера), `quicendpoint_detach` (прощание уходит раньше, чем исчезает
состояние за ним) и закрытие сокета. Датаграмма, оставшаяся в пакете, не
опаздывает — её просто нет.

**2. Пробуждение — одно на очередь, а не на ответ.** `quicendpoint_wake` писала в
eventfd на каждый опубликованный ответ: сисколл, чтобы повторить то, на что
воркер ещё не среагировал ни разу. Теперь флаг `wake_pending`: пишет только тот
производитель, который нашёл его опущенным. **Порядок здесь и есть корректность** —
воркер опускает флаг **до** разбора очереди, а не после: ответ, попавший в очередь
между опусканием и разбором, либо виден этому разбору, либо поднимает флаг сам и
пишет своё пробуждение. Опусти флаг после разбора — и такой ответ будет ждать
таймера. Отсюда же 11 % CPU в `__libc_write`, которых больше нет.

**3. UDP GSO — то, чего `sendmmsg` не даёт.** После первых двух правок доля
`sendmmsg` в профиле не упала (26 → 32 % от меньшего целого): пакетирование
снимает вход в ядро, но не работу ядра **на каждую датаграмму**, а она и
доминирует — 52 мкс на запрос из 164. `UDP_SEGMENT` отдаёт ядру одно сообщение и
размер сегмента, и оно режет его само.

Пакет пересобран под это: датаграммы лежат в одной арене подряд, и подряд идущие
к одному пиру, с одним локальным адресом и одного размера объединяются в одно
сообщение. Правила ядра — «все сегменты равны, кроме последнего» и «не больше 64
сегментов» — выражены прямо: датаграмма меньше текущего размера **закрывает**
серию, больше — начинает свою.

**Запасной путь обязателен и проверен настоящим отказом.** Узнать заранее, умеет
ли ядро (или контейнер) offload, нельзя — первый отказ и есть проба. На `EINVAL`/
`EIO`/`ENOTSUP` пакет навсегда выключает offload, пишет одну строку в лог и
досылает всё содержимое по одной датаграмме — сегменты лежат в арене подряд, так
что их видно и без отдельного учёта. Проверено не рассуждением: сборка, где в
cmsg подсовывается заведомо неприемлемый размер сегмента, дала
`no segmentation offload (errno 22)` **один раз** и передала 8 и 32 МБ побайтово
верно.

**Проверено (всё на GSO-пути):** 102073 юнит-теста Release и ASan; побайтовая
сверка `cmp` для 4/8/16/32/64 МБ — идентично, и под помехами (`--loss-in 10`,
`--loss-in 20 --reorder 20`, `--dup 15 --reorder 10`) тоже; одиннадцать живых
сценариев `quicclient`; ASan-сервер на больших передачах и 30 параллельных
потоках — ноль сообщений и ноль утечек. Для сверки у клиента появился **`--out`**:
ответ правильной *длины* ничего не говорит о пути, который может переставить или
неверно нарезать датаграммы, а это ровно то, что делает offload.

**Мигающий кейс, который НЕ регрессия:** `/big.bin --loss-in 20 --reorder 20`
проходит 4 раза из 10 по seed'ам — и **ровно так же 4 из 10** на сервере
2026-08-09. Контрольный опыт сделан до объяснений (§3y), потому что первым
побуждением было объяснить это GSO. Разбираться с ним стоит, но отдельно.

> **Разобрано в §7g: это был дефект клиента стенда, а не сервера.** Клиент
> считал ответ завершённым по одному FIN, не проверяя, доехало ли то, что перед
> ним. С починкой — 10 из 10.

**Что осталось от 222 мкс.** Профиль после правок ровный: `sendmmsg` ещё заметен,
дальше `memmove`, тело ответа и фильтры — то есть дальнейшее удешевление это уже
не «убрать сисколл», а работа с копиями и с самим h3. Ориентир §7 («не хуже 60 %
от h2») по-прежнему **не выполнен**: 39 %.

### 7f. Дальше — не сисколлы, а вопрос «сколько раз это вызвано» (2026-08-13)

После §7e профиль ровный: ни одного символа выше 5 %, и хвост из полутора
десятков имён по 2–3 %. На таком профиле «оптимизировать самое дорогое» больше не
работает — зато работает другой вопрос: **не «сколько это стоит», а «сколько раз
это вызвано»**.

| | req/s | CPU на запрос |
|---|---|---|
| после §7e | 8630–8800 | 113–116 мкс |
| бюджет записи — раз за виток | 9280–9345 | 106–108 мкс |
| очередь отправки — без общего замка | 9360–9400 | 106 мкс |

**1. Бюджет записи (4 % CPU).** `quicconn_write_room()` обходит все потоки
соединения, и комментарий рядом объяснял, почему это дёшево: «спрашивают раз на
16 КБ». Счётчик сказал другое: **146 раз на запрос, 5176 шагов по списку**.
Причина в устройстве, а не в цикле: write-ahead бюджет — один на соединение
(256 КБ), его делят тридцать потоков, и каждый ответ уезжает дриблингом по
несколько сотен байт; каждая порция обходила список заново.

Первая правка была неверной и это стоило замера: я перенёс запрос бюджета
из цикла наружу — вызовов стало 143 вместо 152, потому что **они шли не из
цикла**. Счётчик пустых входов (`remaining == 0`) показал ноль: все 146 вызовов
были настоящими записями. Только тогда стало видно, что чинить надо не цикл, а
частоту входов.

Сделано так: `h3conn_write` **открывает окно** (`quicconn_budget_open`) на весь
свой виток и закрывает в конце, внутри окна `write_room` отвечает из
запомненного значения, а всё записанное объявляется через `quicconn_note_queued`.
Вне окна — прежний обход.

**Почему именно окно, а не кэш с инвалидацией.** Первая версия была кэшем «до
следующего изменения», и **юнит-тесты его убили за минуту**: `test_h3data`
опустошает поток напрямую `quicsendbuf_mark_sent`, минуя путь отправки, кэш
этого не видел и `write_room` навсегда отвечал «нет места» — восемь падений.
Ровно тот класс дефекта, о котором предупреждал старый комментарий («running
total может разойтись с буферами»). Окно эту зависимость снимает: внутри него
из буферов ничего не уходит, потому что в это время никто не отправляет.

**2. Очередь отправки (152 захвата общего замка на запрос).** `quicconn_want_write`
зовётся на каждую порцию тела и каждый раз брала лежащий на всех соединениях
`tx_lock`, чтобы узнать то, что почти всегда одно и то же: соединение уже в
очереди. Счётчики: **61045 вызовов на 400 запросов и 518 настоящих постановок** —
99,2 % вызовов не делали ничего, заплатив за это разделяемой строкой кэша.
Теперь `in_tx_queue` — атомик, и замок берёт только переход 0→1. Правило
корректности то же, что у флага пробуждения в §7e: **воркер опускает флаг до
работы, а не после**, поэтому опубликованный в этот момент ответ либо попадает в
уже начавшийся виток, либо ставит соединение в очередь заново.

Стоило это, честно говоря, **около процента** — замок был почти неконтендованным,
а 3 % в профиле у `quicendpoint_wake` сидят не в нём. Правка остаётся (99 % вызовов
больше не трогают разделяемую строку), но проекция «уберём замок — уберём 3 %» не
подтвердилась, и это тоже запись в журнал.

**Итог дня по этому стенду:** 4020–4490 → **9360–9400 req/s**, 222–235 → **106 мкс**
на запрос. Вдвое больше запросов и вдвое дешевле каждый; **44 % от h2** (было
19–21 %). Ориентир §7 (60 %) всё ещё не достигнут.

**Проверено:** 102073 юнит-теста Release и ASan; восемь живых сценариев; побайтовая
сверка 8/32/64 МБ; ASan-сервер на 16 МБ, тридцати параллельных потоках и нагрузке
h2load — ноль сообщений и утечек.

**Метод, одна запись.** На ровном профиле следующий шаг находится не в
профилировщике, а в **счётчике вызовов**: три из трёх находок этого дня
(`sent_tail` в §7c, бюджет и очередь здесь) выглядели дешёвыми по одному вызову и
были дорогими по их числу. Профиль называет функцию; сколько раз её позвали,
приходится спрашивать отдельно — и спрашивать до правки, иначе чинишь не то
место (как с циклом выше).

### 7g. Три витка впустую — и «регрессия», которой не было (2026-08-13)

Продолжение §7f тем же способом: **считать вызовы, а не проценты**.

| | req/s | CPU на запрос |
|---|---|---|
| после §7f | 9360–9400 | 106 мкс |
| страж бюджета в `h3conn_write` | 9600 | 103–104 мкс |
| порог `H3_WRITE_MIN_ROOM` | 9490–9805 | 102 мкс |
| счётчик 103-х | **9890–9970** | **100–101 мкс** |

**Счётчик развёл вход и работу.** На запрос — **145 входов в `h3_data_write` и
всего 9 кадров** (в среднем по 3,3 КБ). То есть 136 входов из 145 прогоняли всю
цепочку фильтров, чтобы упереться в исчерпанный бюджет и вернуть BLOCKED.
Причина структурная: write-ahead бюджет один на соединение, его делят тридцать
потоков.

Отсюда три правки, каждая — «не входить туда, где заведомо нечего делать»:
**страж** (`h3conn_write` спрашивает бюджет один раз за виток и пропускает
потоки, которым осталось только тело), **порог** (`H3_WRITE_MIN_ROOM` = 4 КБ:
без него бюджет почти никогда не равен нулю, он равен «мало», и виток тратился на
двести байт) и **счётчик 103-х** (проход по всем потокам ради Early Hints
выполнялся на каждом витке ради возможности, которой почти никто не пользуется).
Потоки, ещё не отдавшие заголовки, страж пропускает всегда: заголовки — не тело,
и придерживать их значило бы задерживать каждый новый ответ за чужим дренажем.

**А потом набор сценариев показал `-n 30` FAIL — и это оказалось важнее всей
оптимизации.**

Бисекция по коммитам (по пять прогонов на точку): `1570024` — 5/5 OK,
`dac5410` (§7e) — 5/5 FAIL. Внутри §7e — выключение offload одной строкой:
**3/3 OK**. Обвинение выглядело построенным: GSO ломает тридцать параллельных
потоков.

**Обвинение было ложным.** Клиент сообщал `body lengths: 0..200000` — один поток
получил всё, двадцать девять ноль, — при том что сервер отвечал
`http3.responses.2xx: 120`, `stream_errors: 0`. Дальше решило одно место в
клиенте:

```c
if (quicclient_stream_fin(client, id) && quicclient_stream_readable(...) == 0)
    complete = 1;   /* FIN пришёл -- значит, всё */
```

**FIN приезжает на своём смещении независимо от того, доехало ли то, что перед
ним.** Потерянный пакет оставляет дыру, `readable()` честно говорит «читать
нечего», FIN уже здесь — и клиент объявляет ответ завершённым с пустым телом.
А объявив, перестаёт читать: ретрансмиссия, которую сервер уже отправил, никому
не нужна. Правильный предикат существовал всё это время — `quicrecvbuf_complete()`
(«FIN известен И прочитано до final_size»), просто клиент его не звал.

Роль GSO — только в том, что он **быстрее**: пачки крупнее, приёмная очередь
клиента переполняется чаще, потерь больше. Он не ломал ничего, он **проявлял**.

С починкой клиента (`quicclient_stream_complete`, три места в `h3client`):
`-n 30` — **5/5**, и заодно тот самый мигавший кейс §7e
`--loss-in 20 --reorder 20` — **10 из 10 вместо 4** (та же причина: на пути с 20 %
потерь дыра перед FIN — обычное дело). Запись §7e о «мигающем кейсе, с которым
стоит разобраться отдельно» тем самым закрыта.

**Что из этого следует для метода.**

1. **Бисекция назвала место, а не причину.** «Выключил GSO — прошло» — верное
   наблюдение и неверный вывод: правка меняла скорость, а скорость меняла
   вероятность потери. Прежде чем чинить обвинённое, стоит спросить, **как именно**
   оно приводит к симптому; здесь ответа не было ни одного, и это был знак.
2. **Сервер отвечал 120 из 120 и сообщал ноль ошибок** — то есть две стороны
   расходились в том, что произошло. Когда клиент и сервер противоречат друг
   другу, первым подозреваемым становится тот, кого никто не тестирует, а это
   всегда харнесс (§3y — про чужой сервер, здесь то же про своего клиента).
3. **Ответ правильной длины и завершённый ответ — разные вопросы**, и `--out`
   из §7e проверял первый. Теперь проверяется второй, и проверяется в клиенте,
   а не глазами.

**Итог дня:** 4020–4490 → **9890–9970 req/s**, 222–235 → **100–101 мкс** на
запрос, **45 % от h2** (22300 req/s, 44 мкс). Ориентир §7 (60 %) не достигнут.

**Проверено:** 102073 юнит-теста Release и ASan; десять живых сценариев; `-n 30`
5/5; мигавший кейс 10/10; побайтовая сверка 8/32/64 МБ; ASan-сервер на 16 МБ,
тридцати потоках и помехах — ноль сообщений и утечек.

### 7h. TSan: сначала убедиться, что параллельность вообще есть (2026-08-13)

Вторая внешняя проверка после §7e–§7g, и на этот раз не провода, а потоков:
день добавил два флага с правилом «опустить до работы, а не после»
(`wake_pending`, `in_tx_queue`), передающий пакет во владении воркера и окно
бюджета записи. Рассуждения о корректности записаны рядом с каждым — то есть
ровно то состояние, в котором инструмент нужнее всего.

Сборка отдельная (`-DSANITIZE=thread`, взаимоисключающий с ASan), хендлеры в
конфиге — из неё же. **Юнит-тесты: 102073/102073, ноль предупреждений.**

**Первая нагрузка не проверила ничего, и это было видно по счётчикам потоков.**
h2load по h3, 2000 запросов, 20 соединений — ноль предупреждений; выглядит как
успех. Но `/proc/<pid>/task/*/stat` говорит другое:

```
Server worker    138 тиков
Server handler     0
Server handler     0
Server handler     0
Server handler     0
```

**Статика отдаётся прямо в воркере**, потоков-обработчиков в этой нагрузке нет
вовсе — а весь изменённый межпоточный путь начинается именно с них. Проверка
измеряла не то, что проверяла (тот же класс ошибки, что §7b и §7c, третий раз за
фазу).

Лечится маршрутом на настоящий хендлер: `app/routes/bench/bench.c` — `delay`
(блокирующий sleep, то есть ответ гарантированно публикуется из потока
обработчика), `early_hints` (тот самый путь, где §7g завёл счётчик 103-х) и
`trailers`. С ними обработчики загружаются, и первая же нагрузка дала
**предупреждение**.

**Гонка — не в сегодняшнем коде, а под ним, и живёт там давно.**

```
Read of size 1 by thread T3:   __is_multiplexed (httpserverhandlers.c:40)
                               __publish_response  <- поток-обработчик
Previous write of size 2 by T1: __ctx_reset (connection_s.c:544)
                               connection_after_write <- воркер
```

Читается `ctx->is_http2`, пишутся `cont_pending`/`cont_sent`. Это **разные поля
одного слова битовых полей**, и запись соседа — read-modify-write всего слова.
Комментарий рядом с `is_http2` разбирает потокобезопасность подробно и
заканчивается словами: «write-once и делает обычное битовое поле безопасным;
всё, что начнёт его переключать позже, обязано выехать из слова». Упущено
одно: **переключать начал сосед**, а не само поле.

Практического вреда на x86 сегодня нет — `is_http2` никто не меняет, и чтение
слова вернёт верный бит в любом порядке. Но это неопределённое поведение по
модели памяти, и одна будущая запись со стороны обработчика превращает его в
потерянное обновление. Починка ровно та, что предписывал комментарий: **байт
вместо бита** (`uint8_t is_http2`), а рядом записано, чем именно старое
рассуждение было неполным.

**После правки — ноль предупреждений** на всём, чем удалось нагрузить:
`delay` по h3 и по h2/TCP, `early_hints`, `trailers`, статика по h3 и h2
одновременно с десятью процессами `quicclient`, плюс остановка сервера с
дренажом. Юнит-тесты зелёные во всех трёх сборках (TSan, Release, ASan), живые
сценарии и побайтовая сверка 32 МБ — тоже.

**Про сегодняшние правки TSan не сказал ничего** — ни про `wake_pending`, ни про
`in_tx_queue`, ни про передающий пакет, ни про окно бюджета. Это и был вопрос,
ради которого он запускался.

**Метод, одна запись.** У санитайзера, как у любого замера, первым делом
проверяется, что он видит нужный код: «ноль предупреждений» при простаивающих
потоках-обработчиках — это не результат, а его отсутствие. Дешёвая проверка —
тики в `/proc/<pid>/task/*/stat` рядом с прогоном.

### 7i. Оба оставшихся отказа стенда — тоже стенд (2026-08-13)

§7g кончился двумя незакрытыми отказами `quicclient`, и §7g же подсказал, где
искать: сначала подозревается тот, кого никто не тестирует. Оба оказались в
клиенте, и оба **выглядели как отказ сервера**.

**`-n ≥ 40` — клиент отказывал молча.** В `h3client_get_many` стоял потолок
`count > CLIENT_MAX_STREAMS / 2` — половина таблицы слотов, то есть 32 из 64.
Просьба о сорока возвращала 0, а `main` печатал `FAIL: not every response
completed`. §3q описывал ровно эту же ловушку при `-n 10` и потолке 8; таблицу
с тех пор увеличили, а деление на два осталось. Теперь предел считается тем, чем
он на самом деле является — слотами: `CLIENT_MAX_STREAMS - H3CLIENT_SERVICE_SLOTS`
(восемь на служебные потоки обеих сторон: наш control и два QPACK, серверные три,
по grease на каждого). **`-n 56` проходит, а на 57 клиент объясняет отказ вслух**
вместо того, чтобы выдать его за серверный.

**`--cid --migrate` — тест ломал соединение перед следующим тестом.** Проверка
идентификаторов **нарочно** заканчивается провокацией: retire номера, который
никто не выдавал (§19.16 — это MUST на протокольную ошибку). Сервер правильно
закрывает соединение, а идущая следом миграция перепривязывает порт и получает
**stateless reset** — тоже правильный, по §10.3. В сумме — `FAIL: the connection
did not migrate`, то есть обвинение серверу за две вещи, которые он сделал по
букве RFC.

Провокация теперь пропускается, когда за ней стоит `--migrate`, и пропуск
печатается строкой. Побочно комбинация стала **новым сценарием, которого не было**:
миграция на повёрнутый connection id — 3 прогона из 3.

**Общее у обоих:** харнесс отказывался делать то, о чём его просили, и сообщал об
этом как о поведении сервера. Правило §7g («когда клиент и сервер противоречат
друг другу, первым подозреваемым идёт тот, кого никто не тестирует») этим
дополняется: **отказ инструмента обязан быть громким**, иначе он неотличим от
дефекта проверяемого.

Двенадцать живых сценариев после обеих правок — зелёные, включая `-n 40`.

### 7j. Отложенный ACK стоил 25 мс на каждый запрос (2026-08-14)

`h2load` по одному запросу за раз (`-m 1`, поведение браузера, открывающего
одиночный ресурс) против живого сервера: **h2 — 34292 req/s, h3 — 3623**.
Разрыв в 9,5 раза там, где §7e–§7g оставили 45 %.

**Форма числа назвала причину раньше кода.** Медиана `request` — 25,18 мс,
p95 — 26,44 мс, s.d. — 2,21 мс: распределение шириной в два процента вокруг
круглого числа. Так выглядит таймер, а не очередь и не сеть — тем более что
`min RTT` в том же прогоне 149 мкс. И 25 мс — это `max_ack_delay` из §18.2.
Одно соединение (`-c 1`) дало ту же медиану при `min 1,03 мс`: **первый запрос
быстрый, каждый следующий ровно на такт**. Нагрузка была ни при чём с самого
начала.

**qlog клиента показал, чего он ждёт.** Ответ приходит в тот же миллисекунд,
что и запрос; следующий запрос уходит **ровно в момент прихода серверного ACK**,
через 24 мс. Клиент ждал не ответ, а подтверждение своего запроса: пока оно не
пришло, поток не закрыт, и `h2load` считает запрос незавершённым.

**Маяки на сервере** (`QUICBEACON`, включается `CWFR_QUIC_BEACON`) закрыли
вопрос со стороны сервера:

```
 1.381  RECV pn=4 elic=1 pending=1 deadline_in=25000   ← запрос
 1.484  APP  stream=8 wrote=109                        ← ответ готов, 0,10 мс
 1.493  SENT pn=6 payload=170 ack_bytes=0              ← ответ уходит БЕЗ ACK
 1.510  TX                                             ← в сокете, 0,13 мс
26.398  BUILD ack_due=1 deadline_in=-23                ← дедлайн
26.442  SENT pn=7 ack_bytes=6                          ← ACK, +25 мс
26.640  RECV pn=6                                      ← следующий запрос
```

Сервер отвечал за **0,13 мс** и держал подтверждение 25 мс. `__build_packet`
писал ACK только под `quicack_should_send`, то есть **только когда ACK сам по
себе был поводом отправить пакет**. Пакет с ответом строился рядом, с местом в
полезной нагрузке, и уезжал без него — `BUILD ack_due=0 pending=1` в маяках
повторяется на каждом ответе.

**Правка** — §13.2.1 буквально: ACK пишется, когда что-то не подтверждено
(`quicack_pending`), а не когда он «созрел». Если попутчиков не нашлось, пакет
не строится вовсе (`p == ack_len && !ack_due`) — отложенный ACK по-прежнему не
выводит на провод пакет ради себя одного. Побочно `quicack_on_sent` переехал
туда, где пакет действительно построен: раньше ACK засчитывался отправленным
перед тремя `return 0` по ошибкам заголовка и AEAD.

**Результат.** `-c 1`: 25,24 мс → **1,26 мс** (Debug+ASan). Release, 100
соединений: **3623 → 6262 req/s**, srtt клиента **76 мс → 90 мкс** — то есть
раздутый srtt §7c имел ещё и эту составляющую.

**Почему §7 этого не увидел.** Все замеры §7d–§7g шли с `-m 30`: при тридцати
потоках в полёте ожидание подтверждения не блокирует ничего, и дефект
невидим — на сегодняшнем стенде с конвейером **h3 даёт 66254 req/s против 63871
у h2**, то есть по пропускной способности вопрос закрыт и был закрыт. Ловушка не
в измерении, а в том, что один параметр стенда исключал целый класс дефектов:
**конвейер маскирует всё, что стоит round-trip'ов**. Профиль нагрузки — часть
измерения, и «быстро при `-m 30`» ничего не говорит о `-m 1`.

**Остаток оказался вторым таймером — и он в h2load.** После правки при `-m 1`
оставалось 6262 против 32030 req/s. Разбор по порядку, каждый шаг снимал
подозреваемого:

- **клиент по CPU** — `-t 4` дал +4 %, четыре отдельных процесса по 25
  соединений — 8170 против 6488 у одного. Не упирается;
- **сервер по CPU** — тики `/proc/<pid>/task/*/stat`: 0,69 с CPU за 1,55 с на
  двух потоках, **загрузка 22 %** (у h2 — 65 %). 69 мкс CPU на запрос против 42
  у h2. Сервер не работал, а ждал;
- **машина** — голый UDP ping-pong на loopback (на Python!) даёт медиану
  **23 мкс**, TCP — 7 мкс. Ядро ни при чём;
- **сервер по времени** — маяки под нагрузкой 100 соединений: `RECV` → `SENT`
  ответа = **17 мкс**, ACK клиента приходит через 117 мкс, а следующий запрос —
  через 8,3 мс.

Обе стороны простаивали, значит ждал таймер. `strace -tt -T` на клиенте назвал
его:

```
402578 sendmsg 1342                          ← запрос
402723 recvmsg  198                          ← ОТВЕТ, через 145 мкс
402904 sendmsg   31                          ← ACK клиента
402960 epoll_wait(timeout=1) = 0 <0.001077>  ← спит ровно 1 мс
404130 sendmsg 1342                          ← следующий запрос
```

На 12 запросов — 10 пробуждений по таймауту; **в режиме h2 тот же h2load ставит
таймаут 59743 мс и по таймауту не просыпается ни разу**, цикл идёт
`write → read → write`. То есть `h2load -m 1 --h3` меряет свой собственный такт
в миллисекунду, а не сервер: 1,15 мс на запрос — это 145 мкс работы и 1 мс сна.
Отсюда и «рост с числом соединений»: за один такт цикл успевает выдать 6–7
запросов из ста.

**Чем мерить вместо этого.** Конвейером, где такт клиента растворяется:
`-c 50 -m 30`, по пять прогонов, медианы — h3 **68903** против h2 **74943**,
то есть **92 %**; ориентир §7 («не хуже 60 %») выполнен с запасом, и разрыв при
`-m 1` к серверу отношения не имеет.

**Отдельный урок про сам стенд, стоивший двух неверных чисел.** Первые прогоны
дали «h3 66254 против h2 63871» и позже «90–118 тыс.» — из этого был сделан
вывод «h3 быстрее h2», и он **неверен**. Причина: сервер слушает под
`SO_REUSEPORT`, поэтому **второй экземпляр не падает с EADDRINUSE, а молча
делит порт** — недобитые процессы предыдущих запусков обслуживали часть
соединений, и замер складывал их пропускную способность. Хуже того, подмена
`libcwfr_framework.so` между A/B-прогонами **не действует на уже запущенный
процесс** (образ отображён), так что «база» и «правка» мерились вперемешку.
Признак, по которому это ловится без разбирательств: **разброс внутри одной
сборки больше разницы между сборками** (было 12–35 тыс. на одном и том же коде).
Проверка перед каждым замером — одна строка: `ps -eo pid,comm,args | awk
'$2=="cwfr"'` должна показывать ровно один процесс. `pgrep -f` для этого не
годится по другой причине: шаблон совпадает с собственной командной строкой
шелла, и `kill` убивает свой же процесс.

**Урок — тот же, что в §7g и §7i, третий раз:** когда клиент и сервер
противоречат друг другу, первым подозревается тот, кого никто не тестирует. И
новое к нему: **если обе стороны недогружены, измеряется не пропускная
способность, а чей-то таймер** — загрузка CPU обеих сторон стоит одной команды и
задаёт весь дальнейший разбор.

### 7k. Кредит на потоки ехал отдельной датаграммой (2026-08-14)

Единственное, что §7j нашёл на нашей стороне: `datagrams_sent` = **2,04 на
запрос**. Вторая — четыре байта `MAX_STREAMS`. Поток освобождается, когда пир
подтверждает ответ, а в этот момент отправлять больше нечего, и `__streams_reap`
безусловно поднимал `want_write` — то есть требовал пакет **ради кредита**.

Сам кадр пишет любой строящийся пакет, если кредит не выдан, поэтому решать надо
было только одно: **стоит ли кредит собственного пакета**. Стоит — когда у пира
кончается запас; не стоит — когда запас есть, потому что пир с запасом откроет
следующий поток, и тот запрос и есть попутка. Порог — четверть объявленного
лимита (минимум 1), запас считается как `announced - next_peer_bidi`; при
`headroom == 0` поведение прежнее, так что случай «пир исчерпал кредит и ждёт»
закрыт как раньше.

**Результат: 2,04 → 1,08 датаграммы на запрос** — вдвое меньше `sendmsg` и вдвое
меньше пакетов в сети. По req/s на loopback — **ничего**: чистый A/B (шесть
пар прогонов, перезапуск сервера перед каждым) дал медианы 57197 против 56940,
то есть −0,4 %, в пределах шума. Здесь пакеты дешёвые; на настоящем пути их
половина — это половина шансов на потерю и половина работы NIC.

Проверено на упоре в лимит потоков (`-c 1 -m 100/150/300` при
`initial_max_streams_bidi` = 100): 3000/3000 без отказов и зависаний. Гейт
`ci.sh` — noh3/asan/tsan/fuzz зелёные.

### 7l. Чужой сервер как мера — единственный способ закрыть спор о цифре

§7j объяснил `-m 1` тактом клиента, и объяснение было **неполным**: при такте
1 мс и ста соединениях потолок был бы много выше наблюдаемых 8 тыс., а четыре
процесса h2load по 25 соединений давали в сумме не больше одного со ста (8848
против 10112) — то есть ограничитель общий, и «во всём виноват клиент» этого не
покрывает. Проверять дальше на своих числах бессмысленно: нужен **тот же клиент
против чужого сервера**.

Он был под рукой всё это время — образы interop-стенда (§3e). ngtcp2 запускается
и вне раннера: `wsslserver '*' <port> <key> <cert> -s -d /www` в контейнере с
`--network host`.

| одинаковый h2load | ngtcp2 | cwfr |
|---|---|---|
| `-c 100 -m 1` | 1163 req/s | **6390** |
| `-c 50 -m 30` | 15432 req/s | **59848** |

**20 тыс. req/s при `-m 1` не даёт ни одна из реализаций** — это свойство связки
«клиент + сценарий», а не нашего сервера, и в этой связке мы впятеро быстрее
эталона. На `-c 1 -m 1` картина иная (ngtcp2 896–1785 против наших 874–1261 при
разбросе в два раза) — единственное место, где отставание возможно; оно требует
отдельного аккуратного замера, а не вывода с трёх прогонов.

**Правило, которого не хватало:** когда спор идёт о числе, а все свои измерения
уже сходятся, следующий шаг — не ещё один свой замер, а **чужая реализация в тех
же условиях**. Она отвечает на вопрос «сколько тут вообще возможно», на который
собственный код ответить не может.

### 7m. Стенд из nginx и HAProxy — постоянный ориентир (2026-08-14)

Интероп-эндпоинты (§7l) — примеры из `examples/`, и мерить по ним
производительность нечестно. Ориентир должны задавать зрелые серверы, решающие
ту же задачу: отдачу маленького статического файла.

```bash
# nginx 1.31 (mainline-alpine собран --with-http_v3_module)
docker run -d --name ngx --network host --ulimit nofile=65536:65536 \
  -v $PWD/nginx.conf:/etc/nginx/nginx.conf:ro -v <certs>:/c:ro -v $PWD/www:/www:ro \
  nginx:mainline-alpine          # listen 4444 quic reuseport; listen 4444 ssl; http2 on

# HAProxy 3.2 -- ВАЖНО: официальный haproxy:3.1 собран с -QUIC, а 3.2 с +QUIC
docker run -d --name hap --network host --ulimit nofile=65536:65536 \
  -v $PWD/haproxy.cfg:/usr/local/etc/haproxy/haproxy.cfg:ro \
  -v $PWD/haproxy.pem:/c/haproxy.pem:ro -v $PWD/www:/www:ro haproxy:3.2
  # bind quic4@:4445 ssl crt /c/haproxy.pem alpn h3
```

Две мелочи, каждая стоит одного падения: PEM для HAProxy — это `cert` и `key` в
одном файле, и он должен быть **читаем не только владельцем** (контейнер работает
не от root); `worker_processes`/`nbthread` выставлены в 2, как воркеров у cwfr.

Все три сервера отдают один и тот же 109-байтовый `robots.txt`; медианы по 3–7
прогонам, `h2load` тот же:

| | h3 `-c100 -m1` | h3 `-c50 -m30` | h2 `-c100 -m1` | h2 `-c50 -m30` |
|---|---|---|---|---|
| **cwfr** | **9475** | **84344** | 34940 | 84541 |
| nginx | 8527 | 78286 | 38714 | 97608 |
| HAProxy | 4434 | 35108 | 40742 | 154030 |

**Ответ на вопрос, ради которого стенд поднимался: 20 тыс. req/s по h3 при
`-m 1` не выдаёт никто.** nginx — 8527, мы — 9475. И отношение h3 к h2 в этом
режиме одинаковое у всех: 22 % у cwfr, 22 % у nginx, 11 % у HAProxy. Это форма
самого сценария (один запрос в полёте на соединение поверх QUIC у клиента,
который между запросами спит), а не свойство реализации: CPU клиента при этом
32–35 % против обоих серверов.

По h3 мы **не медленнее nginx** ни в одном из двух режимов, а на конвейере
опережаем его на 8 %. Отставание есть по h2 на конвейере (84,5 тыс. против 97,6
у nginx и 154 у HAProxy — последний отдаёт тело из памяти, а не из файла) — это
уже вопрос к h2-пути, не к h3.

## 8. CI

Добавить в существующий прогон:

1. Сборка с `-DINCLUDE_HTTP3=no` — регрессия «h3 ничего не сломал».
2. Сборка с `-DINCLUDE_HTTP3=yes -DBUILD_TESTS=yes`, `runner` (ASan).
3. То же под TSan.
4. Короткий фаззинг-прогон (60 с на цель) на каждый PR, длинный — по расписанию.
5. Hard reload с незавершённым QUIC handshake: старые worker-потоки должны
   завершиться, новое поколение — обслужить h3-запрос.
6. `h3spec` против поднятого сервера.
7. quic-interop-runner против 3–4 клиентов (полная матрица — по расписанию,
   она долгая).

### 8a. Сделано: `tests/ci.sh` (обновлено 2026-08-15)

**«Добавить в существующий прогон» упиралось в то, что прогона нет:** в
репозитории нет ни GitHub Actions, ни GitLab CI, ни чего-либо ещё, и выбор CI —
не решение фазы 8. Поэтому список выше написан скриптом, который запускается
откуда угодно, а задача CI сводится к одному вызову.

```bash
tests/ci.sh                      # все стадии
tests/ci.sh release              # полный обязательный release-gate
tests/ci.sh h3unit asan tsan     # подмножество, в указанном порядке
tests/ci.sh limits               # connection/memory exhaustion и drain
SOAK_REQUESTS=10000 tests/ci.sh soak
tests/ci.sh earlydata            # 0-RTT: приём, счётчики и анти-повтор
tests/ci.sh vn                   # неизвестная версия QUIC: ответ, молчание, лимит
BENCH_RECORD=/var/lib/cwfr/h3-baseline.json tests/ci.sh benchmark
BENCH_BASELINE=/var/lib/cwfr/h3-baseline.json tests/ci.sh benchmark
FUZZ_SECONDS=600 tests/ci.sh fuzz
H3SPEC=/path/to/h3spec tests/ci.sh h3spec
REQUIRE_H3SPEC=1 tests/ci.sh h3spec  # только обязательный h3spec
```

| стадия | что делает | пункт §8 |
|---|---|---|
| `noh3` | сборка без h3 + юнит-тесты | 1 |
| `h3unit` | отдельный QUIC/H3/QPACK runner | 1–3 |
| `config` | неверные типы/диапазоны HTTP/3 останавливают запуск; неверный reload-кандидат не останавливает текущее поколение | 1–3, 5 |
| `limits` | 64 занятых QUIC slots, отказ лишним соединениям, CRYPTO memory refusal и возврат обоих gauges к нулю | 3, 7 |
| `soak` | 1000 запросов обычно, 10000 в release; loss/reorder/dup, migration, drain gauges и RSS около прогретого baseline | 7 |
| `earlydata` | 0-RTT: запрос едет в early data и обслуживается, счётчики `early_data.*` растут, флайт с незавершённым рукопожатием **не** доходит до диспетчера (`http3.requests` не растёт), и без ключа early data отвергается | 1–3 |
| `vn` | датаграмма 1200 байт с версией QUIC v2 получает Version Negotiation (v1 + резервная версия, cid'ы переставлены, ответ меньше запроса, сама v2 не предлагается); на 1199 байт сервер молчит; v1 не получает VN; `http3_version_negotiation_rate` ограничивает поток | 1–3, 7 |
| `benchmark` | медиана 5 прогонов: short req/s, обязательный h2load-профиль 57074 байта `-n10000 -c100 -m30` и загрузка 64 МиБ; FAIL при неполных/не-2xx ответах или деградации любой метрики более 5% | 7 |
| `asan` | сборка с h3 (ASan+LSan), unit + hard/soft reload | 2, 5 |
| `tsan` | сборка с h3 (TSan), unit + hard/soft reload | 3, 5 |
| `fuzz` | семь целей по `FUZZ_SECONDS` каждая | 4 |
| `reload` | hard reload при живом QUIC: проверяет уход старых TID и запрос к новому поколению | 5 |
| `softreload` | общий UDP socket: незавершённый старый ответ, новая конфигурация и drain старых TID | 5 |
| `h3spec` | поднимает свой сервер и гоняет h3spec | 6 |

Benchmark baseline намеренно не хранится в репозитории: loopback throughput и
req/s зависят от CPU, kernel, OpenSSL и topology runner-а сильнее допустимых
5%. Его снимают `BENCH_RECORD` на закреплённом runner-е и передают следующей
сборке через `BENCH_BASELINE`. Формат фиксирует также short-client method;
baseline от h2load нельзя случайно сравнить с fallback-методикой. Baseline
содержит три независимых числа: `short_rps`, `representative_rps` и
`throughput_mbps`; старый двухметричный файл отвергается и должен быть один раз
перезаписан. Без baseline
локальная стадия — `SKIP`, а `tests/ci.sh release` — `FAIL`.
Перед выборкой прогреваются CPU/OpenSSL и page cache; без этого ondemand CPU
governor занижал первый short sample в 2–3 раза и делал 5%-й порог фикцией.
Контрольный A/B после прогрева: baseline 3060.60 req/s и 550.6 МБ/с, повтор
3025.79 req/s и 530.0 МБ/с; отклонения −1.14% и −3.74%, gate — `OK`.

При наличии harness предпочитает `/opt/nghttp2-http3/bin/h2load` системному
бинарнику. Проверенный A/B с nghttp2 1.71.0-DEV: baseline 167868.32 req/s и
582.4 МБ/с, повтор 163080.26 req/s и 562.4 МБ/с; −2.85% и −3.43%, gate `OK`.
Первый профиль `-c100 -m30` дал тысячи 404 не из-за HTTP/3: soft
`RLIMIT_NOFILE=1024` не вмещал до 3000 одновременно открытых статических
ответов, а `http_response_file` ошибочно называл любой отказ `open()` отсутствием
файла. Benchmark теперь требует limit не меньше 8192; `EMFILE`, `ENFILE` и
`ENOMEM` сервер отвечает 503. После этого профиль завершает 10000/10000 с 2xx.

Сопоставимый H2/H3 контроль 2026-08-15 выполнен одним бинарником
`/opt/nghttp2-http3/bin/h2load` и одним Release-сервером. Для short response
использованы одинаковые `-n20000 -c10 -m30`, пять прогонов и медиана; для
throughput — пять загрузок одного прогретого файла 64 МиБ:

| Метрика | H2 | H3 | H3/H2 |
|---|---:|---:|---:|
| short response | 147739.95 req/s | 131572.89 req/s | 89.1% |
| 64 МиБ | 49.84 мс, ≈1285 МБ/с | 84.78 мс, 755.37 МБ/с | 58.8% |

Все short-прогоны завершили 20000/20000 с 2xx; все загрузки передали ровно
67108864 байта. Это loopback-сравнение верхнего CPU/syscall-потолка, а не
прогноз скорости через реальную сеть: у H3 здесь дороже AEAD и UDP packet path,
тогда как преимуществ при loss/смене пути стенд без сети намеренно не даёт.

Полный локальный release-soak 2026-08-15 прошёл: 10000/10000 запросов, затем
детерминированные loss/reorder/dup и migration; после закрытия
`connections.current` и `memory.current_bytes` вернулись к нулю, RSS — с
16860 до 20664 КиБ относительно прогретого baseline (рост 3804 КиБ).

`h3unit` намеренно отделён от общего runner: регрессия другого протокола не
скрывает состояние QUIC/H3/QPACK. Он остаётся дополнительным gate и не заменяет
полный unit-прогон.

Memory-budget unit-набор проверяет точный reserve/refuse/rollback/free для
receive segments, send capacity и CRYPTO reassembly, включая переполнение
offset/size до выделения памяти. После аудита 2026-08-15 полный результат —
1921/1921; `h3unit`, ASan hard/soft reload и TSan hard/soft reload зелёные.

Пять вещей, ради которых это скрипт, а не строчка в yaml:

- **h3spec-стадия поднимает сервер сама** — своим конфигом, с
  **закоммиченным тестовым сертификатом** (`tests/data/quic_test_cert.pem`, тот
  же, которым пользуется юнит-тест TLS-моста). Ничего не нужно настраивать на
  машине, и нечему разойтись между машинами;
- **локально `h3spec` опционален**: без бинарника стадия сообщает `SKIP`.
  Release-job обязан запускать `tests/ci.sh release`; этот режим сам делает
  h3spec обязательным, и отсутствие бинарника становится `FAIL`;
- **TSan живёт в своём дереве сборки и со своим окружением** — он не линкуется с
  ASan, и `ASAN_OPTIONS` для него сбрасывается;
- **фаззинг не пишет в рабочую копию**: корпус из дерева копируется в каталог
  сборки, находки остаются там же, падающие входы — в `$CI_BUILD_DIR/crashes`.
  Если окружение явно сообщает, что LeakSanitizer недоступен, цель один раз
  повторяется с `detect_leaks=0`; любой иной ненулевой exit остаётся crash;
- **reload-стадия проверяет жизненный цикл потока, а не только доступность**:
  `tests/h3_hard_reload.sh` запоминает TID двух `Server worker`, удерживает
  незавершённый QUIC handshake и до сигнала проверяет через `/metrics`, что
  `quic.handshakes.inflight > 0`; затем посылает `SIGUSR1` и требует, чтобы оба
  старых TID исчезли, а два новых worker обслужили HTTP/3-запрос. Поэтому утечка
  старого поколения не маскируется успешно запущенным новым поколением.
  Дополнительный unit-кейс вызывает завершение handler после `detached=1` при
  заведомо отсутствующем endpoint: поздний ответ остаётся собственностью
  stream teardown и не ставит QUIC write/wakeup, что закрепляет границу гонки
  hard reload с асинхронной публикацией;

Для этого `quicclient --loss-in` действует уже во время handshake. Клиентский
цикл `quicclient_run()` обязан читать через impairment shim, как и последующий
`quicclient_pump()`; прямой `recv()` здесь делает тест потерь ложноположительным:
в выводе заявлено 100 % входящих потерь, но handshake успевает завершиться.
Тот же цикл принимает `--timeout`: значение относится и к handshake, и к
HTTP/3-обмену (а также к обоим соединениям сценария `--new-token`), поэтому
короткий отрицательный тест не скрыто ждёт жёстко заданные пять секунд.

`tests/h3_soft_reload.sh` использует отдельную наблюдаемую точку: старый клиент
получает первые DATA большого ответа и приостанавливает чтение. После `SIGUSR1`
старый CID обязан получить оставшиеся байты из старого root, параллельное новое
соединение — файл из нового root, а после клиентского CONNECTION_CLOSE два
старых `Server worker` TID обязаны исчезнуть. Пауза до отправки request для этой
проверки неверна: idle-соединение уже не входит в drain set и может быть законно
закрыто после GOAWAY.

Под TSan сценарий запускается с `halt_on_error=1` без suppressions. Управляющие
сигналы принимает main thread через `sigwait()`, поэтому reload выполняется в
обычном thread context. Именно полный прогон обнаружил гонки между
`quicconn_recv()` нового reader и `quicconn_next_timeout()` старого endpoint,
раннюю публикацию незаполненного `appconfig`, а также неатомарные reload-policy
и диагностический beacon; после исправлений soft и hard reload проходят.

Оба reload-сценария входят непосредственно в стадии `asan` и `tsan` файла
`tests/ci.sh`, а не остаются ручной проверкой поверх Release. Отдельные стадии
`reload`/`softreload` сохраняются как быстрые production-build integration
гейты; sanitizer-варианты проверяют lifetime и гонки того же живого пути.

Fuzz-стадия перечисляет семь обязательных целей явно. Отсутствующий или
несобранный бинарник считается `FAIL`; пустой glob больше не может превратить
сломавшуюся fuzz-сборку в ложный зелёный результат.

**Пункт 7 (interop) сознательно не включён**: он требует докера и десятков минут,
то есть принадлежит расписанию, а не гейту. Как его гонять — в
`docs/http3/08` §3e и в записях стенда.

## 9. Порядок отладки, когда «не работает»

Опыт h2 показывает, что это стоит записать заранее:

1. Долетают ли датаграммы вообще — `quic.datagrams_received` в `/metrics`.
   Ноль → firewall/UDP-порт (самая частая причина).
2. Отбрасываются ли — `quic.datagrams_dropped{reason}` называет причину.
3. Проходит ли рукопожатие — `quic.handshakes_completed`. Не проходит →
   Wireshark + `SSLKEYLOGFILE`; чаще всего дело в transport parameters или в
   том, что серверный flight упёрся в anti-amplification.
4. Рукопожатие есть, запросов нет → h3-слой: не отправлен SETTINGS, не открыт
   control-поток, QPACK заблокирован.
5. Работает, но медленно → qlog + qvis: смотреть cwnd и `bytes_in_flight`.
   Плоский cwnd = упёрлись в flow control, а не в перегрузку.
6. Работает у одних клиентов и не работает у других → почти всегда QPACK или
   ошибка в трактовке допустимых кадров/настроек; сверяться с h3spec.

**Ловушка, известная из h2** (`docs/http2/09`): ASan **не** сообщил об
use-after-free декодированного значения заголовка, потому что единственным
чтением был формат `log_info` — неинструментированная libc. Если что-то ведёт
себя как порча памяти, а ASan молчит, добавить `memcmp` по значению: там это
сработало мгновенно.

---

### 7n. Несколько больших файлов в одном соединении ехали задом наперёд (2026-08-15)

**Жалоба.** «Один поток не справляется с раздачей нескольких больших файлов в
одном соединении h3, время загрузки контента разрастается.» Гипотеза в самой
жалобе — упор в CPU воркера.

**Контрольный опыт до гипотез.** Первый замер (ASan-сборка, `quicclient -n N`,
файл 1 МБ) показал ровно то, что подтверждало бы жалобу: 11 МБ/с и рост цены
байта втрое от n=1 к n=8. **Обе цифры оказались артефактом ASan.** Тот же замер
в Release: 150–450 МБ/с, цена байта плоская в пределах шума. Правило §7 (мерить
перегрузку и CPU только в Release) стоило здесь получаса.

Второй контроль — кто вообще упирается. Клиент и сервер оба стояли на ~100 %
CPU, что не отвечает на вопрос. Ответ дал опыт «та же нагрузка, но двумя
клиентами»: 1×16 потоков → 336 МБ/с, 2×8 потоков → 208 МБ/с. Больше клиентов не
дали больше байт, значит упор серверный. Но потолок в сотни МБ/с на loopback
лежит настолько выше любого реального канала, что **жалоба про CPU не
подтвердилась**.

**Что нашлось на самом деле.** `log_debug` в отправке уже печатает id потока на
каждый STREAM-кадр — включаем `level: debug` и читаем последовательность.
Четыре запроса по 1 МБ, 3378 отправок:

```
поток 12: #   4 .. # 846      <- запрошен последним, отдан первым
поток  8: # 847 .. #1690
поток  4: #1691 .. #2533
поток  0: #2534 .. #3377      <- запрошен первым, отдан последним
```

Строго последовательно и строго **в обратном порядке запросов**. Причина в двух
строчках, лежащих далеко друг от друга: список потоков строился вставкой в
голову (`s->next = conn->streams`), а цикл наполнения пакета идёт от головы и
отдаёт первому же готовому потоку столько пакета, сколько тот возьмёт. Голова —
самый новый поток; он держит соединение до конца своего ответа. Комментарий над
циклом при этом утверждал «round-robin, чтобы один большой ответ не заморил
остальные» — кода, который бы это делал, не существовало никогда.

Для страницы это ровно «время загрузки контента разрастается»: первый
запрошенный ассет (обычно критический CSS) приезжает последним, после того как
докачаются все остальные.

**Правка.** Вставка в хвост (`__stream_append` + `conn->streams_tail`). Порядок
списка = порядок открытия: однонаправленные control/QPACK-потоки, открытые на
рукопожатии, остаются впереди, дальше запросы по возрастанию id. Это и есть
рекомендация RFC 9218 §7 для ответов равной срочности и не-инкрементальных:
отдавать по одному, начиная с меньшего id, потому что ресурс, полезный только
целиком, стоит дочитать. Заблокированный поток по-прежнему пропускает ход
следующему, так что соединение не простаивает.

Хвост, а не обход до конца: `__stream_ensure` открывает все потоки до id,
которым воспользовался пир, — обход внутри этого цикла квадратичен по величине,
которую выбирает пир (`initial_max_streams_bidi` доходит до 65536). Хвост при
удалении не патчится, а пересобирается из обхода, который `__streams_reap` и так
делает каждый ход: восстанавливать предыдущий узел из `quicstream_t**` — тот
самый указательный трюк, который читается нормально и оказывается неверным.

После правки те же 3378 отправок идут `0 → 4 → 8 → 12`: первый запрошенный файл
готов на 25 % передачи вместо 100 %. Суммарное время не изменилось — изменилось,
что приезжает раньше.

**Тест.** `test_quic_stand_stream_order`: на старом поведении падает
(`expected 0, got 8`), на новом проходит. Проверено возвратом вставки в голову.

**Чего замер сделать не смог.** Пропускная способность на loopback скачет в
2–3 раза от прогона к прогону даже с `taskset` (n=4: 125, 362, 399 МБ/с). Роста
цены байта с числом потоков в этих данных не видно надёжно — цикл наполнения
пакета обходит все потоки на каждый пакет, и это стоит проверить отдельно, но
не этими цифрами. Пункт заведён как `09` §2.7, вместе с тем, чем его мерить:
счётчиком посещений узлов на пакет, а не пропускной способностью на loopback. Ещё одна ловушка того же дня: замер после диагностики шёл с
забытым `level: debug`, и syslog на каждый STREAM-кадр «показал» падение до
72 МБ/с.

### 7o. Профиль 57 КБ × 10 000 выявил разрыв с h2 (2026-08-15)

Боевой прогон `/opt/nghttp2-http3/bin/h2load -n10000 -c100 -m30` на ответе
57074 байта дал 8189.81 req/s и 446.71 МБ/с по h3 против 24041.93 req/s и
1.28 ГБ/с по h2. Все ответы были 2xx, QUIC-клиент не зарегистрировал потерь.
Это не вариация короткого loopback-теста: h3 получил только 34 % req/s и около
35 % полосы h2, медиана запроса выросла с 42.75 до 235.46 мс.

Старый performance-gate этого не видел: короткий профиль использует `c=10`, а
64-МиБ throughput-профиль — одно соединение. Ни один не создаёт одновременно
сотню congestion-control/pacing/AEAD контекстов и не проверяет цену примерно
460 тысяч исходящих UDP-датаграмм.

Перед оптимизацией добавлена недостающая телеметрия transmit batching:

| `/metrics` | Что означает |
|---|---|
| `quic.send.batch_calls` | вызовы `sendmmsg` из endpoint batch |
| `quic.send.messages` | UDP-сообщения, переданные этим вызовам |
| `quic.send.gso_messages` | сообщения с `UDP_SEGMENT`, принятые ядром |
| `quic.send.gso_segments` | датаграммы внутри принятых GSO-сообщений |
| `quic.send.gso_fallbacks` | отказы ядра от GSO с переходом на одиночные send |
| `quic.send.partial` | сообщения в коротких `sendmmsg`, которые ядро не приняло |

Для одного интервала полезны отношения `datagrams_sent / send.batch_calls`
(эффективный размер batch) и `send.gso_segments / send.gso_messages`
(эффективный размер GSO). `gso_fallbacks > 0` означает, что заявленное наличие
GSO на этой машине фактически не работает; `gso_messages == 0` при большом
ответе означает, что соседние пакеты одного пути не доходят до batch вместе.
Только после этих чисел можно выбирать между укрупнением batch, сокращением
пробуждений, пакетным AEAD и изменением worker topology. Отключать pacing ради
красивого loopback-результата нельзя: это меняет сетевую семантику и маскирует
переполнение очередей на реальном пути.

Корректный `perf` (Release, `RLIMIT_NOFILE=8192`, три раза по 10000/10000 2xx)
дал 8558–9512 req/s и назвал цену иначе, чем первоначальная гипотеза:
`quiccrypto_seal` занял 0.36 %, OpenSSL encrypt primitives — около 1.4 %.
Пакетный AEAD поэтому не является первым рычагом. Крупнейший отдельный символ —
`memmove` (9.59 %); HTTP/3/QUIC orchestration распределена между
`quicconn_send`, `h3conn_write`, filter chain и поисками stream/request.

Попытка заменить линейный send buffer кольцом действительно убрала compaction,
но A/B ухудшила representative median с 9611 до 8437 req/s (−12.2 %) и
throughput с 569.1 до 548.8 МБ/с. Правка отклонена: wrapped chunks и более
дорогой growth path оказались хуже измеренной копии. Из подтверждённого
малорискового overhead устранён внешний вызов `h3conn_request_of`: два pointer
check теперь inline (сам символ вместе с PLT занимал 3.7 %). После правки:
9653.70 req/s и 568.7 МБ/с; representative/throughput остались на baseline,
short-профиль вырос с 167448 до 203417 req/s, но из-за шума loopback это не
объявляется общим ускорением без повторов на закреплённом runner-е.

Следующий O(N)-участок — `h3conn_stream_by_response` — нельзя просто перенести
в публичный `httpresponse_t`: это изменило бы ABI обработчиков ради детали H3.
Вместо этого первый найденный `quicstream_t` кэшируется в приватном состоянии
terminal H3 write-filter и очищается его `reset`. Первый поиск при публикации
остаётся под connection lock и сохраняет проверку ownership; повторные header/
body passes одного ответа больше список не обходят.

Два независимых A/B после кэша дали representative **11392** и **10806 req/s**
против baseline 9611 (+18.5 % и +12.4 %), каждый 10000/10000 2xx. Short-профиль
остался в шумовом диапазоне; одиночный throughput дал 523–526 МБ/с и требует
отдельного повтора на закреплённом runner-е, поскольку этот кэш на одном потоке
с одним ответом почти не исполняется. Lifetime проверен полным H3 runner под
ASan+LSan: 1924/1924, ошибок и утечек нет.

---

### 8b. Version negotiation: поведение без теста выше юнита (2026-08-16)

**Как нашлось.** Обсуждали, стоит ли реализовывать QUIC v2 (RFC 9369), и ответ
получился «нет, потребителя нет» — он записан в `09` §3.2. Но у этого ответа
есть условие, которое до сегодня никем не проверялось: отказ от v2 безопасен
ровно потому, что клиент чужой версии получает от нас Version Negotiation и
переходит на v1. То есть единственная страховка за весь класс «пир из будущего»
— один путь в `quicendpoint.c`, и тестов на него не было ни одного выше юнита.

Проверено грепом, а не памятью: `tests/unit/test_quic_invariants.c` разбирает
байты кодировщика (`quic_invariants_write_version_negotiation`), в `tests/ci.sh`
стадии про VN нет, а `quicclient.c` вообще не мог послать чужую версию —
`hdr.version = QUIC_VERSION_1` зашито в единственном месте сборки пакета. Иначе
говоря, проверялось, что мы умеем собрать такой пакет, но не что работающий
сервер его отправляет.

Уточнение, чтобы запись была честной: **один раз это проверялось живьём** — в
фазе 1 пробником `probe.py` на 25 проверок (`01` §«Проверено на живом
сервере»). Но его в дереве нет и никогда не было: `git log --all -- '*probe.py'`
пуст. Разовая проверка руками — не тест; между фазой 1 и сегодня путь пережил
шесть фаз, лимитер, soft reload и переезд на общий UDP-сокет, и ничто не сказало
бы, если бы он сломался.

**Почему зонд — не соединение.** Весь остальной клиент говорит на v1 и только на
нём; чтобы построить пакет неизвестной версии, ему пришлось бы иметь ключи,
которых для этой версии не существует. Поэтому `quicclient_probe_version()`
устроен отдельно от `quicclient_t`: сокет, один long header через
`quicpkt_write_header` с произвольным полем версии, случайное тело (не нули —
нули после заголовка читаются как padding между склеенными пакетами, §12.2), и
разбор ответа **инвариантами** (RFC 8999), потому что ответ чужой версии больше
ничем читать нельзя.

**Что проверяется — шесть утверждений, а не одно «ответил».**

| Проверка | Почему именно она |
|---|---|
| Ответ есть и это VN | базовое поведение §6.1 |
| Cid'ы переставлены | §17.2.1: наш SCID становится их DCID; ошибка здесь даёт пакет, который настоящий клиент **молча** игнорирует |
| Предложена v1 | иначе переходить некуда |
| Предложена резервная версия `0x?a?a?a?a` | §15: клиент не должен полагаться на точный список |
| Запрошенная версия **не** предложена | сервер не должен утверждать, что умеет то, чего не умеет |
| Ответ меньше запроса | §8: пакет уходит на непроверенный адрес, усиления быть не должно |

Плюс вторая половина правила §14.1: та же датаграмма на **один байт меньше**
1200 не должна получить ничего. Размер выбран впритык к порогу нарочно —
интересный отказ здесь не «сервер отвечает на 100 байт», а off-by-one в
сравнении.

**Результат первого прогона (`tests/h3_version_negotiation.sh`):**

```
probed version:            0x6b3343cf      (QUIC v2, RFC 9369)
version negotiation:       yes
connection ids echoed:     yes
version 1 offered:         yes
reserved version offered:  yes
probed version offered:    no
reply / probe bytes:       31 / 1200
undersized probe answered: no
```

Все шесть плюс молчание — с первого раза, дефектов не найдено. Это ожидаемый и
не бесполезный исход: цена теста здесь не в найденном баге, а в том, что
единственная страховка за отказ от v2 перестала быть непроверенной.

**Контрольный опыт обязателен, иначе тест ничего не значит.** Зонд с версией
`00000001` — то есть нашей собственной — прогнан против того же сервера и дал
`version negotiation: no`, выход 1, `FAIL`. Без этого шага набор проверок мог бы
проходить на сервере, который отвечает VN вообще всем и потому не может
обслужить никого. Он же вынесен в скрипт отдельным шагом (`--version 00000001
--version-flood 3`, ноль VN в ответ).

**Лимит проверяется отдельно и по счётчику.** VN — ответ на непроверенный адрес,
то есть ровно то, что атакующий хотел бы вызывать потоком. Сервер поднимается
второй раз с `http3_version_negotiation_rate: 1`, `burst: 1`, восемь зондов
подряд дают 2 ответа (два токена за время прогона) и `quic.drop.no_budget`
+6. Пять повторов подряд — 2/2/2/2/2, мигания нет; отдельный прогон под ASan
чист.

**Что этот тест не может.** Он не проверяет, что клиент после VN действительно
переходит на v1 и завершает рукопожатие: переходить ему не с чего — v2 у нас
нет. Это остаётся за interop-раннером (§2), где вторая сторона — чужая
реализация. Формулировка «сервер отвечает правильно» здесь честная, «fallback
работает» — нет.

Стадия гейта: `tests/ci.sh vn` (порт 18463, Release-дерево, ~9 с).
